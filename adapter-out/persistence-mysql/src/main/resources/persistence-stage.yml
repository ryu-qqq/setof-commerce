# ============================================================
# Persistence Layer 스테이징 환경 설정
# ============================================================
# 활성화: spring.profiles.active=stage
# ⚠️ 모든 민감 정보는 환경 변수로 관리
# ============================================================

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST}:${DB_PORT:3306}/${DB_NAME}?useSSL=true&requireSSL=true&serverTimezone=Asia/Seoul
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

    hikari:
      # Pool Size (스테이징: 중간)
      maximum-pool-size: 10
      minimum-idle: 5

      # Timeout
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

      # Leak Detection (스테이징: 활성화)
      leak-detection-threshold: 60000

      pool-name: HikariPool-Stage

      # Connection Health Check
      connection-init-sql: SELECT 1

      # MySQL 최적화 (스테이징)
      data-source-properties:
        cachePrepStmts: true
        prepStmtCacheSize: 250
        prepStmtCacheSqlLimit: 2048
        useServerPrepStmts: true
        rewriteBatchedStatements: true
        cacheResultSetMetadata: true
        cacheServerConfiguration: true

  jpa:
    properties:
      hibernate:
        format_sql: false
        use_sql_comments: false

  flyway:
    # ⚠️ 스테이징에서도 clean 비활성화
    clean-disabled: true

logging:
  level:
    com.ryuqq: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: DEBUG
