# ============================================================
# Persistence Legacy Layer 운영 환경 설정
# ============================================================
# 활성화: spring.profiles.active=prod
#
# 운영에서 레거시 엔티티/리포지토리를 활성화합니다.
# @since 1.1.0
# ============================================================

persistence:
  legacy:
    enabled: true
