# ============================================================
# Persistence Legacy Layer 스테이지 환경 설정
# ============================================================
# 활성화: spring.profiles.active=stage
#
# 스테이지에서 레거시 엔티티/리포지토리를 활성화합니다.
# @since 1.1.0
# ============================================================

persistence:
  legacy:
    enabled: true
    seller:
      enabled: true
    category:
      enabled: true
    brand:
      enabled: true
    board:
      enabled: true
    faq:
      enabled: true
