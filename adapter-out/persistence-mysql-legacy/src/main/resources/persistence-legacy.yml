# ============================================================
# Persistence Legacy Layer 공통 설정
# ============================================================
# Strangler Fig 패턴: 레거시 DB 스키마 접근 설정
# 기본값 비활성화 - 환경별 설정에서 활성화
#
# 활성화 시 LegacyJpaConfig가 레거시 엔티티/리포지토리를 등록합니다.
# 동일 DataSource를 공유하므로 별도 DB 연결 설정 불필요.
#
# @since 1.1.0
# ============================================================

persistence:
  legacy:
    enabled: false
    seller:
      enabled: false
    category:
      enabled: false
    brand:
      enabled: false
    faq:
      enabled: false
    productgroup:
      enabled: false
    member:
      enabled: false
    refundaccount:
      enabled: false
    shippingaddress:
      enabled: false
    wishlist:
      enabled: false
