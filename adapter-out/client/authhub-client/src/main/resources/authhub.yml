# ============================================================
# AuthHub SDK 공통 설정
# ============================================================
# 환경별 설정은 authhub-{profile}.yml에서 override
# ============================================================

authhub:
  timeout:
    connect: 5s
    read: 30s
  retry:
    enabled: true
    max-attempts: 3
    delay: 1s
