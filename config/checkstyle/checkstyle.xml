<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">
<!-- Lean Checkstyle tuned for DDD/Hexagonal projects -->
<module name="Checker">
    <property name="severity" value="warning"/>

    <!-- Global suppressions (file exists alongside this config) -->
    <module name="SuppressionFilter">
        <property name="file" value="${config_loc}/checkstyle-suppressions.xml"/>
    </module>
    <!-- Allow @SuppressWarnings at code level -->
    <module name="SuppressWarningsFilter"/>

    <!-- Whitespace & basic hygiene -->
    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>
    <module name="NewlineAtEndOfFile"/>
    <module name="LineLength">
        <property name="max" value="140"/>
        <property name="ignorePattern" value="^package |^import |a href|http://|https://"/>
    </module>

    <module name="TreeWalker">
        <module name="AvoidStarImport"/>
        <module name="UnusedImports"/>
        <module name="RedundantImport"/>
        <module name="NeedBraces"/>
        <module name="LeftCurly"/>
        <module name="RightCurly"/>
        <module name="WhitespaceAfter"/>
        <module name="WhitespaceAround"/>

        <!-- Lombok ban to keep domain pure -->
        <module name="IllegalImport">
            <property name="illegalPkgs" value="lombok"/>
        </module>

        <!-- Optionally, discourage public setters (prefer ArchUnit for precision) -->
        <!--
        <module name="Regexp">
          <property name="format" value="public\s+void\s+set[A-Z]"/>
          <property name="message" value="도메인 public setter 지양 — 의미 있는 메서드로 전환 고려"/>
          <property name="ignoreComments" value="true"/>
          <property name="illegalPattern" value="true"/>
          <property name="severity" value="info"/>
        </module>
        -->
    </module>
</module>