name: Terraform Apply

on:
  workflow_dispatch:  # 수동 트리거 허용
  workflow_call:      # 다른 워크플로우에서 호출 가능
    inputs:
      web-api-image-tag:
        description: 'Web API Docker image tag (e.g., web-api-92-f08d571)'
        required: false
        type: string
      download-worker-image-tag:
        description: 'Download Worker Docker image tag'
        required: false
        type: string
      scheduler-image-tag:
        description: 'Scheduler Docker image tag'
        required: false
        type: string
    secrets:
      AWS_ROLE_ARN:
        required: true

permissions:
  contents: read
  id-token: write

jobs:
  terraform-apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      # 순차 실행으로 변경 (의존성 고려)
      max-parallel: 1
      matrix:
        module:
          # 의존성 순서대로 배치
          - name: ecr
            dir: terraform/ecr
            order: 0
          # - name: elasticache  # TODO: State migration needed - existing resources don't match module paths
          #   dir: terraform/elasticache
          #   order: 1
          - name: s3
            dir: terraform/s3
            order: 2
          - name: sqs
            dir: terraform/sqs
            order: 3
          - name: ecs-web-api
            dir: terraform/ecs-web-api
            order: 4
          - name: ecs-scheduler
            dir: terraform/ecs-scheduler
            order: 5
          - name: ecs-download-worker
            dir: terraform/ecs-download-worker
            order: 6

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.0

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ap-northeast-2
          role-duration-seconds: 3600
          role-session-name: GitHubActions-FileFlow-TerraformApply

      - name: Terraform Init - ${{ matrix.module.name }}
        working-directory: ${{ matrix.module.dir }}
        run: terraform init -reconfigure

      - name: Check and Import Existing Resources - ${{ matrix.module.name }}
        working-directory: ${{ matrix.module.dir }}
        run: |
          echo "🔍 기존 리소스 확인 및 Import 중..."

          # Plan 실행하여 충돌 확인
          set +e
          terraform plan -out=tfplan 2>&1 | tee plan_output.txt
          PLAN_EXIT_CODE=$?
          set -e

          # 409 에러 또는 "already exists" 패턴 확인
          if grep -q "EntityAlreadyExists\|already exists" plan_output.txt; then
            echo "⚠️  기존 리소스 발견! Import 작업 시작..."

            # ECS Service 모듈
            if [ "${{ matrix.module.name }}" == "ecs-service" ]; then
              echo "📦 ECS Service 리소스 Import 중..."

              # IAM Execution Role
              if aws iam get-role --role-name fileflow-prod-ecs-execution-role --region ap-northeast-2 >/dev/null 2>&1; then
                echo "  → Importing fileflow-prod-ecs-execution-role..."
                terraform import -input=false aws_iam_role.fileflow_execution_role fileflow-prod-ecs-execution-role || echo "  ⚠️  Import failed or already in state"
              fi

              # IAM Task Role
              if aws iam get-role --role-name fileflow-prod-ecs-task-role --region ap-northeast-2 >/dev/null 2>&1; then
                echo "  → Importing fileflow-prod-ecs-task-role..."
                terraform import -input=false aws_iam_role.fileflow_task_role fileflow-prod-ecs-task-role || echo "  ⚠️  Import failed or already in state"
              fi

              # ALB
              ALB_ARN=$(aws elbv2 describe-load-balancers --names fileflow-prod-alb --region ap-northeast-2 --query 'LoadBalancers[0].LoadBalancerArn' --output text 2>/dev/null || echo "")
              if [ -n "$ALB_ARN" ] && [ "$ALB_ARN" != "None" ]; then
                echo "  → Importing ALB: $ALB_ARN"
                terraform import -input=false aws_lb.fileflow_alb "$ALB_ARN" || echo "  ⚠️  Import failed or already in state"
              fi

              # Target Group
              TG_ARN=$(aws elbv2 describe-target-groups --names fileflow-prod-alb-fileflow --region ap-northeast-2 --query 'TargetGroups[0].TargetGroupArn' --output text 2>/dev/null || echo "")
              if [ -n "$TG_ARN" ] && [ "$TG_ARN" != "None" ]; then
                echo "  → Importing Target Group: $TG_ARN"
                terraform import -input=false aws_lb_target_group.fileflow "$TG_ARN" || echo "  ⚠️  Import failed or already in state"
              fi
            fi

            # ElastiCache Redis 모듈
            if [ "${{ matrix.module.name }}" == "elasticache-redis" ]; then
              echo "📦 ElastiCache Redis 리소스 Import 중..."
              if aws elasticache describe-replication-groups --replication-group-id fileflow-prod-redis --region ap-northeast-2 >/dev/null 2>&1; then
                echo "  → Importing fileflow-prod-redis..."
                terraform import -input=false aws_elasticache_replication_group.redis fileflow-prod-redis || echo "  ⚠️  Import failed or already in state"
              fi
            fi

            # S3 Bucket 모듈
            if [ "${{ matrix.module.name }}" == "s3-bucket" ]; then
              echo "📦 S3 Bucket 리소스 Import 중..."
              BUCKET_NAME="fileflow-prod"
              if aws s3api head-bucket --bucket "$BUCKET_NAME" --region ap-northeast-2 >/dev/null 2>&1; then
                echo "  → Importing $BUCKET_NAME..."
                terraform import -input=false aws_s3_bucket.main "$BUCKET_NAME" || echo "  ⚠️  Import failed or already in state"
              fi
            fi

            # ECR 모듈
            if [ "${{ matrix.module.name }}" == "ecr" ]; then
              echo "📦 ECR Repository 리소스 Import 중..."
              if aws ecr describe-repositories --repository-names fileflow-prod --region ap-northeast-2 >/dev/null 2>&1; then
                echo "  → Importing fileflow-prod ECR repository..."
                terraform import -input=false aws_ecr_repository.fileflow fileflow-prod || echo "  ⚠️  Import failed or already in state"
              fi
            fi

            echo "✅ Import 완료! Plan 재실행..."
            terraform plan -out=tfplan
          else
            echo "✅ 기존 리소스 없음 또는 State와 일치. 정상 진행."
          fi

          rm -f plan_output.txt

      - name: Terraform Apply - ${{ matrix.module.name }}
        working-directory: ${{ matrix.module.dir }}
        run: |
          echo "🚀 Applying Terraform for ${{ matrix.module.name }}..."

          # Image Tag 변수 설정 (Build & Deploy 워크플로우에서 전달됨)
          EXTRA_VARS=""

          if [ "${{ matrix.module.name }}" == "ecs-web-api" ] && [ -n "${{ inputs.web-api-image-tag }}" ]; then
            echo "📦 Using Web API image tag: ${{ inputs.web-api-image-tag }}"
            EXTRA_VARS="-var=image_tag=${{ inputs.web-api-image-tag }}"
          elif [ "${{ matrix.module.name }}" == "ecs-download-worker" ] && [ -n "${{ inputs.download-worker-image-tag }}" ]; then
            echo "📦 Using Download Worker image tag: ${{ inputs.download-worker-image-tag }}"
            EXTRA_VARS="-var=image_tag=${{ inputs.download-worker-image-tag }}"
          elif [ "${{ matrix.module.name }}" == "ecs-scheduler" ] && [ -n "${{ inputs.scheduler-image-tag }}" ]; then
            echo "📦 Using Scheduler image tag: ${{ inputs.scheduler-image-tag }}"
            EXTRA_VARS="-var=image_tag=${{ inputs.scheduler-image-tag }}"
          else
            echo "ℹ️  No image tag provided for ${{ matrix.module.name }}, using default from provider.tf"
          fi

          # Terraform Plan with optional image_tag variable
          echo "📋 Generating fresh terraform plan..."
          terraform plan $EXTRA_VARS -out=tfplan

          terraform apply -auto-approve -no-color tfplan
          echo "✅ Apply completed for ${{ matrix.module.name }}"

      - name: Terraform Output - ${{ matrix.module.name }}
        working-directory: ${{ matrix.module.dir }}
        run: |
          echo "📋 Outputs for ${{ matrix.module.name }}:"
          terraform output -json > outputs-${{ matrix.module.name }}.json
          terraform output

      - name: Upload Apply Results - ${{ matrix.module.name }}
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: apply-results-${{ matrix.module.name }}
          path: ${{ matrix.module.dir }}/outputs-${{ matrix.module.name }}.json
          retention-days: 30

  notify-completion:
    name: Notify Deployment Completion
    needs: terraform-apply
    runs-on: ubuntu-latest
    if: always()

    steps:
      - name: Create Deployment Summary
        run: |
          echo "## 🎉 FileFlow Infrastructure Deployment" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Status**: ${{ needs.terraform-apply.result }}" >> $GITHUB_STEP_SUMMARY
          echo "**Environment**: Production" >> $GITHUB_STEP_SUMMARY
          echo "**Deployed by**: @${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Modules Deployed" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ ECR Repository" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ ElastiCache" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ S3" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ SQS Queue" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ ECS Web API" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ ECS Scheduler" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ ECS Download Worker" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Next Steps**:" >> $GITHUB_STEP_SUMMARY
          echo "1. Verify infrastructure in AWS Console" >> $GITHUB_STEP_SUMMARY
          echo "2. Check CloudWatch logs" >> $GITHUB_STEP_SUMMARY
          echo "3. Test application endpoints" >> $GITHUB_STEP_SUMMARY
