# ===============================================
# Application Configuration - Production Environment
# ===============================================
# 운영 환경 전용 설정
# 공통 설정은 application.yml 참조
#
# @since 1.0.0
# ===============================================

server:
  tomcat:
    threads:
      max: 200

  # Graceful Shutdown Timeout
  lifecycle:
    timeout-per-shutdown-phase: 30s

# ===============================================
# Config Import Override (환경별 설정 로드)
# ===============================================
spring:
  config:
    import:
      - classpath:rest-api.yml
      - classpath:persistence.yml
      - classpath:persistence-prod.yml
      - classpath:persistence-legacy.yml
      - classpath:persistence-legacy-prod.yml
      - classpath:redis.yml
      - classpath:redis-prod.yml
      - classpath:portone.yml
      - classpath:fileflow.yml
      - classpath:fileflow-prod.yml

# ===============================================
# Sentry Configuration (Production Only)
# ===============================================
sentry:
  dsn: ${SENTRY_DSN:}
  environment: production
  send-default-pii: true
  traces-sample-rate: 1.0
  exception-resolver-order: -2147483647
  attach-stacktrace: true
  logging:
    minimum-event-level: error
    minimum-breadcrumb-level: info

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus
