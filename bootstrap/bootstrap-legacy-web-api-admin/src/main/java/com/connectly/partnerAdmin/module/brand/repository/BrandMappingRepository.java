package com.connectly.partnerAdmin.module.brand.repository;

import com.connectly.partnerAdmin.module.brand.entity.BrandMapping;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BrandMappingRepository extends JpaRepository<BrandMapping, Long> {
}
