package com.connectly.partnerAdmin.module.product.repository.image;

import com.connectly.partnerAdmin.module.product.entity.image.ProductGroupImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductGroupImageRepository extends JpaRepository<ProductGroupImage, Long> {
}
