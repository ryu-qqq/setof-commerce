=== V1 Legacy Admin API Overview

[WARNING]
====
V1 Admin API는 하위 호환성을 위해 유지되며, 신규 개발 시 V2 API 사용을 권장합니다.
====

==== V1 vs V2 차이점

[cols="2,3,3"]
|===
| 항목 | V1 (Legacy) | V2 (권장)

| 인증 방식
| 🔒 **orgAdminOrHigher** (조직 관리자 이상)
| 🔒 **Role-based** (세분화된 권한)

| 응답 래퍼
| `V1ApiResponse` (data, response)
| `ApiResponse` (success, data, error)

| 에러 응답
| Legacy 형식
| 표준화된 에러 코드

| 페이징
| `V1PageResponse` (Spring Page 호환)
| `PageResponse` (표준 페이징)

| 경로 패턴
| `/api/v1/{resource}`, `/api/v1/{resources}`
| `/api/v2/admin/{resources}`

| 승인 상태 변경
| 단일 PUT 엔드포인트 (일괄 처리)
| 개별 PATCH 엔드포인트 (approve/reject/suspend)
|===

==== V1 응답 형식

V1 Admin API는 다음과 같은 응답 구조를 사용합니다:

[source,json]
----
{
  "data": { ... },
  "response": {
    "status": 200,
    "message": "success"
  }
}
----

===== 응답 필드

[cols="1,1,3"]
|===
| 필드 | 타입 | 설명

| `data`
| Object/Array
| 응답 데이터

| `response.status`
| Number
| HTTP 상태 코드 (200: 성공, 404: 찾을 수 없음, 500: 서버 오류)

| `response.message`
| String
| 응답 메시지 (success, 에러 메시지 등)
|===

==== V1 페이징 응답 형식

목록 조회 API는 Spring Page 호환 형식을 사용합니다:

[source,json]
----
{
  "data": {
    "content": [...],
    "pageable": {
      "pageNumber": 0,
      "pageSize": 20
    },
    "totalElements": 100,
    "totalPages": 5,
    "first": true,
    "last": false,
    "numberOfElements": 20
  },
  "response": {
    "status": 200,
    "message": "success"
  }
}
----

==== V1 에러 응답

[source,json]
----
{
  "data": null,
  "response": {
    "status": 404,
    "message": "리소스를 찾을 수 없습니다"
  }
}
----

==== 마이그레이션 가이드

V1 Admin API에서 V2 Admin API로 마이그레이션 시 다음 사항을 고려하세요:

. **경로 변경**: `/api/v1/seller` → `/api/v2/admin/sellers`
. **응답 구조 변경**: `response.status` 대신 `success` 필드를 사용합니다.
. **승인 상태 변경**: 일괄 PUT 대신 개별 PATCH 엔드포인트를 사용합니다.
** `PUT /api/v1/seller/approval-status` → `PATCH /api/v2/admin/sellers/{id}/approve`
. **페이징**: 동일한 표준 페이징 파라미터 (`page`, `size`)를 사용합니다.

==== 엔드포인트 매핑

[cols="2,2,1"]
|===
| V1 엔드포인트 | V2 엔드포인트 | HTTP Method

| `/api/v1/seller`
| `/api/v2/admin/sellers` (POST)
| POST

| `/api/v1/seller/{id}`
| `/api/v2/admin/sellers/{id}`
| GET, PUT

| `/api/v1/sellers`
| `/api/v2/admin/sellers`
| GET

| `/api/v1/seller/approval-status`
| `/api/v2/admin/sellers/{id}/approve` 등
| PUT → PATCH

| `/api/v1/categories`
| `/api/v2/admin/categories`
| GET

| `/api/v1/brands`
| `/api/v2/admin/brands`
| GET
|===
