=== V1 Legacy API Overview

[WARNING]
====
⚠️ **Legacy API**: V1 API는 하위 호환성을 위해 유지되며, 신규 개발 시 V2 API 사용을 권장합니다.
====

==== V1 vs V2 차이점

[cols="2,3,3"]
|===
| 항목 | V1 (Legacy) | V2 (권장)

| 인증 방식
| 🔓 **Public** (대부분 인증 불필요)
| 🔒 **Authenticated** (JWT 토큰 필요)

| 응답 래퍼
| `V1ApiResponse` (data, response)
| `ApiResponse` (result, data, message)

| 에러 응답
| Legacy 형식
| RFC 7807 Problem Details

| 페이징
| 미지원 또는 제한적
| 표준 페이징 지원

| 버저닝
| URL 경로 (`/api/v1/...`)
| URL 경로 (`/api/v2/...`)
|===

==== V1 응답 형식

V1 API는 다음과 같은 응답 구조를 사용합니다:

[source,json]
----
{
  "data": { ... },
  "response": {
    "status": 200,
    "message": "success"
  }
}
----

===== 응답 필드

[cols="1,1,3"]
|===
| 필드 | 타입 | 설명

| `data`
| Object/Array
| 응답 데이터

| `response.status`
| Number
| HTTP 상태 코드 (200: 성공, 404: 찾을 수 없음, 500: 서버 오류)

| `response.message`
| String
| 응답 메시지 (success, 에러 메시지 등)
|===

==== V1 에러 응답

[source,json]
----
{
  "data": null,
  "response": {
    "status": 404,
    "message": "리소스를 찾을 수 없습니다"
  }
}
----

==== 마이그레이션 가이드

V1 API에서 V2 API로 마이그레이션 시 다음 사항을 고려하세요:

. **인증 추가**: V2 API는 대부분 인증이 필요합니다. JWT 토큰을 헤더에 포함하세요.
. **응답 구조 변경**: `response.status` 대신 `result` 필드를 사용합니다.
. **에러 처리**: RFC 7807 Problem Details 형식으로 변경되었습니다.
. **페이징**: 표준 페이징 파라미터 (`page`, `size`)를 사용합니다.

