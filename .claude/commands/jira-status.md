---
description: Jira 이슈 상태 관리 (To Do/In Progress/Done)
tags: [project]
---

# /jira-status - Jira 상태 관리

**목적**: Jira Issue 상태를 간편하게 변경

## 사용법

```bash
/jira-status {ISSUE-KEY} {상태}
/jira-status MEMBER-123 done
/jira-status MEMBER-123 progress
/jira-status MEMBER-123 todo
```

---

## 상태 종류

| 명령어 | Jira 상태 | 설명 |
|--------|----------|------|
| `todo` | To Do | 작업 대기 |
| `progress` | In Progress | 작업 중 |
| `done` | Done | 작업 완료 |

---

## 실행 프로세스

```
/jira-status MEMBER-123 done
        ↓
┌─────────────────────────────────────────────────┐
│ 1️⃣ Issue 확인                                   │
│    - Issue 존재 여부 확인                        │
│    - 현재 상태 조회                              │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 2️⃣ Transition 조회                              │
│    - 가능한 상태 전이 확인                       │
│    - Done transition ID 획득                    │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 3️⃣ 상태 변경                                    │
│    - Transition API 호출                        │
│    - 상태 변경 실행                              │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 4️⃣ 완료 Comment (done일 경우)                   │
│    - 구현 완료 요약 추가                         │
│    - PR 링크 포함 (있을 경우)                    │
└─────────────────────────────────────────────────┘
```

---

## 단축 명령어

### 현재 브랜치 기반 자동 감지

```bash
# 현재 브랜치: feature/MEMBER-123-member-withdraw
/jira-status done

# 자동으로 MEMBER-123 감지하여 Done 처리
```

### 빠른 상태 변경

```bash
/jira-status done      # 현재 작업 → Done
/jira-status progress  # 현재 작업 → In Progress
```

---

## Jira API 호출

### 현재 상태 조회

```bash
GET /rest/api/3/issue/{issueKey}?fields=status
Authorization: Basic {base64(email:api_token)}
```

### 가능한 Transition 조회

```bash
GET /rest/api/3/issue/{issueKey}/transitions
Authorization: Basic {base64(email:api_token)}
```

### 상태 변경 실행

```bash
POST /rest/api/3/issue/{issueKey}/transitions
Authorization: Basic {base64(email:api_token)}
Content-Type: application/json

{
  "transition": {
    "id": "{transitionId}"
  }
}
```

---

## 출력 예시

### 상태 변경 성공

```
🔍 Issue 확인 중...
   ✅ MEMBER-123: 회원 탈퇴 기능 구현
   └─ 현재 상태: In Progress

📊 상태 변경 중...
   ✅ In Progress → Done

─────────────────────────────────────

## ✅ 상태 변경 완료

| 항목 | 값 |
|------|-----|
| Issue | MEMBER-123 |
| 제목 | 회원 탈퇴 기능 구현 |
| 이전 상태 | In Progress |
| 현재 상태 | Done ✅ |
```

### Done 상태 + Comment

```
📊 상태 변경 중...
   ✅ In Progress → Done

💬 완료 Comment 추가 중...
   ✅ 구현 완료 요약 등록

─────────────────────────────────────

## ✅ 작업 완료

MEMBER-123: 회원 탈퇴 기능 구현

### 구현 내용
- Domain: Member.withdraw() 추가
- Application: WithdrawMemberUseCase 생성
- Persistence: MemberEntity soft delete
- REST API: DELETE /members/{id}

### 테스트 결과
- 단위 테스트: 12개 통과
- 통합 테스트: 3개 통과

🤖 Generated by Claude Code
```

---

## 상태 전이 규칙

### 일반적인 Jira 워크플로우

```
To Do → In Progress → Done
  ↑         ↓
  └─────────┘ (재작업)
```

### 프로젝트별 커스텀 상태

Jira 프로젝트 설정에 따라 상태가 다를 수 있습니다:

| 표준 상태 | 가능한 커스텀 상태 |
|----------|-------------------|
| To Do | Backlog, Open, New |
| In Progress | In Development, Working |
| Done | Closed, Resolved, Complete |

---

## 에러 처리

### Issue 없음
```
❌ Issue를 찾을 수 없습니다: MEMBER-999
   - Issue Key를 확인하세요
```

### 상태 전이 불가
```
❌ 상태 변경 불가: To Do → Done
   - 현재 워크플로우에서 직접 전이가 불가능합니다
   - To Do → In Progress → Done 순서로 변경하세요
```

### 브랜치 감지 실패
```
⚠️ 현재 브랜치에서 Issue Key를 감지할 수 없습니다
   - 브랜치명: main
   - Issue Key를 직접 지정하세요: /jira-status MEMBER-123 done
```

---

## 완료 시 자동 Comment

### Done 상태 변경 시 추가되는 Comment

```markdown
## ✅ 구현 완료

### 변경 내용
- [Domain] Member.withdraw() 메서드 추가
- [Application] WithdrawMemberUseCase 구현
- [Persistence] soft delete 로직 구현
- [REST API] DELETE /members/{id} 엔드포인트 추가

### 테스트
- Unit Tests: ✅ All Passed
- Integration Tests: ✅ All Passed
- ArchUnit Tests: ✅ All Passed

### 관련 커밋
- feat: 회원 탈퇴 Domain 로직 구현
- feat: 회원 탈퇴 Application Layer 구현
- feat: 회원 탈퇴 Persistence Layer 구현
- feat: 회원 탈퇴 REST API 구현

---
🤖 Generated by Claude Code
```

---

## 연관 커맨드

- `/jira-task {KEY}` - Jira Issue → Plan 실행
- `/jira-register` - Plan → Jira 등록 + 브랜치 생성
- `/plan "{기능}"` - 직접 Plan 실행 (Jira 없이)
