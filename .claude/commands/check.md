---
description: 정적 분석 + ArchUnit 테스트 실행. Checkstyle, PMD, SpotBugs, ArchUnit.
tags: [workflow, quality]
---

# /check - Static Analysis & Architecture Tests

정적 코드 분석과 아키텍처 테스트를 실행합니다.

## 사용법

```bash
/check                      # 전체 검사
/check --quick              # ArchUnit만 빠르게
/check --fix                # 자동 수정 가능한 항목 수정
```

## 실행 프로세스

```
/check
        ↓
┌─────────────────────────────────────────────────┐
│ 1️⃣ Spotless 자동 포맷팅                         │
│    ./gradlew spotlessApply                      │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 2️⃣ ArchUnit 테스트                              │
│    ./gradlew test --tests "*ArchTest"          │
│    - Zero-Tolerance 규칙                        │
│    - 레이어 의존성                              │
│    - 네이밍 컨벤션                              │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 3️⃣ Gradle Quality Checks                       │
│    ./gradlew check                             │
│    - Checkstyle: 코드 스타일                   │
│    - PMD: Law of Demeter, SRP                  │
│    - SpotBugs: 잠재적 버그                     │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 4️⃣ 결과 분석 및 리포트                         │
│    - 위반 사항 요약                            │
│    - 자동 수정 가능 항목 표시                   │
│    - 리포트 파일 위치 안내                      │
└─────────────────────────────────────────────────┘
```

## 검사 항목

### ArchUnit 테스트
| 테스트 | 검증 내용 |
|--------|----------|
| ZeroToleranceArchTest | Lombok 금지, Transaction 경계, Spring 프록시 |
| DomainLayerArchTest | 외부 의존성 금지, VO 패턴 |
| ApplicationLayerArchTest | CQRS 분리, UseCase 패턴 |
| HexagonalArchTest | 레이어 의존성 방향 |

### Checkstyle
- Lombok import 금지
- 라인 길이 제한 (140자)
- 중괄호 스타일
- Import 정리

### PMD
- Law of Demeter (Getter 체이닝 금지)
- DataClass (DTO 예외)
- SRP (메서드/필드 수 제한)
- God Class 방지

### SpotBugs
- Null 포인터 위험
- 리소스 누수
- 동시성 이슈
- 보안 취약점

## 결과 리포트 위치

```bash
# ArchUnit 테스트 결과
build/reports/tests/test/index.html

# Checkstyle 결과
build/reports/checkstyle/main.html

# PMD 결과
build/reports/pmd/main.html

# SpotBugs 결과
build/reports/spotbugs/main.html

# JaCoCo 커버리지
build/reports/jacoco/test/html/index.html
```

## 출력 예시

```
🔍 정적 분석 시작...

────────────────────────────────────────
1️⃣ Spotless 포맷팅
────────────────────────────────────────
✅ 포맷팅 적용됨 (3개 파일 수정)

────────────────────────────────────────
2️⃣ ArchUnit 테스트
────────────────────────────────────────
./gradlew test --tests "*ArchTest"

✅ ZeroToleranceArchTest: PASSED
✅ DomainLayerArchTest: PASSED
✅ ApplicationLayerArchTest: PASSED
✅ HexagonalArchTest: PASSED

총 45개 테스트 통과

────────────────────────────────────────
3️⃣ Gradle Quality Checks
────────────────────────────────────────
./gradlew check

📊 Checkstyle:
   ✅ 0 violations

📊 PMD:
   ⚠️ 2 violations
   - Order.java:67 - LawOfDemeter
   - OrderService.java:89 - TooManyMethods

📊 SpotBugs:
   ✅ 0 bugs found

────────────────────────────────────────
📋 결과 요약
────────────────────────────────────────

🔴 실패: 0건
🟡 경고: 2건
🟢 통과: 나머지 모든 항목

⚠️ PMD 경고 상세:
   1. Order.java:67
      규칙: LawOfDemeter
      내용: customer.getAddress().getCity() 체이닝
      수정: order.getShippingCity() 도메인 메서드로 캡슐화

   2. OrderService.java:89
      규칙: TooManyMethods
      내용: public 메서드 12개 (권장: 10개 이하)
      수정: 일부 메서드를 별도 서비스로 분리

📁 상세 리포트:
   build/reports/pmd/main.html
```

## 옵션

| 옵션 | 설명 |
|------|------|
| `--quick` | ArchUnit만 실행 (빠름) |
| `--fix` | 자동 수정 가능 항목 수정 |
| `--report` | 리포트 파일 브라우저로 열기 |
| `--verbose` | 상세 출력 |

## 다음 단계

모든 검사 통과 후:
```bash
/ship     # 최종 커밋 및 푸시
```

위반 사항 있을 경우:
```bash
# 수동 수정 후 재검사
/check
```
