---
description: Claude 직접 코드 리뷰. Knowledge Base 기반 컨벤션 검토 + Zero-Tolerance 체크.
tags: [workflow, review, serena]
---

# /review - Code Review by Claude

Claude가 **Knowledge Base 규칙 기반**으로 직접 코드 리뷰를 수행합니다.

## 핵심 개념

```
/review 실행 시:

┌─────────────────────────────────────────────────┐
│  📝 변경된 코드                                  │
│       │                                         │
│       ├── Zero-Tolerance 규칙 체크 (212개)      │
│       ├── Layer별 규칙 체크 (401개)             │
│       ├── 코드 품질 분석                        │
│       └── 개선 제안                             │
│                                                 │
│  → 필수 수정 / 권장 수정 / 통과 분류            │
└─────────────────────────────────────────────────┘
```

---

## 사용법

```bash
/review                     # 현재 변경사항 전체 리뷰
/review --staged            # staged 파일만 리뷰
/review --file Order.java   # 특정 파일만 리뷰
/review --fix               # 자동 수정 가능 항목 즉시 수정
/review --no-serena         # Serena 없이 실행
```

---

## 실행 프로세스

```
/review
        ↓
┌─────────────────────────────────────────────────┐
│ 1️⃣ 변경사항 수집                                │
│    - git diff로 변경된 파일 목록                 │
│    - 변경 내용 추출                             │
│    - 레이어별 파일 분류                         │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 2️⃣ Zero-Tolerance 규칙 체크 🚨                 │
│    - Lombok 사용 여부                           │
│    - Getter 체이닝 (Law of Demeter)             │
│    - JPA 관계 어노테이션                        │
│    - Controller @Transactional                  │
│    → 위반 시 필수 수정 항목으로 분류            │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 3️⃣ Layer별 규칙 체크                           │
│    - Domain: Tell Don't Ask, 불변성, VO 패턴   │
│    - Application: CQRS 분리, Transaction 경계   │
│    - Persistence: Long FK, QueryDSL Projection │
│    - REST API: RESTful, @Valid, DTO 분리       │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 4️⃣ 코드 품질 분석                              │
│    - 코드 복잡도                                │
│    - 네이밍 컨벤션                              │
│    - 테스트 커버리지                            │
│    - 잠재적 버그                                │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 5️⃣ 리뷰 결과 정리                              │
│    - 🔴 필수 수정 (Zero-Tolerance 위반)         │
│    - 🟡 권장 수정 (품질 개선)                   │
│    - 🟢 통과                                    │
│    - 자동 수정 가능 항목 표시                   │
└─────────────────────────────────────────────────┘
```

---

## 🔗 Serena 통합 (기본 활성화)

```
/review 실행 시 Serena 자동 연동:

┌─────────────────────────────────────────────────┐
│ 1️⃣ Knowledge Base 규칙 로드                    │
│    → read_memory("zero-tolerance")              │
│    → read_memory("{layer}-rules")               │
│    → 401개 규칙 컨텍스트 로드                   │
│                                                 │
│ 2️⃣ 변경 코드 심볼 분석                         │
│    → find_symbol() - 변경된 클래스/메서드 검색  │
│    → get_symbols_overview() - 구조 파악         │
│    → 영향 범위 분석                             │
│                                                 │
│ 3️⃣ 참조 코드 분석 (컨텍스트)                   │
│    → find_referencing_symbols() - 호출 관계    │
│    → 변경 영향 받는 코드 파악                   │
│                                                 │
│ 4️⃣ 리뷰 결과 저장                              │
│    → write_memory("review-{epic}", findings)    │
│    → 위반 항목, 수정 제안 저장                  │
│                                                 │
│ 5️⃣ 작업 세션 업데이트                          │
│    → edit_memory("work-session-{epic}", review) │
│    → 리뷰 상태, 필수 수정 항목 기록             │
└─────────────────────────────────────────────────┘

비활성화: --no-serena 옵션 사용
```

### Serena Memory 리뷰 결과 형식

**review-{epic-key}.md** 예시:
```markdown
# Review: AESA-123

## 리뷰 요약
- **리뷰 일시**: 2024-01-15 18:30
- **변경 파일**: 4개
- **총 변경량**: +321, -17 lines

## 필수 수정 (Zero-Tolerance)
| 파일 | 라인 | 규칙 | 설명 |
|------|------|------|------|
| Order.java | 67 | AGG-014 | Getter 체이닝 |

## 권장 수정
| 파일 | 라인 | 카테고리 | 설명 |
|------|------|----------|------|
| Order.java | 45 | 불변성 | final 누락 |
| OrderService.java | 23 | 코드 품질 | 메서드 분리 필요 |

## 리뷰 상태
- [ ] 필수 수정 완료
- [ ] 권장 수정 검토
- [ ] 최종 확인
```

---

## 리뷰 체크리스트

### 🚨 Zero-Tolerance (필수)

| 규칙 ID | 규칙 | 설명 |
|---------|------|------|
| AGG-001 | Lombok 금지 | @Data, @Getter, @Setter 등 사용 금지 |
| AGG-014 | Law of Demeter | `getA().getB().getC()` 체이닝 금지 |
| ENT-002 | JPA 관계 금지 | @OneToMany, @ManyToOne 등 사용 금지 |
| CTR-005 | Controller @Transactional | Controller에서 @Transactional 금지 |

### 📦 Domain Layer

| 체크 항목 | 설명 |
|----------|------|
| Tell, Don't Ask | 객체에게 물어보지 말고 시키기 |
| 불변성 | final 필드, 방어적 복사 |
| VO 패턴 | 값 객체는 equals/hashCode 구현 |
| 도메인 이벤트 | 상태 변경 시 이벤트 발행 |

### ⚙️ Application Layer

| 체크 항목 | 설명 |
|----------|------|
| CQRS 분리 | Command/Query 분리 |
| Transaction 경계 | UseCase에서만 @Transactional |
| DTO Record | Command/Query는 Record 사용 |
| 외부 API 분리 | @Transactional 내 외부 호출 금지 |

### 💾 Persistence Layer

| 체크 항목 | 설명 |
|----------|------|
| Long FK 전략 | 관계 대신 Long ID 사용 |
| QueryDSL Projection | DTO 직접 조회 |
| N+1 방지 | fetch join 또는 batch size |

### 🌐 REST API Layer

| 체크 항목 | 설명 |
|----------|------|
| @Valid 필수 | Request DTO에 @Valid |
| RESTful 설계 | HTTP 메서드, 상태 코드 |
| DTO 분리 | Request/Response DTO 분리 |

---

## 출력 예시

```
🔍 코드 리뷰 시작...

────────────────────────────────────────
📝 변경된 파일: 4개
────────────────────────────────────────
   📦 Domain:
      - Order.java (+120, -5)
      - OrderStatus.java (+45)
   ⚙️ Application:
      - OrderService.java (+67, -12)
   🧪 Test:
      - OrderTest.java (+89)

────────────────────────────────────────
🚨 Zero-Tolerance 체크
────────────────────────────────────────
   ✅ AGG-001 Lombok 금지: 통과
   ❌ AGG-014 Law of Demeter: 위반 1건
      └─ Order.java:67 - getCustomer().getName()
   ✅ ENT-002 JPA 관계 금지: 통과
   ✅ CTR-005 Controller @Transactional: 통과

────────────────────────────────────────
📋 Layer별 규칙 체크
────────────────────────────────────────
   📦 Domain Layer:
      ✅ Tell, Don't Ask: 통과
      ⚠️ 불변성: Order.java:45 - final 누락
      ✅ VO 패턴: 통과

   ⚙️ Application Layer:
      ✅ CQRS 분리: 통과
      ✅ Transaction 경계: 통과

────────────────────────────────────────
💡 코드 품질 분석
────────────────────────────────────────
   ⚠️ OrderService.java:23 - 메서드가 너무 김 (45줄)
      → 메서드 분리 권장
   ⚠️ OrderTest.java:45 - 엣지 케이스 테스트 부족
      → 경계값 테스트 추가 권장

════════════════════════════════════════
📊 리뷰 결과 요약
════════════════════════════════════════

🔴 필수 수정: 1건
   1. Order.java:67 - Getter 체이닝 제거 (AGG-014)
      현재: order.getCustomer().getName()
      수정: order.getCustomerName() 또는 Tell, Don't Ask 적용

🟡 권장 수정: 3건
   1. Order.java:45 - final 키워드 추가
   2. OrderService.java:23 - 메서드 분리
   3. OrderTest.java:45 - 엣지 케이스 테스트 추가

🟢 통과: 나머지 모든 항목

────────────────────────────────────────
📌 다음 단계
────────────────────────────────────────
   /review --fix    # 필수 수정 항목 자동 수정
   /check           # 정적 분석 실행
   /ship            # 최종 커밋 및 푸시
```

---

## 자동 수정 옵션

```bash
/review --fix              # 자동 수정 가능한 필수 항목만 수정
/review --fix-all          # 권장 항목 포함 모든 수정 적용
```

### 자동 수정 가능 항목

| 항목 | 자동 수정 | 설명 |
|------|----------|------|
| Getter 체이닝 | ⚠️ 부분 | 단순 케이스만 자동 |
| final 누락 | ✅ 가능 | 필드에 final 추가 |
| @Valid 누락 | ✅ 가능 | Request 파라미터에 추가 |
| 메서드 분리 | ❌ 수동 | 로직 이해 필요 |

---

## 옵션

| 옵션 | 설명 | 예시 |
|------|------|------|
| `--staged` | staged 파일만 리뷰 | `/review --staged` |
| `--file FILE` | 특정 파일만 리뷰 | `/review --file Order.java` |
| `--fix` | 자동 수정 실행 | `/review --fix` |
| `--fix-all` | 모든 수정 적용 | `/review --fix-all` |
| `--verbose` | 상세 출력 | `/review --verbose` |
| `--layer LAYER` | 특정 레이어만 | `/review --layer domain` |
| `--no-serena` | Serena 없이 실행 (Memory 연동 비활성화) | `/review --no-serena` |

---

## Skill 연동

**reviewer** skill이 활성화됩니다:

```
reviewer skill 수행 작업:
├── Knowledge Base 규칙 로드
├── 변경 코드 분석
├── 규칙 위반 탐지
├── 수정 제안 생성
└── 자동 수정 실행 (--fix 옵션)
```

---

## 다음 단계

```bash
/check    # 정적 분석 실행 (Checkstyle, PMD, SpotBugs)
/ship     # 최종 커밋 및 푸시
```
