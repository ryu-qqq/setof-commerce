---
description: 작업 완료 처리 + 아카이브. plan/design 메모리 정리, usage-history 업데이트, 외부 아카이브 저장.
tags: [project]
---

# /complete - 작업 완료 및 아카이브

작업을 완료 처리하고 메모리를 정리합니다.

## 사용 형식

```bash
/complete {feature}

# 예시
/complete order-cancel
```

---

## 실행 프로세스

```
/complete order-cancel
        ↓
┌─────────────────────────────────────────────────────────────────────┐
│ 1️⃣ 메모리 존재 확인                                                 │
│    - plan-order-cancel 존재?                                        │
│    - design-order-cancel 존재?                                      │
└─────────────────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────────────┐
│ 2️⃣ 구현 완료 여부 확인                                              │
│    - design 체크리스트 대비 파일 존재 확인                           │
│    - 미완료 항목 있으면 경고 (강제 완료 가능)                        │
└─────────────────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────────────┐
│ 3️⃣ component-usage-history 업데이트                                 │
│    - 사용된 패턴 usage_count +1                                     │
│    - last_used 날짜 업데이트                                        │
│    - examples에 현재 기능 추가                                      │
└─────────────────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────────────┐
│ 4️⃣ 아카이브 저장                                                    │
│    - 위치: /Users/sangwon-ryu/archive/{project}/{feature}/{date}.md│
│    - 내용: plan + design + 사용 패턴 + 특이사항                     │
└─────────────────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────────────┐
│ 5️⃣ 메모리 삭제                                                      │
│    - delete_memory("plan-order-cancel")                             │
│    - delete_memory("design-order-cancel")                           │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 상세 단계

### 1. 메모리 존재 확인

```python
# Serena MCP 호출
plan = mcp__serena__read_memory(memory_file_name="plan-{feature}")
design = mcp__serena__read_memory(memory_file_name="design-{feature}")

if not plan or not design:
    # 에러: 메모리 없음
    raise Error("plan 또는 design 메모리가 없습니다.")
```

### 2. 구현 완료 여부 확인

design 메모리에서 체크리스트 추출 후 파일 존재 확인:

```markdown
## 🔍 구현 완료 여부 확인

| 레이어 | 컴포넌트 | 상태 |
|--------|----------|------|
| Domain | Order.cancel() | ✅ 존재 |
| Domain | OrderCancelledEvent | ✅ 존재 |
| Application | CancelOrderUseCase | ✅ 존재 |
| Application | CancelOrderService | ✅ 존재 |
| Persistence | OrderJpaEntity 수정 | ⚠️ 확인 필요 |

⚠️ 일부 항목 확인이 필요합니다. 계속 진행하시겠습니까? [Y/N]
```

### 3. usage-history 업데이트

```python
# 사용된 패턴 확인 (design 메모리에서)
pattern = design.pattern  # 예: "complex_with_event"

# usage-history 업데이트
mcp__serena__edit_memory(
    memory_file_name="component-usage-history",
    needle=f"pattern_id: {pattern}\\s+usage_count: (\\d+)",
    repl=f"pattern_id: {pattern}\\n    usage_count: {{incremented}}",
    mode="regex"
)

# examples 추가
mcp__serena__edit_memory(
    memory_file_name="component-usage-history",
    needle=f"examples:\\s*\\[",
    repl=f"examples: [\"{feature}\", ",
    mode="regex"
)
```

### 4. 아카이브 저장

```markdown
# 아카이브 파일 구조

위치: /Users/sangwon-ryu/archive/claude-spring-standards/order-cancel/2024-12-08.md

내용:
---
feature: order-cancel
project: claude-spring-standards
completed_at: 2024-12-08
pattern: complex_with_event
---

## 요구사항 요약
- 주문 상태가 PLACED, CONFIRMED일 때만 취소 가능
- 취소 시 결제 환불 처리 필요

## 생성된 컴포넌트
### Domain Layer
- Order.cancel() 메서드
- OrderCancelledEvent

### Application Layer
- CancelOrderUseCase
- CancelOrderService
- CancelOrderFacade
...

## 특이사항
- Payment BC 연동 필요
- TransactionalEventListener로 외부 API 호출

## 레이어 간 계약
(design 메모리에서 복사)
```

### 5. 메모리 삭제

```python
mcp__serena__delete_memory(memory_file_name="plan-{feature}")
mcp__serena__delete_memory(memory_file_name="design-{feature}")
```

---

## 출력 형식

```markdown
## ✅ 작업 완료: order-cancel

### 📊 요약
- **패턴**: complex_with_event
- **생성 컴포넌트**: 12개
- **소요 시간**: 2024-12-06 ~ 2024-12-08

### 💾 아카이브 저장됨
- 위치: /Users/sangwon-ryu/archive/claude-spring-standards/order-cancel/2024-12-08.md

### 📈 usage-history 업데이트됨
- complex_with_event: 5회 → 6회

### 🗑️ 메모리 정리됨
- ❌ plan-order-cancel
- ❌ design-order-cancel

수고하셨습니다! 🎉
```

---

## 강제 완료 옵션

```bash
# 미완료 항목이 있어도 강제 완료
/complete order-cancel --force
```

---

## 관련 커맨드

| 커맨드 | 용도 |
|--------|------|
| `/status {feature}` | 진행률 확인 |
| `/verify {feature}` | 체크리스트 검증 |
| `/cleanup` | 오래된 메모리 정리 |
