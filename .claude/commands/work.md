---
description: Epic 작업 시작. Epic 브랜치 생성, Task 큐 로드, Auto-commit Hook 활성화.
tags: [workflow, implementation, serena]
---

# /work - Start Working on Epic

**Epic 단위**로 작업을 시작합니다. Epic 브랜치 생성 → Task 큐 로드 → 순차 작업.

## 핵심 개념: Epic = 1 Branch

```
/work EPIC-123
        ↓
┌─────────────────────────────────────────────────┐
│  🌿 feature/EPIC-123-payment (Epic 브랜치)      │
│       │                                         │
│       ├── Task 1 작업 → 커밋                    │
│       ├── Task 2 작업 → 커밋 (/next)            │
│       ├── Task 3 작업 → 커밋 (/next)            │
│       └── 완료 → /ship → PR                     │
└─────────────────────────────────────────────────┘
```

**핵심**: Task마다 새 브랜치를 만들지 않습니다. Epic 하나 = 브랜치 하나 = PR 하나.

---

## 사용법

```bash
/work EPIC-123              # Epic으로 작업 시작 (권장)
/work TASK-456              # 특정 Task부터 시작 (Epic 브랜치 내에서)
/work                       # 가장 최근 Epic/Task 로드
/work --continue            # 중단된 작업 이어서
/work EPIC-123 --no-serena  # Serena 없이 실행
```

---

## 실행 프로세스

```
/work EPIC-123
        ↓
┌─────────────────────────────────────────────────┐
│ 1️⃣ Epic 컨텍스트 로드                           │
│    - Jira MCP로 Epic + Sub-tasks 조회           │
│    - Serena Memory에서 jira-epic-{key} 로드     │
│    - Task 큐 (순서대로) 구성                    │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 2️⃣ Epic 브랜치 생성/체크아웃                    │
│    - feature/{epic-key}-{short-desc}           │
│    - 이미 존재하면 체크아웃                     │
│    - main/develop에서 분기                      │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 3️⃣ Jira 상태 업데이트 ⭐                        │
│    - Epic: To Do → In Progress                 │
│    - 첫 번째 Task: To Do → In Progress         │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 4️⃣ 작업 상태 초기화                            │
│    - .claude/state/work-mode 활성화             │
│    - .claude/state/current-epic 기록            │
│    - .claude/state/current-task 기록            │
│    - .claude/state/task-queue 저장              │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 5️⃣ Auto-commit Hook 활성화                     │
│    - 15분 간격 WIP 커밋                         │
│    - 메시지: "WIP: EPIC-123 - Task 1/4 설명"   │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 6️⃣ 첫 번째 Task 작업 시작                      │
│    - Knowledge Base 규칙 로드                   │
│    - Task 컨텍스트 표시                         │
│    - 구현 시작                                  │
└─────────────────────────────────────────────────┘
```

---

## 🔗 Serena 통합 (기본 활성화)

```
/work 실행 시 Serena 자동 연동:

┌─────────────────────────────────────────────────┐
│ 1️⃣ Epic 컨텍스트 로드                          │
│    → read_memory("jira-{epic-key}")            │
│    → Task Queue, 브랜치명, 진행 상태 복원      │
│                                                 │
│ 2️⃣ 관련 코드베이스 분석                        │
│    → find_symbol() - 영향받는 클래스 검색      │
│    → get_symbols_overview() - 구조 파악        │
│    → 수정 대상 파일 목록 생성                  │
│                                                 │
│ 3️⃣ Knowledge Base 로드 (컨벤션)               │
│    → read_memory("convention-*")               │
│    → read_memory("domain-rules-*")             │
│    → 레이어별 규칙 로드                        │
│                                                 │
│ 4️⃣ 작업 상태 저장                              │
│    → write_memory("work-session-{epic}", state)│
│    → 현재 Task, 진행 상태, 커밋 내역           │
└─────────────────────────────────────────────────┘

비활성화: --no-serena 옵션 사용
```

### Serena Memory 작업 세션 형식

**work-session-{epic-key}.md** 예시:
```markdown
# Work Session: AESA-123

## 세션 정보
- Epic: AESA-123
- Branch: feature/AESA-123-payment
- Started: 2024-01-15 14:30
- Last Updated: 2024-01-15 16:45

## 진행 상태
- Current Task: 2/4 (AESA-125)
- Progress: 25% (1/4 완료)
- Auto-commits: 5회

## Task 상태
| 순서 | Key | Status | Commits |
|------|-----|--------|---------|
| 1/4 | AESA-124 | done | 3 |
| 2/4 | AESA-125 | in_progress | 2 |
| 3/4 | AESA-126 | pending | 0 |
| 4/4 | AESA-127 | pending | 0 |
```

---

## Task 큐 시스템

```
┌──────────────────────────────────────────────────┐
│ 🔧 작업 중: EPIC-123 (Payment)                   │
│    🌿 브랜치: feature/EPIC-123-payment           │
├──────────────────────────────────────────────────┤
│ 📋 Task 큐:                                      │
│                                                  │
│    ✅ 1/4 TASK-124 [Domain] PaymentAggregate    │
│    🔄 2/4 TASK-125 [Application] UseCase  ◀     │
│    ⬜ 3/4 TASK-126 [Persistence] Entity          │
│    ⬜ 4/4 TASK-127 [REST API] Controller         │
│                                                  │
│    진행률: ████████░░░░░░░░ 25% (1/4 완료)      │
├──────────────────────────────────────────────────┤
│ ⏰ Auto-commit: 활성화 (3회 커밋됨)              │
│ 🕐 시작: 14:30 | 경과: 1h 15m                    │
└──────────────────────────────────────────────────┘
```

### 상태 아이콘

| 아이콘 | 의미 |
|--------|------|
| ✅ | 완료 (커밋됨) |
| 🔄 | 현재 작업 중 |
| ⬜ | 대기 중 |
| ⏸️ | 일시 중단 |
| ❌ | 스킵됨 |

---

## Auto-commit 동작

```bash
# Epic 단위 WIP 커밋
git add -A
git commit -m "WIP: EPIC-123 [2/4] ProcessPaymentUseCase 구현 중"

# 커밋 메시지 형식
WIP: {EPIC-KEY} [{순서}/{전체}] {현재 Task 설명}

# 예시
WIP: AESA-123 [1/4] PaymentAggregate 기본 구조 생성
WIP: AESA-123 [2/4] ProcessPaymentUseCase 구현 중
WIP: AESA-123 [2/4] UseCase 테스트 작성
```

---

## Skill 연동

작업 중 **implementer** skill이 활성화됩니다:

### Knowledge Base 자동 로드

```bash
# 현재 Task 레이어에 맞는 규칙 자동 로드
Task: [Domain] PaymentAggregate
  → @knowledge/rules/domain-rules.md
  → @knowledge/templates/domain-templates.md

Task: [Application] UseCase
  → @knowledge/rules/application-rules.md
  → @knowledge/templates/application-templates.md
```

### Zero-Tolerance 규칙

- ❌ Lombok 금지
- ❌ Law of Demeter (Getter 체이닝 금지)
- ❌ Long FK 전략
- ❌ Transaction 경계 위반

---

## 작업 상태 파일

```
.claude/state/
├── work-mode              # "active" | "paused" | "inactive"
├── current-epic           # "EPIC-123"
├── current-task           # "TASK-125"
├── task-queue             # JSON: Task 목록 + 상태
├── last-auto-commit       # 마지막 커밋 시간
└── work-started-at        # 작업 시작 시간
```

### task-queue 형식

```json
{
  "epic": "EPIC-123",
  "branch": "feature/EPIC-123-payment",
  "tasks": [
    {"key": "TASK-124", "summary": "[Domain]...", "status": "done", "commits": 3},
    {"key": "TASK-125", "summary": "[Application]...", "status": "in_progress", "commits": 1},
    {"key": "TASK-126", "summary": "[Persistence]...", "status": "pending", "commits": 0},
    {"key": "TASK-127", "summary": "[REST API]...", "status": "pending", "commits": 0}
  ],
  "current_index": 1
}
```

---

## 출력 예시

```
🚀 Epic 작업 시작!

────────────────────────────────────────
📋 Epic: AESA-123
────────────────────────────────────────
   제목: [Payment] 결제 기능 구현
   URL: https://jira.atlassian.net/browse/AESA-123

────────────────────────────────────────
🌿 Git 브랜치:
────────────────────────────────────────
   feature/AESA-123-payment (신규 생성)
   ✅ main에서 분기됨

────────────────────────────────────────
📊 Jira 상태 업데이트:
────────────────────────────────────────
   Epic AESA-123: To Do → In Progress ✅
   Task AESA-124: To Do → In Progress ✅

────────────────────────────────────────
📋 Task 큐 (4개):
────────────────────────────────────────
   🔄 1/4 AESA-124: [Domain] PaymentAggregate 구현      ◀ 현재
   ⬜ 2/4 AESA-125: [Application] ProcessPaymentUseCase
   ⬜ 3/4 AESA-126: [Persistence] PaymentEntity 구현
   ⬜ 4/4 AESA-127: [REST API] PaymentController 구현

────────────────────────────────────────
⏰ Auto-commit:
────────────────────────────────────────
   활성화됨 (15분 간격)
   형식: WIP: AESA-123 [1/4] PaymentAggregate...

────────────────────────────────────────
📚 Knowledge Base 로드됨:
────────────────────────────────────────
   - domain-rules.md (214개 규칙)
   - domain-templates.md (16개 템플릿)
   - zero-tolerance.md (212개 필수 규칙)

════════════════════════════════════════
🔧 Task 1/4 시작: [Domain] PaymentAggregate 구현
════════════════════════════════════════

무엇을 구현할까요?
```

---

## 옵션

| 옵션 | 설명 | 예시 |
|------|------|------|
| `--continue` | 중단된 작업 이어서 | `/work --continue` |
| `--from N` | N번째 Task부터 시작 | `/work EPIC-123 --from 2` |
| `--no-auto-commit` | Auto-commit 비활성화 | `/work EPIC-123 --no-auto-commit` |
| `--status` | 현재 작업 상태만 표시 | `/work --status` |
| `--no-serena` | Serena 없이 실행 (Memory 연동 비활성화) | `/work EPIC-123 --no-serena` |

---

## 작업 흐름

### 정상 흐름

```bash
/work EPIC-123          # Epic 작업 시작, Task 1 진행
# ... 작업 ...
/next                   # Task 1 완료 → Task 2 시작
# ... 작업 ...
/next                   # Task 2 완료 → Task 3 시작
# ... 모든 Task 완료 ...
/ship                   # Epic PR 생성
```

### 중단 후 재개

```bash
/work EPIC-123          # 작업 시작
# ... 작업 중 세션 종료 ...

# 새 세션
/work --continue        # 또는 /resume
# → 마지막 Task부터 이어서 진행
```

### 특정 Task부터 시작

```bash
/work EPIC-123 --from 3   # Task 3부터 시작 (1, 2는 완료 처리)
```

---

## Jira 상태 전이

```
/work 실행 시:

Epic:
   To Do → In Progress

첫 번째 Task:
   To Do → In Progress
```

---

## Jira MCP 활용

```python
# /work 실행 시 Jira 상태 업데이트

# 1. Epic 상태 변경 (To Do → In Progress)
jira_transition_issue(
    issue_key="AESA-123",
    transition="In Progress"  # 또는 transition_id
)

# 2. 첫 번째 Task 상태 변경 (To Do → In Progress)
jira_transition_issue(
    issue_key="AESA-124",
    transition="In Progress"
)
```

---

## 다음 단계

```bash
/next           # 현재 Task 완료 → 다음 Task 시작
/next --skip    # 현재 Task 스킵 → 다음 Task
/next --status  # 진행 상황만 확인

/review         # 코드 리뷰 요청 (Task 완료 후)
/check          # 정적 분석 실행
/ship           # 모든 Task 완료 후 Epic PR 생성
```
