---
description: Epic 문서 기반으로 Jira에 Epic/Task 등록. 메타데이터 포함. Jira MCP 활용.
tags: [workflow, jira, serena]
---

# /jira-create - Create Jira Epic & Tasks

`/epic` 커맨드로 생성된 문서를 기반으로 Jira에 Epic과 Sub-task를 **메타데이터 포함**하여 등록합니다.

## 핵심 개념: Epic 중심 워크플로우

```
📋 Epic = 1 Branch = 1 PR
     │
     ├── Task 1 → 커밋
     ├── Task 2 → 커밋
     ├── Task 3 → 커밋
     └── 완료 → /ship → PR
```

**중요**: Epic이 하나의 브랜치가 됩니다. Task는 같은 브랜치에서 순차적으로 커밋됩니다.

---

## 사용법

```bash
/jira-create                          # 가장 최근 epic 문서 사용
/jira-create epic-payment             # 특정 epic 문서 지정
/jira-create --dry-run                # 등록 없이 미리보기
/jira-create --no-serena              # Serena 없이 실행
```

---

## 실행 프로세스

```
/jira-create
        ↓
┌─────────────────────────────────────────────────┐
│ 1️⃣ Epic 문서 로드                               │
│    - .claude/plans/epic-{feature}.md 읽기       │
│    - 또는 Serena Memory에서 로드                │
│    - 작업 유형 확인 (신규/확장/리팩토링/버그/연동)│
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 1.5️⃣ 현재 사용자 정보 조회 ⭐                   │
│    - atlassianUserInfo()로 본인 account_id 조회 │
│    - Epic/Task 담당자로 자동 지정               │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 2️⃣ 메타데이터 수집                             │
│    - Components 추출 (레이어 기반)              │
│    - Story Points 계산 (복잡도 기반)            │
│    - Priority 결정 (비즈니스 영향도)            │
│    - Labels 생성 (작업 유형 + 도메인)           │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 3️⃣ Jira Epic 생성 (Jira MCP)                   │
│    - Epic 타입 이슈 생성                        │
│    - 전체 메타데이터 설정                       │
│    - 브랜치명 규칙: feature/EPIC-{key}          │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 4️⃣ Sub-task 일괄 생성                          │
│    - 각 Task를 Sub-task로 등록                  │
│    - Task별 Story Points 배분                   │
│    - 실행 순서 (order) 명시                     │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 5️⃣ 결과 저장                                   │
│    - Epic 문서에 Jira ID 추가                   │
│    - Serena Memory 업데이트                     │
│    - 브랜치명 정보 포함                         │
└─────────────────────────────────────────────────┘
```

---

## 🔗 Serena 통합 (기본 활성화)

```
/jira-create 실행 시 Serena 자동 연동:

┌─────────────────────────────────────────────────┐
│ 1️⃣ Epic 문서 로드 (Serena Memory)              │
│    → read_memory("epic-{feature}")             │
│    → 작업 유형, Task 목록, 비즈니스 규칙 추출   │
│                                                 │
│ 2️⃣ 코드베이스 분석 (영향도 파악)               │
│    → find_symbol() - 관련 클래스 검색          │
│    → 레이어별 Component 자동 추출              │
│                                                 │
│ 3️⃣ Jira 이슈 생성 결과 저장                   │
│    → write_memory("jira-{epic-key}", result)   │
│    → Epic key, Sub-task keys, 브랜치명 저장    │
│                                                 │
│ 4️⃣ Epic 문서 업데이트                         │
│    → edit_memory("epic-{feature}", jira_info)  │
│    → Jira ID, 브랜치명 추가                    │
└─────────────────────────────────────────────────┘

비활성화: --no-serena 옵션 사용
```

### Serena Memory 저장 형식

**jira-{epic-key}.md** 예시:
```markdown
# Jira Epic: AESA-123

## Epic 정보
- Key: AESA-123
- Summary: [Payment] 결제 기능 구현
- Branch: feature/AESA-123-payment
- Status: To Do
- Story Points: 13

## Sub-tasks
| 순서 | Key | Summary | SP | Status |
|------|-----|---------|----|----|
| 1/4 | AESA-124 | [Domain] PaymentAggregate 구현 | 3 | To Do |
| 2/4 | AESA-125 | [Application] ProcessPaymentUseCase | 2 | To Do |
| 3/4 | AESA-126 | [Persistence] PaymentEntity 구현 | 2 | To Do |
| 4/4 | AESA-127 | [REST API] PaymentController 구현 | 1 | To Do |

## 참조
- Epic Memory: epic-payment
- Created: 2024-01-15
```

---

## 메타데이터 상세

### Components (레이어 기반 자동 추출)

| Component | 기준 | 예시 |
|-----------|------|------|
| `domain` | Domain Layer 작업 포함 | Aggregate, VO, Event |
| `application` | Application Layer 작업 포함 | UseCase, Service |
| `persistence` | Persistence Layer 작업 포함 | Entity, Repository |
| `rest-api` | REST API Layer 작업 포함 | Controller, DTO |
| `infrastructure` | 인프라 작업 포함 | Config, External API |

### Story Points (복잡도 기반 계산)

```
┌─────────────────────────────────────────────────────────────┐
│ Story Points 계산 공식                                       │
├─────────────────────────────────────────────────────────────┤
│ 기본값: Task 개수 × 레이어당 기본 포인트                     │
│                                                             │
│ 레이어별 기본 포인트:                                        │
│   - Domain: 3점 (비즈니스 로직 복잡)                         │
│   - Application: 2점 (조합/조율)                             │
│   - Persistence: 2점 (쿼리 복잡도)                           │
│   - REST API: 1점 (상대적 단순)                              │
│                                                             │
│ 보정 계수:                                                   │
│   - 신규 개발: ×1.5                                          │
│   - 기존 수정: ×1.0                                          │
│   - 리팩토링: ×1.2 (테스트 필요)                             │
│   - 버그 수정: ×0.8                                          │
│   - 외부 연동: ×2.0 (불확실성)                               │
└─────────────────────────────────────────────────────────────┘
```

### Priority (비즈니스 영향도 기반)

| Priority | 기준 |
|----------|------|
| `Highest` | 서비스 장애, 매출 직접 영향 |
| `High` | 핵심 기능, 고객 영향 |
| `Medium` | 일반 기능, 개선 |
| `Low` | 리팩토링, 기술 부채 |
| `Lowest` | 선택적 개선 |

### Labels (작업 유형 + 도메인)

```yaml
# 자동 생성되는 Labels
작업_유형:
  - new-feature      # 🆕 신규 기능
  - extension        # ➕ 기능 확장
  - refactoring      # 🔄 리팩토링
  - bugfix           # 🐛 버그 수정
  - integration      # 🔌 외부 연동

도메인:
  - payment          # 결제
  - order            # 주문
  - member           # 회원
  - product          # 상품
  # ... (Epic에서 추출)

기술:
  - backend
  - hexagonal
  - spring-boot
```

---

## Jira MCP 활용 (상세)

```python
# 0. 현재 사용자 정보 조회 (본인 자동 할당용)
current_user = atlassianUserInfo()
my_account_id = current_user.account_id  # 담당자로 사용

# 1. Epic 생성 (메타데이터 포함 + 본인 할당)
epic = jira_create_issue(
    project="AESA",
    issue_type="Epic",
    summary="[Payment] 결제 기능 구현",
    description="""
    ## 개요
    주문 결제 기능을 구현합니다.

    ## 작업 범위
    - Domain: PaymentAggregate, PaymentStatus
    - Application: ProcessPaymentUseCase
    - Persistence: PaymentEntity, PaymentRepository
    - REST API: PaymentController

    ## 비즈니스 규칙
    - BR-001: 결제 금액은 주문 금액과 일치해야 함
    - BR-002: 결제 실패 시 3회 재시도

    ## 브랜치
    feature/EPIC-{key}-payment
    """,

    # 메타데이터
    components=["domain", "application", "persistence", "rest-api"],
    story_points=13,
    priority="High",
    labels=["new-feature", "payment", "backend", "hexagonal"],

    # 담당자 (본인 자동 할당) ⭐
    assignee_account_id=my_account_id,

    # 스프린트 (선택)
    sprint="Sprint 23"
)

# 2. Sub-task 생성 (순서와 포인트 포함)
tasks = [
    {
        "summary": "[Domain] PaymentAggregate 구현",
        "story_points": 3,
        "order": 1,
        "labels": ["domain", "aggregate"]
    },
    {
        "summary": "[Application] ProcessPaymentUseCase 구현",
        "story_points": 2,
        "order": 2,
        "labels": ["application", "usecase"]
    },
    {
        "summary": "[Persistence] PaymentEntity 구현",
        "story_points": 2,
        "order": 3,
        "labels": ["persistence", "entity"]
    },
    {
        "summary": "[REST API] PaymentController 구현",
        "story_points": 1,
        "order": 4,
        "labels": ["rest-api", "controller"]
    }
]

for task in tasks:
    jira_create_issue(
        project="AESA",
        issue_type="Sub-task",
        parent=epic.key,
        summary=task["summary"],
        story_points=task["story_points"],
        labels=task["labels"],
        description=f"순서: {task['order']}/{len(tasks)}",
        assignee_account_id=my_account_id  # ⭐ 본인 할당
    )
```

---

## 출력 예시

```
✅ Jira Epic 생성 완료!

────────────────────────────────────────
📋 Epic: AESA-123
────────────────────────────────────────
   제목: [Payment] 결제 기능 구현
   URL: https://your-jira.atlassian.net/browse/AESA-123

   📊 메타데이터:
      Components: domain, application, persistence, rest-api
      Story Points: 13
      Priority: High
      Labels: new-feature, payment, backend, hexagonal
      Sprint: Sprint 23
      Assignee: sangwon.ryu (본인) ⭐

   🌿 브랜치명:
      feature/AESA-123-payment

────────────────────────────────────────
📝 Sub-tasks 생성됨 (총 4개, 13 SP):
────────────────────────────────────────
   순서  │ Key        │ 제목                              │ SP
   ──────┼────────────┼───────────────────────────────────┼────
   1/4   │ AESA-124   │ [Domain] PaymentAggregate 구현    │ 3
   2/4   │ AESA-125   │ [Application] ProcessPaymentUseCase│ 2
   3/4   │ AESA-126   │ [Persistence] PaymentEntity 구현  │ 2
   4/4   │ AESA-127   │ [REST API] PaymentController 구현 │ 1

────────────────────────────────────────
💾 저장됨:
────────────────────────────────────────
   📄 Epic 문서: .claude/plans/epic-payment.md (Jira ID 추가됨)
   🧠 Serena Memory: jira-epic-AESA-123

────────────────────────────────────────
📌 다음 단계:
────────────────────────────────────────
   /work AESA-123    # Epic 브랜치 생성 및 작업 시작
                     # → feature/AESA-123-payment 브랜치 생성
                     # → Task 1 (AESA-124)부터 순차 진행
```

---

## 옵션

| 옵션 | 설명 | 예시 |
|------|------|------|
| `--dry-run` | 실제 등록 없이 미리보기 | `/jira-create --dry-run` |
| `--project KEY` | Jira 프로젝트 키 지정 | `/jira-create --project AESA` |
| `--assignee user` | 다른 담당자 지정 (기본: 본인) | `/jira-create --assignee john` |
| `--sprint N` | 스프린트 지정 | `/jira-create --sprint 23` |
| `--priority P` | 우선순위 수동 지정 | `/jira-create --priority High` |
| `--no-subtasks` | Sub-task 없이 Epic만 생성 | `/jira-create --no-subtasks` |
| `--no-serena` | Serena 없이 실행 (Memory 연동 비활성화) | `/jira-create --no-serena` |

---

## Epic 문서 업데이트

등록 후 Epic 문서에 Jira 정보가 추가됩니다:

```markdown
## Jira 정보

| 항목 | 값 |
|------|-----|
| Epic | [AESA-123](https://jira.../AESA-123) |
| Branch | `feature/AESA-123-payment` |
| Sprint | Sprint 23 |
| Story Points | 13 |

### Sub-tasks

| 순서 | Key | 제목 | SP | Status |
|------|-----|------|----|----|
| 1/4 | AESA-124 | [Domain] PaymentAggregate 구현 | 3 | To Do |
| 2/4 | AESA-125 | [Application] ProcessPaymentUseCase | 2 | To Do |
| 3/4 | AESA-126 | [Persistence] PaymentEntity 구현 | 2 | To Do |
| 4/4 | AESA-127 | [REST API] PaymentController 구현 | 1 | To Do |
```

---

## 다음 단계

등록 완료 후:

```bash
/work AESA-123        # Epic 브랜치 생성 및 첫 번째 Task 시작
                      # (feature/AESA-123-payment 브랜치)
```
