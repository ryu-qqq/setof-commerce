---
description: Jira 태스크 분석 및 TodoList 생성
tags: [project]
---

# /jira-task - Jira 이슈 정보 가져오기 + Plan 연동

**목적**: Jira 이슈 정보를 가져와서 요구사항 분석 및 `/plan` 실행

## 사용법

```bash
/jira-task {ISSUE-KEY}
/jira-task MEMBER-123
/jira-task ORDER-456
```

---

## 실행 프로세스

```
/jira-task MEMBER-123
        ↓
┌─────────────────────────────────────────────────┐
│ 1️⃣ Jira API 호출                                │
│    - Issue 정보 조회                             │
│    - Description, Acceptance Criteria 추출      │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 2️⃣ 요구사항 정리                                │
│    - Jira 내용 → 구조화된 요구사항               │
│    - 비즈니스 규칙 추출                          │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 3️⃣ /plan 자동 실행                              │
│    - 영향도 분석                                 │
│    - 구현 전략 결정                              │
│    - Serena Memory 저장                         │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 4️⃣ 결과 출력                                    │
│    - Plan 요약                                  │
│    - 다음 단계 안내                              │
└─────────────────────────────────────────────────┘
```

---

## Jira API 호출

### Issue 조회

```bash
GET /rest/api/3/issue/{issueKey}
Authorization: Basic {base64(email:api_token)}
```

### 추출할 필드

| 필드 | Jira 경로 | 용도 |
|------|----------|------|
| 제목 | `fields.summary` | 기능명 |
| 설명 | `fields.description` | 상세 요구사항 |
| 이슈 타입 | `fields.issuetype.name` | Task/Story/Bug |
| 우선순위 | `fields.priority.name` | 작업 순서 |
| 라벨 | `fields.labels` | 도메인 힌트 |
| Epic | `fields.parent` 또는 `customfield_*` | 상위 기능 |
| Acceptance Criteria | `fields.customfield_*` | 완료 조건 |

---

## 출력 예시

```
🔍 Jira Issue 조회 중...
   ✅ MEMBER-123: 회원 탈퇴 기능 구현

📋 Issue 정보:
   └─ 타입: Task
   └─ 우선순위: High
   └─ Epic: MEMBER (회원 관리)
   └─ 라벨: [backend, domain]

📝 요구사항 추출:
   ## 기능 설명
   회원이 자신의 계정을 탈퇴할 수 있다.

   ## Acceptance Criteria
   - [ ] 탈퇴 시 개인정보 삭제
   - [ ] 탈퇴 후 재가입 30일 제한
   - [ ] 탈퇴 확인 이메일 발송

🚀 /plan "회원 탈퇴 기능" 실행 중...

─────────────────────────────────────

## 📊 영향도 분석 결과

| 레이어 | 파일 | 상태 | 전략 |
|--------|------|------|------|
| Domain | Member.java | 🔧 수정 | TDD |
| Application | - | 🆕 신규 | Doc |
| Persistence | MemberEntity.java | 🔧 수정 | TDD |
| REST API | - | 🆕 신규 | Doc |

## 🚀 구현 계획
1. [TDD] Domain: Member.withdraw() 추가
2. [Doc] Application: WithdrawMemberUseCase 생성
3. [TDD] Persistence: MemberEntity soft delete
4. [Doc] REST API: DELETE /members/{id}

✅ Plan 저장 완료: plan-member-withdraw

🔗 다음 단계:
   /jira-register  (Jira에 Plan 등록 + 브랜치 생성)
```

---

## Jira 환경 설정

### 환경 변수

```bash
# .env 또는 환경 변수
JIRA_URL=https://your-company.atlassian.net
JIRA_EMAIL=your-email@company.com
JIRA_API_TOKEN=your-api-token
JIRA_PROJECT_KEY=MEMBER
```

### API Token 발급

1. https://id.atlassian.com/manage/api-tokens
2. "Create API token" 클릭
3. 토큰 복사 → 환경 변수에 설정

---

## 에러 처리

### Issue 없음
```
❌ Jira Issue를 찾을 수 없습니다: MEMBER-999
   - Issue Key를 확인하세요
   - 프로젝트 접근 권한을 확인하세요
```

### 인증 실패
```
❌ Jira 인증 실패
   - JIRA_EMAIL, JIRA_API_TOKEN 환경 변수 확인
   - API Token 유효성 확인
```

### 네트워크 오류
```
❌ Jira 연결 실패
   - 네트워크 연결 확인
   - VPN 연결 여부 확인
```

---

## 연관 커맨드

- `/plan "{기능}"` - 직접 Plan 실행 (Jira 없이)
- `/jira-register` - Plan 결과를 Jira에 등록
- `/jira-status` - Jira 상태 변경
