---
description: 중단된 작업 재개. Serena Memory에서 컨텍스트 복구.
tags: [workflow, recovery, serena]
---

# /resume - Resume Interrupted Work

중단된 작업을 재개합니다. Serena Memory에서 컨텍스트를 복구하고 이어서 작업합니다.

## 사용법

```bash
/resume                     # 가장 최근 작업 재개
/resume PROJ-456            # 특정 Task 재개
/resume --list              # 재개 가능한 작업 목록
/resume --no-serena         # Serena 없이 실행
```

## 실행 프로세스

```
/resume
        ↓
┌─────────────────────────────────────────────────┐
│ 1️⃣ 작업 상태 확인                               │
│    - Serena Memory 목록 조회                    │
│    - 진행 중인 작업 식별                        │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 2️⃣ 컨텍스트 복구                                │
│    - task-{key} Memory 로드                    │
│    - 진행 상황 파악                             │
│    - 남은 작업 항목 확인                        │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 3️⃣ Git 상태 확인                                │
│    - 현재 브랜치 확인                           │
│    - 미커밋 변경사항 확인                       │
│    - 필요시 브랜치 전환                         │
└─────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────┐
│ 4️⃣ 작업 재개                                    │
│    - /work 모드 활성화                          │
│    - Auto-commit Hook 재활성화                  │
│    - 이어서 구현 진행                           │
└─────────────────────────────────────────────────┘
```

---

## 🔗 Serena 통합 (기본 활성화)

```
/resume 실행 시 Serena 자동 연동:

┌─────────────────────────────────────────────────┐
│ 1️⃣ 진행 중인 작업 검색                          │
│    → list_memories()                            │
│    → work-session-*, task-* 패턴 검색           │
│    → 가장 최근 작업 세션 식별                   │
│                                                 │
│ 2️⃣ 작업 컨텍스트 복구                          │
│    → read_memory("work-session-{epic}")         │
│    → Task 큐, 진행 상태, 커밋 내역 로드         │
│    → 마지막 작업 위치 파악                      │
│                                                 │
│ 3️⃣ 관련 코드베이스 분석                        │
│    → find_symbol() - 작업 중인 클래스 검색      │
│    → get_symbols_overview() - 구조 파악         │
│    → 마지막 변경 지점 확인                      │
│                                                 │
│ 4️⃣ Knowledge Base 재로드                       │
│    → read_memory("{layer}-rules")               │
│    → 현재 Task 레이어에 맞는 규칙 로드          │
│                                                 │
│ 5️⃣ 세션 상태 업데이트                          │
│    → edit_memory("work-session-{epic}", resumed)│
│    → 재개 시간 기록                             │
└─────────────────────────────────────────────────┘

비활성화: --no-serena 옵션 사용
```

---

## Serena Memory 구조

```markdown
# task-PROJ-456

## 상태
- **시작 시간**: 2025-01-14 14:30
- **마지막 작업**: 2025-01-14 16:45
- **진행률**: 60%

## 완료된 항목
- [x] Order.java 생성
- [x] OrderStatus.java 생성

## 진행 중
- [ ] OrderTest.java 작성 (50% 완료)

## 남은 항목
- [ ] 통합 테스트 작성
- [ ] 리뷰 요청

## 마지막 컨텍스트
현재 OrderTest.java에서 cancel() 메서드 테스트 작성 중.
validate() 테스트는 완료됨.
```

## 출력 예시

### 작업 목록 조회

```bash
/resume --list
```

```
📋 재개 가능한 작업 목록

┌─────────────────────────────────────────────────┐
│ 1. PROJ-456: [Domain] Order Aggregate          │
│    진행률: 60% | 마지막: 2시간 전               │
│    브랜치: feature/PROJ-456-order-aggregate    │
│    남은 항목: 테스트 완료, 리뷰                 │
└─────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────┐
│ 2. PROJ-445: [API] 인증 엔드포인트             │
│    진행률: 30% | 마지막: 1일 전                 │
│    브랜치: feature/PROJ-445-auth-api           │
│    남은 항목: Controller, 테스트               │
└─────────────────────────────────────────────────┘

재개하려면: /resume PROJ-456
```

### 작업 재개

```bash
/resume PROJ-456
```

```
🔄 작업 재개: PROJ-456

────────────────────────────────────────
📋 Task 정보
────────────────────────────────────────
제목: [Domain] Order Aggregate 구현
상태: In Progress
진행률: 60%

────────────────────────────────────────
🌿 Git 상태
────────────────────────────────────────
현재 브랜치: main
목표 브랜치: feature/PROJ-456-order-aggregate

🔄 브랜치 전환 중...
✅ feature/PROJ-456-order-aggregate로 전환됨

────────────────────────────────────────
📝 진행 상황 복구
────────────────────────────────────────
✅ 완료:
   - Order.java 생성
   - OrderStatus.java 생성

🔄 진행 중:
   - OrderTest.java (cancel() 테스트 작성 중)

⬜ 남은 작업:
   - 통합 테스트 작성
   - 리뷰 요청

────────────────────────────────────────
📚 마지막 컨텍스트
────────────────────────────────────────
"OrderTest.java에서 cancel() 메서드 테스트 작성 중.
 validate() 테스트는 완료됨."

────────────────────────────────────────
⏰ Auto-commit 재활성화됨
────────────────────────────────────────

이어서 작업을 진행합니다. 어디서부터 계속할까요?
```

## 오토 컴팩팅 대응

Claude 세션이 오토 컴팩팅되어도 Serena Memory에 저장된 컨텍스트로 복구 가능:

```
세션 1                          세션 2 (컴팩팅 후)
────────                        ────────────────────
/work PROJ-456                  "아까 작업 이어서"
  ↓                               ↓
작업 진행 중...                 /resume PROJ-456
  ↓                               ↓
⚠️ 오토컴팩팅                   Serena Memory 로드
                                  ↓
                                컨텍스트 복구 ✅
                                  ↓
                                작업 계속...
```

## 옵션

| 옵션 | 설명 | 예시 |
|------|------|------|
| `--list` | 재개 가능한 작업 목록 | `/resume --list` |
| `--force` | 브랜치 상태 무시하고 재개 | `/resume PROJ-456 --force` |
| `--clean` | 오래된 작업 기록 정리 | `/resume --clean` |
| `--no-serena` | Serena 없이 실행 (Memory 연동 비활성화) | `/resume --no-serena` |

## 관련 커맨드

```bash
/work PROJ-456    # 새 작업 시작
/ship             # 작업 완료 및 푸시
```
