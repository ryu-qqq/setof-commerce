# 엔드포인트 목록: Order 모듈

> Generated by `/legacy-endpoints` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin

---

## 📊 요약

| 분류 | 개수 |
|------|------|
| Query (조회) | 6개 |
| Command (명령) | 3개 |
| **총계** | **9개** |

---

## 📖 Query (조회성) - 6개

| # | Controller | Method | HTTP | Path | Request Type |
|---|------------|--------|------|------|--------------|
| 1 | OrderController | fetchOrder | GET | /api/v1/order/{orderId} | @PathVariable |
| 2 | OrderController | getOrders | GET | /api/v1/orders | @ModelAttribute + Pageable |
| 3 | OrderController | getSettlements | GET | /api/v1/settlements | @ModelAttribute + Pageable |
| 4 | OrderController | getOrderHistory | GET | /api/v1/order/history/{orderId} | @PathVariable |
| 5 | OrderController | getOrderTodayDashboard | GET | /api/v1/order/today-dashboard | @ModelAttribute |
| 6 | OrderController | getOrderDateDashboard | GET | /api/v1/order/date-dashboard | @ModelAttribute |

### 상세 목록

#### 1. OrderController.fetchOrder
- **HTTP**: `GET /api/v1/order/{orderId}`
- **Request**: `@PathVariable("orderId") long orderId`
- **Response**: `ApiResponse<OrderResponse>`
- **설명**: 주문 단건 상세 조회

**Response DTO 구조**:
```java
OrderResponse {
    long orderId;
    BuyerInfo buyerInfo;
    PaymentDetail payment;
    ReceiverInfo receiverInfo;
    PaymentShipmentInfo paymentShipmentInfo;
    SettlementInfo settlementInfo;
    OrderProduct orderProduct;
}
```

---

#### 2. OrderController.getOrders
- **HTTP**: `GET /api/v1/orders`
- **Request**: `@ModelAttribute @Validated OrderFilter filter, Pageable pageable`
- **Response**: `ApiResponse<CustomPageable<OrderListResponse>>`
- **설명**: 주문 목록 조회 (페이징)

**Request DTO 구조**:
```java
OrderFilter extends SearchAndDateFilter implements RoleFilter {
    Long lastDomainId;
    List<OrderStatus> orderStatusList;
    @NotNull PeriodType periodType;
    Long sellerId;
}
```

**Response DTO 구조**:
```java
OrderListResponse {
    long orderId;
    BuyerInfo buyerInfo;
    PaymentDetail payment;
    ReceiverInfo receiverInfo;
    PaymentShipmentInfo paymentShipmentInfo;
    SettlementInfo settlementInfo;
    OrderProduct orderProduct;
    List<OrderHistoryResponse> orderHistories;
}
```

---

#### 3. OrderController.getSettlements
- **HTTP**: `GET /api/v1/settlements`
- **Request**: `@ModelAttribute @Validated BaseDateTimeRangeFilter filter, Pageable pageable`
- **Response**: `ApiResponse<CustomPageable<SettlementResponse>>`
- **설명**: 정산 목록 조회 (페이징)

**Request DTO 구조**:
```java
BaseDateTimeRangeFilter extends BaseRoleFilter implements DateRangeFilter {
    @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    LocalDateTime startDate;
    @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    LocalDateTime endDate;
    Long sellerId; // from BaseRoleFilter
}
```

**Response DTO 구조**:
```java
SettlementResponse {
    long orderCount;
    long ourMallOrderCount;
    long externalMallOrderCount;
    String settlementDay;
    String settlementCompleteDay;
    BigDecimal totalCurrentPrice;
    BigDecimal totalDiscountAmount;
    BigDecimal sellerDiscountAmount;
    BigDecimal ourDiscountAmount;
    BigDecimal totalMileageAmount;
    BigDecimal sellerMileageAmount;
    BigDecimal ourMileageAmount;
    BigDecimal settlementAmount;
    BigDecimal expectationSettlementAmount;
    BigDecimal totalFee;
}
```

---

#### 4. OrderController.getOrderHistory
- **HTTP**: `GET /api/v1/order/history/{orderId}`
- **Request**: `@PathVariable("orderId") long orderId`
- **Response**: `ApiResponse<List<OrderHistoryResponse>>`
- **설명**: 주문 이력 목록 조회

**Response DTO 구조**:
```java
OrderHistoryResponse {
    long orderId;
    String changeReason;
    String changeDetailReason;
    OrderStatus orderStatus;
    String invoiceNo;
    ShipmentCompanyCode shipmentCompanyCode;
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    LocalDateTime updateDate;
}
```

---

#### 5. OrderController.getOrderTodayDashboard
- **HTTP**: `GET /api/v1/order/today-dashboard`
- **Request**: `@ModelAttribute @Validated BaseRoleFilter filter`
- **Response**: `ApiResponse<OrderDashboardResponse>`
- **설명**: 오늘 주문 대시보드 조회

**Request DTO 구조**:
```java
BaseRoleFilter implements RoleFilter {
    Long sellerId;
}
```

**Response DTO 구조**:
```java
OrderDashboardResponse {
    TodayDashboard todayDashboard;
    List<OrderStatistics> monthOrderStatistics;
}
```

---

#### 6. OrderController.getOrderDateDashboard
- **HTTP**: `GET /api/v1/order/date-dashboard`
- **Request**: `@ModelAttribute @Validated BaseDateTimeRangeFilter filter`
- **Response**: `ApiResponse<OrderDateDashboardResponse>`
- **설명**: 기간별 주문 대시보드 조회

**Response DTO 구조**:
```java
OrderDateDashboardResponse {
    OrderStatistics orderStatistics;
    List<OrderDashboardRanking> orderDashBoardRankings;
    List<OrderStatistics> externalMallOrderStatistics;
}
```

---

## ✏️ Command (커맨드성) - 3개

| # | Controller | Method | HTTP | Path | Request Type |
|---|------------|--------|------|------|--------------|
| 1 | OrderController | modifyOrderStatus | PUT | /api/v1/order | @RequestBody |
| 2 | OrderController | modifyOrderStatusList | PUT | /api/v1/orders | @RequestBody |
| 3 | OrderController | modifyOrderStatusList | PATCH | /api/v1/shipment/order/{orderId} | @PathVariable + @RequestBody |

### 상세 목록

#### 1. OrderController.modifyOrderStatus
- **HTTP**: `PUT /api/v1/order`
- **Request**: `@RequestBody @Validated T dto` (T extends UpdateOrder)
- **Response**: `ApiResponse<UpdateOrderResponse>`
- **설명**: 주문 상태 단건 수정

**Request DTO 구조 (다형성 지원)**:
```java
// UpdateOrder 인터페이스의 구현체들
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type")
@JsonSubTypes({
    @JsonSubTypes.Type(value = ShipOrder.class, name = "shipOrder"),
    @JsonSubTypes.Type(value = ClaimOrder.class, name = "claimOrder"),
    @JsonSubTypes.Type(value = ClaimRejectedAndShipmentOrder.class, name = "claimRejectedAndShipmentOrder"),
    @JsonSubTypes.Type(value = NormalOrder.class, name = "normalOrder"),
})
interface UpdateOrder {
    long getOrderId();
    OrderStatus getOrderStatus();
    String getChangeReason();
    String getChangeDetailReason();
    boolean isByPass();
}
```

**Response DTO 구조**:
```java
UpdateOrderResponse {
    long orderId;
    long userId;
    OrderStatus toBeOrderStatus;
    OrderStatus asIsOrderStatus;
    @JsonInclude(JsonInclude.Include.NON_NULL)
    String changeReason;
    @JsonInclude(JsonInclude.Include.NON_NULL)
    String changeDetailReason;
}
```

---

#### 2. OrderController.modifyOrderStatusList
- **HTTP**: `PUT /api/v1/orders`
- **Request**: `@RequestBody @Validated List<T> dto` (T extends UpdateOrder)
- **Response**: `ApiResponse<List<UpdateOrderResponse>>`
- **설명**: 주문 상태 다건 수정 (벌크)

---

#### 3. OrderController.modifyOrderStatusList (배송 시작)
- **HTTP**: `PATCH /api/v1/shipment/order/{orderId}`
- **Request**: `@PathVariable("orderId") long orderId, @RequestBody @Validated ShipmentInfo shipmentInfo`
- **Response**: `ApiResponse<ShipmentInfo>`
- **설명**: 배송 시작 처리 (송장번호 등록)

**Request DTO 구조**:
```java
ShipmentInfo {
    @Length(max = 80)
    String invoiceNo;
    @NotNull
    ShipmentType shipmentType;
    @NotNull
    ShipmentCompanyCode companyCode;
}
```

---

## 🔐 보안 설정

- **인증**: `@PreAuthorize(HAS_ANY_AUTHORITY_MASTER_SELLER)`
- **Base Path**: `/api/v1`

---

## 📦 의존 서비스

| 서비스 | 용도 |
|--------|------|
| `OrderFetchService` | 주문 조회 (목록, 상세, 정산, 대시보드) |
| `OrderHistoryFetchService` | 주문 이력 조회 |
| `OrderUpdateService<T>` | 주문 상태 변경 |
| `ShipmentQueryService` | 배송 시작 처리 |

---

## 🔗 다음 단계

### Query 엔드포인트 분석:
```bash
/legacy-flow admin:OrderController.fetchOrder
/legacy-flow admin:OrderController.getOrders
/legacy-flow admin:OrderController.getSettlements
/legacy-flow admin:OrderController.getOrderHistory
/legacy-flow admin:OrderController.getOrderTodayDashboard
/legacy-flow admin:OrderController.getOrderDateDashboard
```

### Command 엔드포인트 분석:
```bash
/legacy-flow admin:OrderController.modifyOrderStatus
/legacy-flow admin:OrderController.modifyOrderStatusList
```

### DTO 변환:
```bash
/legacy-convert admin:OrderController.fetchOrder
/legacy-convert admin:OrderController.getOrders
```
