# 엔드포인트 목록: QNA 모듈 (Admin)

> Generated by `/legacy-endpoints` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin

---

## 📊 요약

| 분류 | 개수 |
|------|------|
| Query (조회) | 2개 |
| Command (명령) | 3개 |
| **총계** | **5개** |

### Controller 분포

| Controller | Query | Command | 총계 |
|------------|-------|---------|------|
| QnaController | 2 | 2 | 4 |
| ExternalMallQnaController | 0 | 1 | 1 |

---

## 📖 Query (조회성) - 2개

| # | Controller | Method | HTTP | Path | Request Type |
|---|------------|--------|------|------|--------------|
| 1 | QnaController | fetchQna | GET | /api/v1/qna/{qnaId} | @PathVariable |
| 2 | QnaController | fetchQnas | GET | /api/v1/qnas | @ModelAttribute + Pageable |

### 상세 목록

#### 1. QnaController.fetchQna
- **HTTP**: `GET /api/v1/qna/{qnaId}`
- **Request**: `@PathVariable("qnaId") long qnaId`
- **Response**: `ApiResponse<DetailQnaResponse>`
- **권한**: `@PreAuthorize(HAS_ANY_AUTHORITY_MASTER_SELLER)`
- **설명**: QnA 단건 상세 조회

**Response 구조** (`DetailQnaResponse`):
```java
public class DetailQnaResponse {
    private FetchQnaResponse qna;           // QnA 기본 정보
    private Set<AnswerQnaResponse> answerQnas;  // 답변 목록
}
```

---

#### 2. QnaController.fetchQnas
- **HTTP**: `GET /api/v1/qnas`
- **Request**: `@Validated @ModelAttribute QnaFilter filterDto, Pageable pageable`
- **Response**: `ApiResponse<CustomPageable<FetchQnaResponse>>`
- **권한**: `@PreAuthorize(HAS_ANY_AUTHORITY_MASTER_SELLER)`
- **설명**: QnA 목록 조회 (페이징, 필터링)

**Request 구조** (`QnaFilter`):
```java
@AuthorityValidate
public class QnaFilter extends SearchAndDateFilter implements RoleFilter {
    private QnaStatus qnaStatus;       // QnA 상태
    @NotNull private QnaType qnaType;  // QnA 타입 (필수)
    private QnaDetailType qnaDetailType; // QnA 상세 타입
    private Yn privateYn;              // 비공개 여부
    private Long lastDomainId;         // 마지막 도메인 ID (커서 페이징)
    private Long sellerId;             // 판매자 ID (권한에 따라 자동 설정)
}
```

**Response 구조** (`FetchQnaResponse`):
```java
public class FetchQnaResponse {
    private long qnaId;
    private QnaContents qnaContents;
    private Yn privateYn;
    private QnaStatus qnaStatus;
    private QnaType qnaType;
    private QnaDetailType qnaDetailType;
    private String sellerName;
    private UserInfoQnaDto userInfo;
    private QnaTarget qnaTarget;
    private LocalDateTime insertDate;
    private LocalDateTime updateDate;
    private List<QnaImageDto> qnaImages;
}
```

---

## ✏️ Command (커맨드성) - 3개

| # | Controller | Method | HTTP | Path | Request Type |
|---|------------|--------|------|------|--------------|
| 1 | QnaController | replyQna | POST | /api/v1/qna/reply | @RequestBody |
| 2 | QnaController | updateReplyQna | PUT | /api/v1/qna/reply | @RequestBody |
| 3 | ExternalMallQnaController | interLockingOrders | POST | /api/v1/external/qna | @RequestBody |

### 상세 목록

#### 1. QnaController.replyQna
- **HTTP**: `POST /api/v1/qna/reply`
- **Request**: `@RequestBody CreateQnaAnswer createQnaAnswer`
- **Response**: `ApiResponse<CreateQnaAnswerResponse>`
- **권한**: `@PreAuthorize(HAS_ANY_AUTHORITY_MASTER_SELLER)`
- **설명**: QnA 답변 등록

**Request 구조** (`CreateQnaAnswer`):
```java
public class CreateQnaAnswer {
    @NotNull private long qnaId;                 // QnA ID (필수)
    @Valid private CreateQnaContents qnaContents; // 답변 내용
    @Size(max = 3) private List<CreateQnaImage> qnaImages; // 답변 이미지 (최대 3개)
}
```

**Response 구조** (`CreateQnaAnswerResponse`):
```java
public class CreateQnaAnswerResponse {
    private long qnaId;
    private long qnaAnswerId;
    private QnaType qnaType;
    private QnaStatus qnaStatus;
    private List<QnaImageDto> qnaImages;
}
```

---

#### 2. QnaController.updateReplyQna
- **HTTP**: `PUT /api/v1/qna/reply`
- **Request**: `@RequestBody UpdateQnaAnswer updateQnaAnswer`
- **Response**: `ApiResponse<CreateQnaAnswerResponse>`
- **권한**: `@PreAuthorize(HAS_ANY_AUTHORITY_MASTER_SELLER)`
- **설명**: QnA 답변 수정

**Request 구조** (`UpdateQnaAnswer`):
```java
public class UpdateQnaAnswer extends CreateQnaAnswer {
    @NotNull private Long qnaAnswerId;  // 답변 ID (필수)
    // + CreateQnaAnswer 상속 필드
}
```

---

#### 3. ExternalMallQnaController.interLockingOrders
- **HTTP**: `POST /api/v1/external/qna`
- **Request**: `@RequestBody T extends ExMallQna`
- **Response**: `ApiResponse<ExternalQna>`
- **권한**: `@PreAuthorize(HAS_AUTHORITY_MASTER)` (마스터 전용)
- **설명**: 외부몰 QnA 연동 (동기화)

**특이사항**:
- Generic 타입 사용 (`T extends ExMallQna`)
- 외부몰 데이터 연동용 내부 API
- 마스터 권한만 접근 가능

---

## 📋 Enum 정의

### QnaType
QnA 유형 구분 (상품문의 / 주문문의 등)

### QnaStatus
QnA 상태 (대기중 / 답변완료 등)

### QnaDetailType
QnA 상세 유형

---

## 🔐 권한 정보

| Controller | 권한 |
|------------|------|
| QnaController | `HAS_ANY_AUTHORITY_MASTER_SELLER` (마스터 또는 셀러) |
| ExternalMallQnaController | `HAS_AUTHORITY_MASTER` (마스터 전용) |

---

## 🔗 다음 단계

### Query 엔드포인트 분석:
```bash
/legacy-flow admin:QnaController.fetchQna
/legacy-flow admin:QnaController.fetchQnas
```

### DTO 변환:
```bash
/legacy-convert admin:QnaController.fetchQna
/legacy-convert admin:QnaController.fetchQnas
```

### Command 엔드포인트 분석:
```bash
/legacy-flow admin:QnaController.replyQna
/legacy-flow admin:QnaController.updateReplyQna
```

---

## 📁 관련 파일 경로

### Controller
- `bootstrap-legacy-web-api-admin/.../module/qna/controller/QnaController.java`
- `bootstrap-legacy-web-api-admin/.../module/external/controller/ExternalMallQnaController.java`

### DTO
- `...module/qna/dto/filter/QnaFilter.java`
- `...module/qna/dto/query/CreateQnaAnswer.java`
- `...module/qna/dto/query/UpdateQnaAnswer.java`
- `...module/qna/dto/fetch/DetailQnaResponse.java`
- `...module/qna/dto/fetch/FetchQnaResponse.java`
- `...module/qna/dto/CreateQnaAnswerResponse.java`

### Service
- `...module/qna/service/QnaFetchService.java`
- `...module/qna/service/QnaAnswerRegistrationService.java`
- `...module/external/service/qna/ExternalQnaRegistrationService.java`
