# 엔드포인트 목록: Auth 모듈

> Generated by `/legacy-endpoints` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin

---

## 📊 요약

| 분류 | 개수 |
|------|------|
| Query (조회) | 3개 |
| Command (명령) | 4개 |
| **총계** | **7개** |

---

## 📖 Query (조회성) - 3개

| # | Controller | Method | HTTP | Path | Request Type |
|---|------------|--------|------|------|--------------|
| 1 | AuthController | getAdmins | GET | /api/v1/auth | Pageable |
| 2 | AuthController | getAdminValidation | GET | /api/v1/auth/admin-validation | @ModelAttribute |
| 3 | AuthController | getAdminsBySellerId | GET | /api/v1/auth/{sellerId} | @PathVariable + Pageable |

### 상세 목록

#### 1. AuthController.getAdmins
- **HTTP**: `GET /api/v1/auth`
- **Request**: `Pageable`
- **Response**: `ApiResponse<CustomPageable<AdministratorResponse>>`
- **설명**: 관리자 목록 페이징 조회

**Response DTO 구조**:
```java
AdministratorResponse {
    long id;
    String email;
    String fullName;
    String phoneNumber;
    long sellerId;
    String sellerName;
    ApprovalStatus approvalStatus;
}
```

---

#### 2. AuthController.getAdminValidation
- **HTTP**: `GET /api/v1/auth/admin-validation`
- **Request**: `@ModelAttribute AdminValidation`
- **Response**: `ApiResponse<Boolean>`
- **설명**: 관리자 이메일 중복 검증

**Request DTO 구조**:
```java
AdminValidation {
    String email;
}
```

---

#### 3. AuthController.getAdminsBySellerId
- **HTTP**: `GET /api/v1/auth/{sellerId}`
- **Request**: `@PathVariable long sellerId` + `Pageable`
- **Response**: `ApiResponse<CustomPageable<AdministratorResponse>>`
- **설명**: 특정 셀러 소속 관리자 목록 조회

---

## ✏️ Command (커맨드성) - 4개

| # | Controller | Method | HTTP | Path | Request Type |
|---|------------|--------|------|------|--------------|
| 1 | AuthController | getAccessToken | POST | /api/v1/auth/authentication | @RequestBody |
| 2 | AuthController | insertUser | POST | /api/v1/auth | @RequestBody |
| 3 | AuthController | getAccessToken (update) | PUT | /api/v1/auth/{authId} | @PathVariable + @RequestBody |
| 4 | AuthController | updateApprovalStatus | PUT | /api/v1/auth/approval-status | @RequestBody |

### 상세 목록

#### 1. AuthController.getAccessToken (인증)
- **HTTP**: `POST /api/v1/auth/authentication`
- **Request**: `@RequestBody @Validated CreateAuthToken`
- **Response**: `ApiResponse<AuthTokenResponse>`
- **설명**: 인증 토큰 발급 (로그인)

**Request DTO 구조**:
```java
CreateAuthToken (record) {
    @NotBlank String userId;      // 최대 100자
    @NotBlank String password;    // 최대 20자
    @NotNull RoleType roleType;
}
```

**Response DTO 구조**:
```java
AuthTokenResponse (record) {
    String token;
}
```

---

#### 2. AuthController.insertUser
- **HTTP**: `POST /api/v1/auth`
- **Request**: `@RequestBody @Validated AdministratorsInsertRequestDto`
- **Response**: `ApiResponse<Long>`
- **설명**: 관리자 계정 생성

**Request DTO 구조**:
```java
AdministratorsInsertRequestDto {
    RoleType roleType;
    String passwordHash;
    String email;
    String fullName;
    String phoneNumber;
    long sellerId;
}
```

---

#### 3. AuthController.getAccessToken (수정)
- **HTTP**: `PUT /api/v1/auth/{authId}`
- **Request**: `@PathVariable long authId` + `@RequestBody AdministratorsInsertRequestDto`
- **Response**: `ApiResponse<Long>`
- **설명**: 관리자 정보 수정

---

#### 4. AuthController.updateApprovalStatus
- **HTTP**: `PUT /api/v1/auth/approval-status`
- **Request**: `@RequestBody AdministratorsApprovalStatusRequestDto`
- **Response**: `ApiResponse<List<Long>>`
- **설명**: 관리자 승인 상태 일괄 변경

**Request DTO 구조**:
```java
AdministratorsApprovalStatusRequestDto (record) {
    List<Long> adminIds;
    ApprovalStatus approvalStatus;
}
```

---

## 📂 관련 Enum

### RoleType
- 관리자 역할 유형 (인증 요청 시 필수)

### ApprovalStatus
- 관리자 승인 상태 (PENDING, APPROVED, REJECTED 등)

---

## 🔗 다음 단계

각 엔드포인트 분석:
- `/legacy-flow admin:AuthController.getAdmins`
- `/legacy-flow admin:AuthController.getAccessToken`

DTO 변환:
- `/legacy-convert admin:AuthController.getAdmins`
- `/legacy-convert admin:AuthController.insertUser`
