# 엔드포인트 목록: Brand 모듈 (Admin API)

> Generated by `/legacy-endpoints` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin
> Package: `com.connectly.partnerAdmin.module.brand`

---

## 📊 요약

| 분류 | 개수 |
|------|------|
| Query (조회) | 1개 |
| Command (명령) | 1개 |
| **총계** | **2개** |

---

## 📖 Query (조회성) - 1개

| # | Controller | Method | HTTP | Path | Request Type |
|---|------------|--------|------|------|--------------|
| 1 | BrandController | fetchBrands | GET | /api/v1/brands | @ModelAttribute + Pageable |

### 상세 목록

#### 1. BrandController.fetchBrands

- **HTTP**: `GET /api/v1/brands`
- **권한**: `@PreAuthorize(HAS_ANY_AUTHORITY_MASTER_SELLER)`
- **Request Parameters**:
  ```java
  @ModelAttribute BrandFilter filter
  - lastDomainId: Long (No-Offset 페이징용)
  - siteId: Long
  - brandName: String
  - brandNames: List<String>
  - mainDisplayType: MainDisplayNameType (US | KR)

  Pageable pageable
  - page, size, sort
  ```
- **Response**: `ApiResponse<CustomPageable<ExtendedBrandContext>>`
  ```java
  ExtendedBrandContext {
    // from BaseBrandContext
    brandId: long
    brandName: String
    // extended fields
    mainDisplayType: MainDisplayNameType
    displayEnglishName: String
    displayKoreanName: String
  }
  ```
- **Service**: `BrandFetchService.fetchBrands(filter, pageable)`
- **설명**: 브랜드 목록 조회 (필터링 + 페이징 지원)

---

## ✏️ Command (커맨드성) - 1개

| # | Controller | Method | HTTP | Path | Request Type |
|---|------------|--------|------|------|--------------|
| 1 | BrandController | convertExternalBrandToInternalBrand | POST | /api/v1/brand/external/{siteId}/mapping | @PathVariable + @RequestBody |

### 상세 목록

#### 1. BrandController.convertExternalBrandToInternalBrand

- **HTTP**: `POST /api/v1/brand/external/{siteId}/mapping`
- **권한**: `@PreAuthorize(HAS_ANY_AUTHORITY_MASTER_SELLER)`
- **Path Variable**:
  ```java
  @PathVariable("siteId") long siteId
  ```
- **Request Body**:
  ```java
  @RequestBody List<BrandMappingInfo> brandMappingInfos

  BrandMappingInfo {
    @JsonProperty("mapping_brand_id")
    brandMappingId: String

    @JsonProperty("brand_name")
    brandName: String

    @JsonProperty("brand_id")
    brandId: Long (응답 시 설정됨)
  }
  ```
- **Response**: `ApiResponse<List<BrandMappingInfo>>`
- **Service**: `BrandMappingManager.convertExternalBrandToInternalBrand(siteId, brandMappingInfos)`
- **설명**: 외부 브랜드를 내부 브랜드로 매핑 변환

---

## 🔗 연관 컴포넌트

### Controller
- `BrandController` - 브랜드 API 컨트롤러

### Service
- `BrandFetchService` - 브랜드 조회 서비스
- `BrandMappingManager` - 브랜드 매핑 관리자

### DTO / Context
- `BrandFilter` - 브랜드 필터 (조회 조건)
- `ExtendedBrandContext` - 확장 브랜드 컨텍스트 (응답)
- `BaseBrandContext` - 기본 브랜드 컨텍스트
- `BrandMappingInfo` - 브랜드 매핑 정보

### Enum
- `MainDisplayNameType` - 메인 표시 이름 타입 (US, KR)

---

## 📂 파일 위치

```
bootstrap/bootstrap-legacy-web-api-admin/src/main/java/com/connectly/partnerAdmin/module/brand/
├── controller/
│   └── BrandController.java
├── core/
│   ├── BaseBrandContext.java
│   ├── BrandContext.java
│   ├── BrandMappingInfo.java
│   └── ExtendedBrandContext.java
├── enums/
│   └── MainDisplayNameType.java
├── filter/
│   └── BrandFilter.java
├── service/
│   ├── BrandFetchService.java
│   └── BrandMappingManager.java
└── repository/
    └── BrandFetchRepository.java
```

---

## 🔗 다음 단계

### Query 엔드포인트 분석
```bash
/legacy-flow admin:BrandController.fetchBrands
```

### Command 엔드포인트 분석
```bash
/legacy-flow admin:BrandController.convertExternalBrandToInternalBrand
```

### DTO 변환
```bash
/legacy-convert admin:BrandController.fetchBrands
/legacy-convert admin:BrandController.convertExternalBrandToInternalBrand
```
