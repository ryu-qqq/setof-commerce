# 엔드포인트 목록: Category 모듈 (Admin)

> Generated by `/legacy-endpoints` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin
> Controller: `com.connectly.partnerAdmin.module.category.controller.CategoryController`

---

## 📊 요약

| 분류 | 개수 |
|------|------|
| Query (조회) | 5개 |
| Command (명령) | 1개 |
| **총계** | **6개** |

---

## 📖 Query (조회성) - 5개

| # | Controller | Method | HTTP | Path | Request Type |
|---|------------|--------|------|------|--------------|
| 1 | CategoryController | fetchAllChildCategories | GET | /api/v1/category/{categoryId} | @PathVariable |
| 2 | CategoryController | fetchAllParentCategories | GET | /api/v1/category/parent/{categoryId} | @PathVariable |
| 3 | CategoryController | fetchAllParentCategories | GET | /api/v1/category/parents | @RequestParam |
| 4 | CategoryController | fetchAllCategoriesAsTree | GET | /api/v1/category | - |
| 5 | CategoryController | fetchCategories | GET | /api/v1/category/page | @ModelAttribute + Pageable |

### 상세 목록

#### 1. CategoryController.fetchAllChildCategories
- **HTTP**: `GET /api/v1/category/{categoryId}`
- **Request**: `@PathVariable("categoryId") long categoryId`
- **Response**: `ApiResponse<List<TreeCategoryContext>>`
- **설명**: 특정 카테고리의 모든 하위 카테고리 트리 조회
- **인증**: `@PreAuthorize(HAS_ANY_AUTHORITY_MASTER_SELLER)`

**TreeCategoryContext 응답 필드**:
| 필드 | 타입 | 설명 |
|------|------|------|
| categoryId | long | 카테고리 ID |
| categoryName | String | 카테고리명 |
| displayName | String | 노출명 |
| categoryDepth | int | 카테고리 depth |
| parentCategoryId | long | 부모 카테고리 ID |
| children | List<TreeCategoryContext> | 하위 카테고리 목록 (재귀) |

---

#### 2. CategoryController.fetchAllParentCategories
- **HTTP**: `GET /api/v1/category/parent/{categoryId}`
- **Request**: `@PathVariable("categoryId") long categoryId`
- **Response**: `ApiResponse<List<TreeCategoryContext>>`
- **설명**: 특정 카테고리의 모든 상위 카테고리 조회 (조상 경로)
- **인증**: `@PreAuthorize(HAS_ANY_AUTHORITY_MASTER_SELLER)`

---

#### 3. CategoryController.fetchAllParentCategories (다건)
- **HTTP**: `GET /api/v1/category/parents`
- **Request**: `@RequestParam List<Long> categoryIds`
- **Response**: `ApiResponse<List<TreeCategoryContext>>`
- **설명**: 여러 카테고리 ID에 대한 상위 카테고리 목록 일괄 조회
- **인증**: `@PreAuthorize(HAS_ANY_AUTHORITY_MASTER_SELLER)`

---

#### 4. CategoryController.fetchAllCategoriesAsTree
- **HTTP**: `GET /api/v1/category`
- **Request**: 없음
- **Response**: `ApiResponse<List<TreeCategoryContext>>`
- **설명**: 전체 카테고리를 트리 구조로 조회
- **인증**: `@PreAuthorize(HAS_ANY_AUTHORITY_MASTER_SELLER)`

---

#### 5. CategoryController.fetchCategories
- **HTTP**: `GET /api/v1/category/page`
- **Request**: `@ModelAttribute CategoryFilter` + `Pageable`
- **Response**: `ApiResponse<CustomPageable<ProductCategoryContext>>`
- **설명**: 카테고리 페이징 조회 (검색 필터 지원)
- **인증**: `@PreAuthorize(HAS_ANY_AUTHORITY_MASTER_SELLER)`

**CategoryFilter 요청 필드**:
| 필드 | 타입 | 설명 |
|------|------|------|
| lastDomainId | Long | No-Offset 페이징용 마지막 ID |
| categoryId | Long | 카테고리 ID 필터 |
| categoryName | String | 카테고리명 검색 |
| categoryDepth | Integer | 카테고리 depth 필터 |

**ProductCategoryContext 응답 필드**:
| 필드 | 타입 | 설명 |
|------|------|------|
| categoryId | long | 카테고리 ID |
| categoryName | String | 카테고리명 |
| displayName | String | 노출명 |
| categoryDepth | int | 카테고리 depth |
| categoryFullPath | String | 카테고리 전체 경로 |
| targetGroup | TargetGroup | 대상 그룹 (enum) |

---

## ✏️ Command (커맨드성) - 1개

| # | Controller | Method | HTTP | Path | Request Type |
|---|------------|--------|------|------|--------------|
| 1 | CategoryController | convertExternalCategoryToInternalCategory | POST | /api/v1/category/external/{siteId}/mapping | @PathVariable + @RequestBody |

### 상세 목록

#### 1. CategoryController.convertExternalCategoryToInternalCategory
- **HTTP**: `POST /api/v1/category/external/{siteId}/mapping`
- **Request**:
  - `@PathVariable("siteId") long siteId`
  - `@RequestBody List<CategoryMappingInfo> categoryMappingInfos`
- **Response**: `ApiResponse<List<CategoryMappingInfo>>`
- **설명**: 외부 카테고리를 내부 카테고리로 매핑/변환
- **인증**: `@PreAuthorize(HAS_ANY_AUTHORITY_MASTER_SELLER)`

**CategoryMappingInfo 요청/응답 필드**:
| 필드 | JSON Key | 타입 | 설명 |
|------|----------|------|------|
| categoryMappingId | mapping_category_id | String | 외부 카테고리 매핑 ID |
| categoryName | category_name | String | 카테고리명 |
| categoryId | category_id | Long | 내부 카테고리 ID (변환 후 설정됨) |

---

## 🏗️ 의존성 구조

```
CategoryController
├── CategoryFetchService (조회)
│   ├── fetchAllChildCategories(categoryId)
│   ├── fetchAllParentCategories(categoryId)
│   ├── fetchAllCategories(Set<Long> categoryIds)
│   ├── fetchAllCategories()
│   └── fetchCategories(filter, pageable)
│
└── CategoryMappingManager (명령)
    └── convertExternalCategoryToInternalCategory(siteId, mappingInfos)
```

---

## 🔗 다음 단계

### 개별 엔드포인트 흐름 분석
```bash
/legacy-flow admin:CategoryController.fetchAllCategoriesAsTree
/legacy-flow admin:CategoryController.fetchCategories
/legacy-flow admin:CategoryController.fetchAllChildCategories
```

### DTO 변환
```bash
/legacy-convert admin:CategoryController.fetchCategories
```

---

## 📝 마이그레이션 노트

### Query 엔드포인트 우선순위

| 순위 | 메서드 | 이유 |
|------|--------|------|
| 1 | fetchAllCategoriesAsTree | 전체 카테고리 트리 - 핵심 데이터 |
| 2 | fetchCategories | 페이징 조회 - 검색/목록 기능 |
| 3 | fetchAllChildCategories | 하위 카테고리 탐색 |
| 4 | fetchAllParentCategories | 상위 카테고리 경로 |
| 5 | fetchAllParentCategories (다건) | 일괄 조회 - 성능 최적화용 |

### Command 엔드포인트 특이사항

- `convertExternalCategoryToInternalCategory`: 외부 몰 연동 전용 기능
  - 단순 CRUD가 아닌 매핑/변환 로직 포함
  - 외부 시스템(siteId) 의존성 있음
