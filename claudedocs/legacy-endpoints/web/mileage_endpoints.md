# 엔드포인트 목록: mileage 모듈

> Generated by `/legacy-endpoints` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api

---

## 📊 요약

| 분류 | 개수 |
|------|------|
| Query (조회) | 1개 |
| Command (명령) | 0개 |
| **총계** | **1개** |

---

## 📖 Query (조회성) - 1개

| # | Controller | Method | HTTP | Path | Request Type |
|---|------------|--------|------|------|--------------|
| 1 | MileageController | fetchMileageHistories | GET | /api/v1/mileage/my-page/mileage-histories | @ModelAttribute + Pageable |

### 상세 목록

#### 1. MileageController.fetchMileageHistories

- **HTTP**: `GET /api/v1/mileage/my-page/mileage-histories`
- **권한**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **Request**:
  - `@ModelAttribute MileageFilter filter`
  - `Pageable pageable`
- **Response**: `ResponseEntity<ApiResponse<MileagePage<UserMileageHistoryDto>>>`
- **설명**: 마이페이지 마일리지 이력 조회

##### Request DTO: MileageFilter

```java
@Getter
@NoArgsConstructor(access = AccessLevel.PROTECTED)
@AllArgsConstructor
public class MileageFilter {
    private Long lastDomainId;        // 마지막 조회 ID (커서 기반 페이징)
    private List<Reason> reasons;     // 마일리지 사유 필터 (SAVE, USE, REFUND, EXPIRED)
}
```

##### Response DTO: MileagePage<UserMileageHistoryDto>

```java
// MileagePage - Page 인터페이스 구현체
@Builder
@AllArgsConstructor
@Getter
public class MileagePage<T> implements Page<T> {
    private UserMileageDto userMileage;   // 사용자 마일리지 요약 정보
    private final List<T> content;         // 이력 목록
    private final Pageable pageable;
    private final boolean last;
    private final int totalPages;
    private final long totalElements;
    private final boolean first;
    private final int number;
    private final Sort sort;
    private final int size;
    private final int numberOfElements;
    private final boolean empty;
    private final Long lastDomainId;       // 다음 페이지 조회용 마지막 ID
}

// UserMileageDto - 사용자 마일리지 요약
@Getter
public class UserMileageDto {
    private long userId;
    private double currentMileage;         // 현재 마일리지
    private double expectedSaveMileage;    // 적립 예정 마일리지
    private double expectedExpireMileage;  // 소멸 예정 마일리지
}

// UserMileageHistoryDto - 마일리지 이력 상세
@Getter
public class UserMileageHistoryDto {
    private long mileageId;
    private String title;                  // 이력 제목
    private long paymentId;
    private long orderId;
    private double changeAmount;           // 변동 금액
    private Reason reason;                 // 사유 (SAVE, USE, REFUND, EXPIRED)
    private LocalDateTime usedDate;        // 사용/적립 일시
    private LocalDateTime expirationDate;  // 만료일
}
```

##### Reason Enum

```java
public enum Reason {
    SAVE,     // 적립
    USE,      // 사용
    REFUND,   // 환불
    EXPIRED;  // 만료
}
```

---

## ✏️ Command (커맨드성) - 0개

해당 없음

---

## 🔗 다음 단계

### 엔드포인트 상세 흐름 분석
```bash
/legacy-flow web:MileageController.fetchMileageHistories
```

### DTO 변환
```bash
/legacy-convert web:MileageController.fetchMileageHistories
```

---

## 📝 참고사항

### 컨트롤러 특징
- 단일 조회 엔드포인트만 존재
- 인증 필수 (`NORMAL_GRADE` 권한)
- 커서 기반 페이징 지원 (`lastDomainId`)
- 복합 Response: 마일리지 요약 + 이력 목록을 함께 반환

### 관련 파일 위치
```
bootstrap-legacy-web-api/src/main/java/com/setof/connectly/module/
├── mileage/
│   ├── controller/MileageController.java
│   ├── dto/
│   │   ├── filter/MileageFilter.java
│   │   └── page/MileagePage.java
│   └── enums/Reason.java
└── user/
    ├── dto/mileage/
    │   ├── UserMileageDto.java
    │   └── UserMileageHistoryDto.java
    └── service/mileage/UserMileageFindService.java
```
