# 엔드포인트 목록: QNA 모듈

> Generated by `/legacy-endpoints` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api

---

## 📊 요약

| 분류 | 개수 |
|------|------|
| Query (조회) | 2개 |
| Command (명령) | 4개 |
| **총계** | **6개** |

---

## 📖 Query (조회성) - 2개

| # | Controller | Method | HTTP | Path | Request Type | Auth |
|---|------------|--------|------|------|--------------|------|
| 1 | QnaController | fetchProductQnas | GET | /api/v1/qna/product/{productGroupId} | @PathVariable + Pageable | - |
| 2 | QnaController | fetchMyQnas | GET | /api/v1/qna/my-page | @ModelAttribute + Pageable | NORMAL_GRADE |

### 상세 목록

#### 1. QnaController.fetchProductQnas
- **HTTP**: `GET /api/v1/qna/product/{productGroupId}`
- **Request**: `@PathVariable("productGroupId") long productGroupId`, `Pageable pageable`
- **Response**: `ApiResponse<Page<QnaResponse>>`
- **인증**: 없음 (공개 API)
- **설명**: 특정 상품그룹의 Q&A 목록 조회 (페이징)

#### 2. QnaController.fetchMyQnas
- **HTTP**: `GET /api/v1/qna/my-page`
- **Request**: `@ModelAttribute QnaFilter qnaFilter`, `Pageable pageable`
- **Response**: `ApiResponse<CustomSlice<QnaResponse>>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: 내 Q&A 목록 조회 (마이페이지, 필터 + 페이징)

---

## ✏️ Command (커맨드성) - 4개

| # | Controller | Method | HTTP | Path | Request Type | Auth |
|---|------------|--------|------|------|--------------|------|
| 1 | QnaController | doQuestion | POST | /api/v1/qna | @RequestBody | NORMAL_GRADE |
| 2 | QnaController | updateQuestion | PUT | /api/v1/qna/{qnaId} | @RequestBody + @PathVariable | NORMAL_GRADE |
| 3 | QnaController | doReply | POST | /api/v1/qna/{qnaId}/reply | @RequestBody + @PathVariable | NORMAL_GRADE |
| 4 | QnaController | updateReply | PUT | /api/v1/qna/{qnaId}/reply/{qnaAnswerId} | @RequestBody + @PathVariable | NORMAL_GRADE |

### 상세 목록

#### 1. QnaController.doQuestion
- **HTTP**: `POST /api/v1/qna`
- **Request**: `@RequestBody @Validated CreateQna createQna`
- **Response**: `ApiResponse<Qna>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: Q&A 질문 등록

#### 2. QnaController.updateQuestion
- **HTTP**: `PUT /api/v1/qna/{qnaId}`
- **Request**: `@PathVariable("qnaId") long qnaId`, `@RequestBody @Validated CreateQna createQna`
- **Response**: `ApiResponse<Qna>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: Q&A 질문 수정

#### 3. QnaController.doReply
- **HTTP**: `POST /api/v1/qna/{qnaId}/reply`
- **Request**: `@PathVariable("qnaId") long qnaId`, `@RequestBody @Validated CreateQna createQna`
- **Response**: `ApiResponse<QnaAnswer>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: Q&A 답변(댓글) 등록

#### 4. QnaController.updateReply
- **HTTP**: `PUT /api/v1/qna/{qnaId}/reply/{qnaAnswerId}`
- **Request**: `@PathVariable("qnaId") long qnaId`, `@PathVariable("qnaAnswerId") long qnaAnswerId`, `@RequestBody @Validated CreateQna createQna`
- **Response**: `ApiResponse<QnaAnswer>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: Q&A 답변(댓글) 수정

---

## 📋 관련 DTO 목록

### Request DTOs
| DTO | 용도 | 위치 |
|-----|------|------|
| `CreateQna` | Q&A 생성/수정 요청 | `dto/query/CreateQna.java` |
| `QnaFilter` | Q&A 필터 조건 | `dto/filter/QnaFilter.java` |

### Response DTOs
| DTO | 용도 | 위치 |
|-----|------|------|
| `QnaResponse` | Q&A 응답 | `dto/QnaResponse.java` |
| `Qna` | Q&A 엔티티 (직접 반환) | `entity/Qna.java` |
| `QnaAnswer` | Q&A 답변 엔티티 (직접 반환) | `entity/QnaAnswer.java` |

---

## ⚠️ 마이그레이션 시 주의사항

1. **엔티티 직접 반환**: `Qna`, `QnaAnswer` 엔티티를 Response로 직접 반환하고 있음
   - 새 아키텍처에서는 Response DTO로 변환 필요

2. **CreateQna 재사용**: 질문/답변 생성/수정에 동일한 DTO 사용
   - 용도별 DTO 분리 검토 필요

3. **인증 체계**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")` 사용
   - 새 아키텍처의 인증 체계와 매핑 필요

---

## 🔗 다음 단계

각 엔드포인트 분석:
```bash
/legacy-flow web:QnaController.fetchProductQnas
/legacy-flow web:QnaController.fetchMyQnas
/legacy-flow web:QnaController.doQuestion
```

DTO 변환:
```bash
/legacy-convert web:QnaController.fetchProductQnas
/legacy-convert web:QnaController.fetchMyQnas
```
