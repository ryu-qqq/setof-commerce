# 엔드포인트 목록: Cart 모듈

> Generated by `/legacy-endpoints` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api

---

## 📊 요약

| 분류 | 개수 |
|------|------|
| Query (조회) | 2개 |
| Command (명령) | 3개 |
| **총계** | **5개** |

**Base Path**: `/api/v1`
**Security**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`

---

## 📖 Query (조회성) - 2개

| # | Controller | Method | HTTP | Path | Request Type |
|---|------------|--------|------|------|--------------|
| 1 | CartController | fetchCartCount | GET | /api/v1/cart-count | - (SecurityContext) |
| 2 | CartController | getCarts | GET | /api/v1/carts | @ModelAttribute + Pageable |

### 상세 목록

#### 1. CartController.fetchCartCount
- **HTTP**: `GET /api/v1/cart-count`
- **Request**: 없음 (SecurityUtils.currentUserId()로 사용자 식별)
- **Response**: `ApiResponse<CartCountDto>`
- **설명**: 현재 로그인한 사용자의 장바구니 아이템 개수 조회

**Response DTO 구조**:
```java
public class CartCountDto {
    private long userId;
    private long cartQuantity;
}
```

---

#### 2. CartController.getCarts
- **HTTP**: `GET /api/v1/carts`
- **Request**: `@ModelAttribute CartFilter` + `Pageable`
- **Response**: `ApiResponse<CustomSlice<CartResponse>>`
- **설명**: 장바구니 목록 조회 (커서 기반 페이징)

**Request DTO 구조**:
```java
public class CartFilter extends AbstractLastDomainIdFilter {
    private Long lastDomainId;    // 커서 페이징용
    private String cursorValue;   // 커서 값
    private OrderType orderType;  // 정렬 타입
}
```

**Response DTO 구조**:
```java
public class CartResponse implements LastDomainIdProvider, DiscountOffer {
    private long brandId;
    private String brandName;
    private String productGroupName;
    private long sellerId;
    private String sellerName;
    private long productId;
    private Price price;
    private int quantity;
    private int stockQuantity;
    private String optionValue;
    private long cartId;
    private String imageUrl;
    private long productGroupId;
    private ProductStatus productStatus;
    private double mileageRate;
    private double expectedMileageAmount;
    private Set<ProductCategoryDto> categories;
}
```

---

## ✏️ Command (커맨드성) - 3개

| # | Controller | Method | HTTP | Path | Request Type |
|---|------------|--------|------|------|--------------|
| 1 | CartController | addToCart | POST | /api/v1/cart | @RequestBody @Validated |
| 2 | CartController | modifyCart | PUT | /api/v1/cart/{cartId} | @PathVariable + @RequestBody |
| 3 | CartController | deleteCarts | DELETE | /api/v1/carts | @ModelAttribute |

### 상세 목록

#### 1. CartController.addToCart
- **HTTP**: `POST /api/v1/cart`
- **Request**: `@RequestBody @Validated List<CartDetails>`
- **Response**: `ApiResponse<List<CartDetails>>`
- **설명**: 장바구니에 상품 추가 (이미 있으면 업데이트)

**Request DTO 구조**:
```java
@Embeddable
public class CartDetails {
    @NotNull(message = "상품 그룹 아이디는 필수 입니다.")
    private long productGroupId;

    @NotNull(message = "상품 아이디는 필수 입니다.")
    private long productId;

    @Min(value = 1, message = "재고의 수량은 0 보다 커야합니다.")
    @Max(value = 999, message = "재고의 수량은 999 보다 작아야 합니다.")
    private int quantity;

    @NotNull(message = "판매자 아이디는 필수 입니다.")
    private long sellerId;
}
```

---

#### 2. CartController.modifyCart
- **HTTP**: `PUT /api/v1/cart/{cartId}`
- **Request**: `@PathVariable("cartId") long cartId` + `@RequestBody @Validated CartDetails`
- **Response**: `ApiResponse<CartDetails>`
- **설명**: 장바구니 아이템 수정 (수량 변경 등)

---

#### 3. CartController.deleteCarts
- **HTTP**: `DELETE /api/v1/carts`
- **Request**: `@ModelAttribute CartDeleteRequestDto`
- **Response**: `ApiResponse<Integer>`
- **설명**: 장바구니 아이템 삭제

**Request DTO 구조**:
```java
public record CartDeleteRequestDto(long cartId) {}
```

---

## 🏗️ 아키텍처 분석

### 의존성 구조
```
CartController
├── CartFindService (조회 전용)
│   └── fetchCartCountQuery()
│   └── fetchCartList()
└── CartQueryService (명령 처리)
    └── insertOrUpdate()
    └── updateCart()
    └── delete()
```

### 특이사항
1. **Query/Command 분리**: `CartFindService`(조회)와 `CartQueryService`(명령)가 분리됨
2. **Embeddable 사용**: `CartDetails`가 JPA Embeddable로 정의되어 Request DTO로도 사용됨
3. **커서 기반 페이징**: `AbstractLastDomainIdFilter`를 상속받아 커서 페이징 지원
4. **DiscountOffer 인터페이스**: `CartResponse`가 할인 적용 대상임을 나타냄

---

## 🔗 다음 단계

각 엔드포인트 상세 분석:
```bash
/legacy-flow web:CartController.fetchCartCount
/legacy-flow web:CartController.getCarts
/legacy-flow web:CartController.addToCart
/legacy-flow web:CartController.modifyCart
/legacy-flow web:CartController.deleteCarts
```

DTO 변환:
```bash
/legacy-convert web:CartController.getCarts
/legacy-convert web:CartController.addToCart
```
