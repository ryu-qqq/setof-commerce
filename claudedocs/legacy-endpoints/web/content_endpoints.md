# 엔드포인트 목록: Content 모듈

> Generated by `/legacy-endpoints` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api

---

## 📊 요약

| 분류 | 개수 |
|------|------|
| Query (조회) | 6개 |
| Command (명령) | 0개 |
| **총계** | **6개** |

---

## 📖 Query (조회성) - 6개

| # | Controller | Method | HTTP | Path | Request Type |
|---|------------|--------|------|------|--------------|
| 1 | ContentController | fetchOnDisplayContents | GET | /api/v1/content/on-display | - |
| 2 | ContentController | fetchContentMetaData | GET | /api/v1/content/meta/{contentId} | @PathVariable |
| 3 | ContentController | fetchContent | GET | /api/v1/content/{contentId} | @PathVariable, @RequestParam |
| 4 | ContentController | fetchContentProducts | GET | /api/v1/content/component/{componentId}/products | @PathVariable, @ModelAttribute, Pageable |
| 5 | ContentController | getBannerGroups | GET | /api/v1/content/banner | @ModelAttribute |
| 6 | ContentController | getGnbs | GET | /api/v1/content/gnbs | - |

### 상세 목록

#### 1. ContentController.fetchOnDisplayContents
- **HTTP**: `GET /api/v1/content/on-display`
- **Request**: 없음
- **Response**: `ApiResponse<OnDisplayContent>`
- **설명**: 현재 전시 중인 콘텐츠 ID 목록 조회

**Response DTO**:
```java
public class OnDisplayContent {
    private HashSet<Long> contentIds;
}
```

---

#### 2. ContentController.fetchContentMetaData
- **HTTP**: `GET /api/v1/content/meta/{contentId}`
- **Request**: `@PathVariable("contentId") long contentId`
- **Response**: `ApiResponse<ContentGroupResponse>`
- **설명**: 콘텐츠 메타데이터만 조회 (컴포넌트 상세 제외)

**Response DTO**:
```java
public class ContentGroupResponse {
    private long contentId;
    private DisplayPeriod displayPeriod;
    private String title;
    private String memo;
    private String imageUrl;
    private List<ComponentDetailResponse> componentDetails;
}
```

---

#### 3. ContentController.fetchContent
- **HTTP**: `GET /api/v1/content/{contentId}`
- **Request**:
  - `@PathVariable("contentId") long contentId`
  - `@RequestParam(value = "bypass", required = false) Yn bypass`
- **Response**: `ApiResponse<ContentGroupResponse>`
- **설명**: 콘텐츠 상세 조회 (컴포넌트 포함)

**Request 파라미터**:
| 파라미터 | 타입 | 필수 | 설명 |
|----------|------|------|------|
| contentId | long | ✅ | 콘텐츠 ID (Path) |
| bypass | Yn | ❌ | 전시 기간 체크 우회 여부 |

---

#### 4. ContentController.fetchContentProducts
- **HTTP**: `GET /api/v1/content/component/{componentId}/products`
- **Request**:
  - `@PathVariable("componentId") long componentId`
  - `@ModelAttribute ComponentFilter filter`
  - `Pageable pageable`
- **Response**: `ApiResponse<CustomSlice<ProductGroupThumbnail>>`
- **설명**: 컴포넌트에 포함된 상품그룹 목록 조회 (페이징)

**Request 파라미터**:
| 파라미터 | 타입 | 필수 | 설명 |
|----------|------|------|------|
| componentId | long | ✅ | 컴포넌트 ID (Path) |
| componentType | ComponentType | ❌ | 컴포넌트 타입 (Filter) |
| tabId | Long | ❌ | 탭 ID (Filter) |
| exclusiveProductIds | List<Long> | ❌ | 제외할 상품 ID 목록 |

**Response DTO**:
```java
public class ProductGroupThumbnail {
    protected long productGroupId;
    protected long sellerId;
    protected String productGroupName;
    protected BrandDto brand;
    protected String productImageUrl;
    protected Price price;
    protected LocalDateTime insertDate;
    protected double averageRating;
    protected long reviewCount;
    protected double score;
    protected boolean isFavorite;
    protected ProductStatus productStatus;
}
```

---

#### 5. ContentController.getBannerGroups
- **HTTP**: `GET /api/v1/content/banner`
- **Request**: `@Validated @ModelAttribute BannerFilter bannerFilter`
- **Response**: `ApiResponse<List<BannerItemDto>>`
- **설명**: 배너 타입별 배너 목록 조회

**Request DTO**:
```java
public class BannerFilter {
    @NotNull(message = "bannerType 은 필수입니다.")
    private BannerType bannerType;
}
```

**Response DTO**:
```java
public class BannerItemDto {
    private long bannerItemId;
    private String title;
    private String imageUrl;
    private String linkUrl;
}
```

---

#### 6. ContentController.getGnbs
- **HTTP**: `GET /api/v1/content/gnbs`
- **Request**: 없음
- **Response**: `ApiResponse<List<GnbResponse>>`
- **설명**: GNB(Global Navigation Bar) 목록 조회

**Response DTO**:
```java
public class GnbResponse {
    private long gnbId;
    private String title;
    private String linkUrl;
}
```

---

## ✏️ Command (커맨드성) - 0개

| # | Controller | Method | HTTP | Path | Request Type |
|---|------------|--------|------|------|--------------|

> 이 컨트롤러에는 Command 엔드포인트가 없습니다.

---

## 📋 의존성 서비스

| Service | 용도 |
|---------|------|
| ContentFindService | 콘텐츠 조회 관련 |
| BannerFindService | 배너 조회 관련 |
| GnbFindService | GNB 조회 관련 |

---

## 🔗 다음 단계

각 엔드포인트 분석:
```bash
/legacy-flow web:ContentController.fetchOnDisplayContents
/legacy-flow web:ContentController.fetchContentMetaData
/legacy-flow web:ContentController.fetchContent
/legacy-flow web:ContentController.fetchContentProducts
/legacy-flow web:ContentController.getBannerGroups
/legacy-flow web:ContentController.getGnbs
```

DTO 변환:
```bash
/legacy-convert web:ContentController.fetchOnDisplayContents
/legacy-convert web:ContentController.fetchContent
/legacy-convert web:ContentController.fetchContentProducts
/legacy-convert web:ContentController.getBannerGroups
```
