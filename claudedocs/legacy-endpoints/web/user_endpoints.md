# 엔드포인트 목록: User 모듈

> Generated by `/legacy-endpoints` command
> Date: 2026-03-08
> Source: bootstrap-legacy-web-api

---

## 요약

| 분류 | 개수 |
|------|------|
| Query (조회) | 8개 |
| Command (명령) | 12개 |
| **총계** | **20개** |

> 분석 대상 Controller: `UserController`, `RedirectController`

---

## Query (조회성) - 8개

| # | Controller | Method | HTTP | Full Path | Request Type | 인증 |
|---|------------|--------|------|-----------|--------------|------|
| 1 | UserController | fetchUser | GET | /api/v1/user | - | NORMAL_GRADE |
| 2 | UserController | isExistUser | GET | /api/v1/user/exists | @ModelAttribute | 없음 |
| 3 | UserController | fetchAddressBook (목록) | GET | /api/v1/user/address-book | - | NORMAL_GRADE |
| 4 | UserController | fetchAddressBook (단건) | GET | /api/v1/user/address-book/{shippingAddressId} | @PathVariable | NORMAL_GRADE |
| 5 | UserController | fetchRefundAccount | GET | /api/v1/user/refund-account | - | NORMAL_GRADE |
| 6 | UserController | fetchMyPage | GET | /api/v1/user/my-page | - | NORMAL_GRADE |
| 7 | UserController | fetchMyFavorites | GET | /api/v1/user/my-favorites | @ModelAttribute + Pageable | NORMAL_GRADE |
| 8 | RedirectController | redirectHandler | GET | /redirect | @RequestParam | 없음 |

### 상세 목록

#### 1. UserController.fetchUser
- **HTTP**: `GET /api/v1/user`
- **Request**: 없음 (SecurityContext에서 사용자 식별)
- **Response**: `ApiResponse<JoinedUser>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: 현재 로그인한 사용자 정보 조회

#### 2. UserController.isExistUser
- **HTTP**: `GET /api/v1/user/exists`
- **Request**: `@ModelAttribute @Validated IsJoinedUser { phoneNumber: String }`
- **Response**: `ApiResponse<JoinedUser>`
- **인증**: 없음 (공개 API)
- **설명**: 전화번호 기반 사용자 존재 여부 확인 (회원가입 전 중복 체크)

#### 3. UserController.fetchAddressBook (목록)
- **HTTP**: `GET /api/v1/user/address-book`
- **Request**: 없음 (SecurityContext에서 사용자 식별)
- **Response**: `ApiResponse<List<UserShippingInfo>>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: 배송지 목록 전체 조회

#### 4. UserController.fetchAddressBook (단건)
- **HTTP**: `GET /api/v1/user/address-book/{shippingAddressId}`
- **Request**: `@PathVariable long shippingAddressId`
- **Response**: `ApiResponse<UserShippingInfo>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: 특정 배송지 단건 조회

#### 5. UserController.fetchRefundAccount
- **HTTP**: `GET /api/v1/user/refund-account`
- **Request**: 없음 (SecurityContext에서 사용자 식별)
- **Response**: `ApiResponse<RefundAccountInfo>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: 환불 계좌 정보 조회

#### 6. UserController.fetchMyPage
- **HTTP**: `GET /api/v1/user/my-page`
- **Request**: 없음 (SecurityContext에서 사용자 식별)
- **Response**: `ApiResponse<MyPageResponse>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: 마이페이지 종합 정보 조회 (이름, 전화번호, 이메일, 등급, 마일리지, 주문 현황 등)

#### 7. UserController.fetchMyFavorites
- **HTTP**: `GET /api/v1/user/my-favorites`
- **Request**: `@ModelAttribute @Validated MyFavoriteFilter { lastDomainId: Long }` + `Pageable`
- **Response**: `ApiResponse<CustomSlice<UserFavoriteThumbnail>>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: 찜한 상품 목록 커서 기반 페이징 조회

#### 8. RedirectController.redirectHandler
- **HTTP**: `GET /redirect`
- **Request**: `@RequestParam String token`
- **Response**: `ResponseEntity<String>`
- **인증**: 없음 (공개 API)
- **설명**: 소셜 로그인 등 외부 리다이렉트 처리 후 토큰 수신

---

## Command (커맨드성) - 12개

| # | Controller | Method | HTTP | Full Path | Request Type | 인증 |
|---|------------|--------|------|-----------|--------------|------|
| 1 | UserController | join | POST | /api/v1/user/join | @RequestBody | 없음 |
| 2 | UserController | login | POST | /api/v1/user/login | @RequestBody | 없음 |
| 3 | UserController | withdrawal | POST | /api/v1/user/withdrawl | @RequestBody | NORMAL_GRADE |
| 4 | UserController | login (resetPassword) | PATCH | /api/v1/user/password | @RequestBody | 없음 |
| 5 | UserController | createAddressBook | POST | /api/v1/user/address-book | @RequestBody | NORMAL_GRADE |
| 6 | UserController | updateAddressBook | PUT | /api/v1/user/address-book/{shippingAddressId} | @PathVariable + @RequestBody | NORMAL_GRADE |
| 7 | UserController | deleteAddressBook | DELETE | /api/v1/user/address-book/{shippingAddressId} | @PathVariable | NORMAL_GRADE |
| 8 | UserController | createRefundAccount | POST | /api/v1/user/refund-account | @RequestBody | NORMAL_GRADE |
| 9 | UserController | updateRefundAccount | PUT | /api/v1/user/refund-account/{refundAccountId} | @PathVariable + @RequestBody | NORMAL_GRADE |
| 10 | UserController | deleteRefundAccount | DELETE | /api/v1/user/refund-account/{refundAccountId} | @PathVariable | NORMAL_GRADE |
| 11 | UserController | createUserFavorite | POST | /api/v1/user/my-favorite | @RequestBody | NORMAL_GRADE |
| 12 | UserController | deleteUserFavorite | DELETE | /api/v1/user/my-favorite/{productGroupId} | @PathVariable | NORMAL_GRADE |

### 상세 목록

#### 1. UserController.join
- **HTTP**: `POST /api/v1/user/join`
- **Request**: `@RequestBody @Validated CreateUser`
  ```
  CreateUser extends LoginUser {
    phoneNumber: String  // 010XXXXXXXX 형식
    passwordHash: String // 영문+숫자+특수문자 8자 이상
    referer: String
    name: String         // 2~5자
    privacyConsent: Yn   // 개인정보 수집 동의
    serviceTermsConsent: Yn // 서비스 이용약관 동의
    adConsent: Yn        // 광고 수신 동의
  }
  ```
- **Response**: `ApiResponse<TokenDto { accessToken, refreshToken }>`
- **인증**: 없음 (공개 API)
- **설명**: 회원가입 (완료 시 JWT 토큰 발급)

#### 2. UserController.login
- **HTTP**: `POST /api/v1/user/login`
- **Request**: `@RequestBody @Validated LoginUser`
  ```
  LoginUser {
    phoneNumber: String  // 010XXXXXXXX 형식
    passwordHash: String // 영문+숫자+특수문자 8자 이상
    referer: String
  }
  ```
- **Response**: `ApiResponse<TokenDto>` (body는 null, 토큰은 Cookie로 전달)
- **인증**: 없음 (공개 API)
- **설명**: 로그인 (JWT 토큰을 HTTP-Only Cookie로 설정)

#### 3. UserController.withdrawal
- **HTTP**: `POST /api/v1/user/withdrawl`
- **Request**: `@RequestBody @Validated WithdrawalDto`
  ```
  WithdrawalDto {
    agreementYn: Yn
    reason: WithdrawalReason (NotNull)
  }
  ```
- **Response**: `ApiResponse<Users>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: 회원 탈퇴
- **비고**: URL typo 존재 (`withdrawl` → 올바른 영단어는 `withdrawal`)

#### 4. UserController.login (resetPassword)
- **HTTP**: `PATCH /api/v1/user/password`
- **Request**: `@RequestBody @Validated LoginUser`
  ```
  LoginUser {
    phoneNumber: String
    passwordHash: String  // 새 비밀번호
    referer: String
  }
  ```
- **Response**: `ApiResponse<UserDto>`
- **인증**: 없음 (공개 API)
- **설명**: 비밀번호 재설정
- **비고**: 메서드명이 `login`으로 잘못 지정됨, 실제 호출은 `userManageService.resetPassword()`

#### 5. UserController.createAddressBook
- **HTTP**: `POST /api/v1/user/address-book`
- **Request**: `@RequestBody UserShippingInfo`
  ```
  UserShippingInfo {
    shippingAddressId: Long (nullable)
    shippingDetails: ShippingDetails
    defaultYn: Yn
  }
  ```
- **Response**: `ApiResponse<UserShippingInfo>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: 배송지 등록

#### 6. UserController.updateAddressBook
- **HTTP**: `PUT /api/v1/user/address-book/{shippingAddressId}`
- **Request**: `@PathVariable long shippingAddressId` + `@RequestBody UserShippingInfo`
- **Response**: `ApiResponse<UserShippingInfo>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: 배송지 전체 수정

#### 7. UserController.deleteAddressBook
- **HTTP**: `DELETE /api/v1/user/address-book/{shippingAddressId}`
- **Request**: `@PathVariable long shippingAddressId`
- **Response**: `ApiResponse<ShippingAddress>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: 배송지 삭제

#### 8. UserController.createRefundAccount
- **HTTP**: `POST /api/v1/user/refund-account`
- **Request**: `@RequestBody @Validated CreateRefundAccount`
  ```
  CreateRefundAccount {
    bankName: String (NotNull)
    accountNumber: String  // 숫자만 허용
    accountHolderName: String
  }
  ```
- **Response**: `ApiResponse<RefundAccountInfo>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: 환불 계좌 등록

#### 9. UserController.updateRefundAccount
- **HTTP**: `PUT /api/v1/user/refund-account/{refundAccountId}`
- **Request**: `@PathVariable long refundAccountId` + `@RequestBody @Validated CreateRefundAccount`
- **Response**: `ApiResponse<RefundAccountInfo>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: 환불 계좌 수정

#### 10. UserController.deleteRefundAccount
- **HTTP**: `DELETE /api/v1/user/refund-account/{refundAccountId}`
- **Request**: `@PathVariable long refundAccountId`
- **Response**: `ApiResponse<RefundAccountInfo>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: 환불 계좌 삭제

#### 11. UserController.createUserFavorite
- **HTTP**: `POST /api/v1/user/my-favorite`
- **Request**: `@RequestBody @Validated CreateUserFavorite`
  ```
  CreateUserFavorite {
    productGroupId: long (NotNull)
  }
  ```
- **Response**: `ApiResponse<UserFavoriteResponse>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: 상품 찜 등록

#### 12. UserController.deleteUserFavorite
- **HTTP**: `DELETE /api/v1/user/my-favorite/{productGroupId}`
- **Request**: `@PathVariable long productGroupId`
- **Response**: `ApiResponse<Long>`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **설명**: 상품 찜 삭제

---

## 발견된 이슈

| # | 위치 | 이슈 유형 | 내용 | 심각도 |
|---|------|-----------|------|--------|
| 1 | UserController L81 | URL typo | `POST /api/v1/user/withdrawl` → 올바른 철자는 `withdrawal` | Medium |
| 2 | UserController L89 | 메서드명 오류 | 메서드명 `login`이 두 개 존재 (L72 로그인, L89 비밀번호 재설정) - 컴파일러는 파라미터 타입 차이로 허용 | Low |
| 3 | UserController | 공개 API 범위 | join, login, exists, password 엔드포인트는 인증 없이 접근 가능 (의도된 설계이나 명시적 문서화 필요) | Info |

---

## 도메인별 엔드포인트 그룹

| 도메인 | Query | Command |
|--------|-------|---------|
| 인증/계정 (Auth) | fetchUser, isExistUser | join, login, withdrawal, resetPassword |
| 배송지 (ShippingAddress) | fetchAddressBook x2 | createAddressBook, updateAddressBook, deleteAddressBook |
| 환불계좌 (RefundAccount) | fetchRefundAccount | createRefundAccount, updateRefundAccount, deleteRefundAccount |
| 마이페이지 (MyPage) | fetchMyPage | - |
| 찜 (Favorite) | fetchMyFavorites | createUserFavorite, deleteUserFavorite |
| 리다이렉트 (Redirect) | redirectHandler | - |

---

## 다음 단계

각 엔드포인트 흐름 분석:
```bash
# Query 엔드포인트 흐름 분석
/legacy-flow web:UserController.fetchUser
/legacy-flow web:UserController.fetchMyPage
/legacy-flow web:UserController.fetchMyFavorites
/legacy-flow web:UserController.fetchAddressBook
/legacy-flow web:UserController.fetchRefundAccount

# Command 엔드포인트 흐름 분석
/legacy-flow web:UserController.join
/legacy-flow web:UserController.login
/legacy-flow web:UserController.withdrawal
/legacy-flow web:UserController.createAddressBook
/legacy-flow web:UserController.createRefundAccount
/legacy-flow web:UserController.createUserFavorite
```

DTO 변환:
```bash
/legacy-convert web:UserController.fetchUser
/legacy-convert web:UserController.fetchMyPage
/legacy-convert web:UserController.fetchMyFavorites
/legacy-convert web:UserController.join
```
