# 엔드포인트 목록: Review 모듈

> Generated by `/legacy-endpoints` command
> Date: 2026-02-04
> Source: bootstrap-legacy-web-api

---

## 📊 요약

| 분류 | 개수 |
|------|------|
| Query (조회) | 3개 |
| Command (명령) | 2개 |
| **총계** | **5개** |

---

## 📖 Query (조회성) - 3개

| # | Controller | Method | HTTP | Path | Request Type |
|---|------------|--------|------|------|--------------|
| 1 | ReviewController | fetchProductGroupReviews | GET | /api/v1/reviews | @ModelAttribute |
| 2 | ReviewController | fetchAvailableReviews | GET | /api/v1/reviews/my-page/available | @ModelAttribute |
| 3 | ReviewController | fetchMyReviews | GET | /api/v1/reviews/my-page/written | @ModelAttribute |

### 상세 목록

#### 1. ReviewController.fetchProductGroupReviews
- **HTTP**: `GET /api/v1/reviews`
- **인증**: 없음 (Public)
- **Request**: `@ModelAttribute ReviewFilter` + `Pageable`
  - `ReviewFilter`: `orderType`, `productGroupId`, `lastDomainId`
- **Response**: `ApiResponse<ReviewPage<ReviewDto>>`
- **설명**: 상품그룹 리뷰 목록 조회 (평균 평점 포함)

#### 2. ReviewController.fetchAvailableReviews
- **HTTP**: `GET /api/v1/reviews/my-page/available`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **Request**: `@ModelAttribute ReviewFilter` + `Pageable`
  - `ReviewFilter`: `orderType`, `productGroupId`, `lastDomainId`
- **Response**: `ApiResponse<CustomSlice<ReviewOrderProductDto>>`
- **설명**: 작성 가능한 리뷰 목록 조회 (마이페이지)

#### 3. ReviewController.fetchMyReviews
- **HTTP**: `GET /api/v1/reviews/my-page/written`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **Request**: `@ModelAttribute ReviewFilter` + `Pageable`
  - `ReviewFilter`: `orderType`, `productGroupId`, `lastDomainId`
- **Response**: `ApiResponse<CustomSlice<ReviewDto>>`
- **설명**: 내가 작성한 리뷰 목록 조회 (마이페이지)

---

## ✏️ Command (커맨드성) - 2개

| # | Controller | Method | HTTP | Path | Request Type |
|---|------------|--------|------|------|--------------|
| 1 | ReviewController | doReview | POST | /api/v1/review | @RequestBody |
| 2 | ReviewController | deleteReview | DELETE | /api/v1/review/{reviewId} | @PathVariable |

### 상세 목록

#### 1. ReviewController.doReview
- **HTTP**: `POST /api/v1/review`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **Request**: `@RequestBody @Validated CreateReview`
  ```java
  CreateReview {
      long orderId;           // @NotNull
      long productGroupId;    // @NotNull
      double rating;          // @NotNull @ValidRating
      String content;         // @Length(max=500)
      List<ReviewImageDto> reviewImages;  // @Size(max=3)
  }
  ```
- **Response**: `ApiResponse<Review>`
- **설명**: 리뷰 작성

#### 2. ReviewController.deleteReview
- **HTTP**: `DELETE /api/v1/review/{reviewId}`
- **인증**: `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")`
- **Request**: `@PathVariable("reviewId") long reviewId`
- **Response**: `ApiResponse<Review>`
- **설명**: 리뷰 삭제

---

## 📋 관련 DTO 구조

### ReviewFilter (Request)
```java
public class ReviewFilter {
    private OrderType orderType;
    private Long productGroupId;
    private Long lastDomainId;
}
```

### ReviewDto (Response)
```java
public class ReviewDto {
    private long reviewId;
    private long orderId;
    private String userName;
    private double rating;
    private String content;
    private long productGroupId;
    private String productGroupName;
    private String productGroupImageUrl;
    private BrandDto brand;
    private long categoryId;
    private String categoryName;
    private Set<ReviewImageDto> reviewImages;
    private LocalDateTime insertDate;
    private LocalDateTime paymentDate;
    private String option;
}
```

### ReviewPage<T> (Response Wrapper)
```java
public class ReviewPage<T> implements Page<T> {
    private double averageRating;  // 평균 평점
    private List<T> content;
    private Pageable pageable;
    private boolean last;
    private int totalPages;
    private long totalElements;
    private Long lastDomainId;
    // ... Page interface 구현
}
```

### ReviewOrderProductDto (Response)
```java
public class ReviewOrderProductDto extends OrderProductDto {
    private LocalDateTime paymentDate;
    // OrderProductDto 필드: paymentId, sellerId, orderId, brand,
    //   productGroupId, productGroupName, productId, sellerName, etc.
}
```

### ReviewImageDto (Request/Response)
```java
public class ReviewImageDto {
    private Long reviewImageId;
    private ProductGroupImageType reviewImageType;
    private String imageUrl;
}
```

---

## 🔗 다음 단계

각 엔드포인트 분석:
```bash
# Query 엔드포인트 흐름 분석
/legacy-flow web:ReviewController.fetchProductGroupReviews
/legacy-flow web:ReviewController.fetchAvailableReviews
/legacy-flow web:ReviewController.fetchMyReviews

# Command 엔드포인트 흐름 분석
/legacy-flow web:ReviewController.doReview
/legacy-flow web:ReviewController.deleteReview
```

DTO 변환:
```bash
/legacy-convert web:ReviewController.fetchProductGroupReviews
```
