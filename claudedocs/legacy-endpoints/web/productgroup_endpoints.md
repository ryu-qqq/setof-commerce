# 엔드포인트 목록: productgroup 모듈

> Generated by `/legacy-endpoints` command
> Date: 2026-03-08
> Source: bootstrap-legacy-web-api
> Controller: `com.setof.connectly.module.product.controller.ProductController`

---

## 요약

| 분류 | 개수 |
|------|------|
| Query (조회) | 5개 |
| Command (명령) | 0개 |
| **총계** | **5개** |

---

## Query (조회성) - 5개

| # | Controller | Method | HTTP | Full Path | Request Type | Response Type |
|---|------------|--------|------|-----------|--------------|---------------|
| 1 | ProductController | fetchProductGroup | GET | `/api/v1/product/group/{productGroupId}` | @PathVariable | `ApiResponse<ProductGroupResponse>` |
| 2 | ProductController | fetchProductGroupLikes | GET | `/api/v1/products/group/recent` | @RequestParam | `ApiResponse<List<ProductGroupThumbnail>>` |
| 3 | ProductController | fetchProductGroups | GET | `/api/v1/products/group` | @ModelAttribute + Pageable | `ApiResponse<CustomSlice<ProductGroupThumbnail>>` |
| 4 | ProductController | fetchProductGroupWithBrand | GET | `/api/v1/product/group/brand/{brandId}` | @PathVariable + Pageable | `ApiResponse<List<ProductGroupThumbnail>>` |
| 5 | ProductController | fetchProductGroupWithSeller | GET | `/api/v1/product/group/seller/{sellerId}` | @PathVariable + Pageable | `ApiResponse<List<ProductGroupThumbnail>>` |

### 상세 목록

#### 1. ProductController.fetchProductGroup

- **HTTP**: `GET /api/v1/product/group/{productGroupId}`
- **Request**: `@PathVariable("productGroupId") long productGroupId`
- **Response**: `ApiResponse<ProductGroupResponse>`
- **Service**: `ProductGroupFindService.fetchProductGroup(productGroupId)`
- **설명**: 상품그룹 단건 상세 조회

**Response DTO 구조** (`ProductGroupResponse`):
```
ProductGroupDto productGroup
ProductNoticeDto productNotices
Set<ProductImageDto> productGroupImages
Set<ProductDto> products
Set<ProductCategoryDto> categories
String detailDescription
double mileageRate
double expectedMileageAmount
boolean isFavorite
EventProductType eventProductType
```

---

#### 2. ProductController.fetchProductGroupLikes

- **HTTP**: `GET /api/v1/products/group/recent`
- **Request**: `@RequestParam List<Long> productGroupIds`
- **Response**: `ApiResponse<List<ProductGroupThumbnail>>`
- **Service**: `ProductGroupFindService.fetchProductGroupRecent(productGroupIds)`
- **설명**: 최근 본 상품그룹 목록 조회 (ID 목록 기반 배치 조회)

---

#### 3. ProductController.fetchProductGroups

- **HTTP**: `GET /api/v1/products/group`
- **Request**: `@ModelAttribute ProductFilter` + `Pageable`
- **Response**: `ApiResponse<CustomSlice<ProductGroupThumbnail>>`
- **Service**: `ProductGroupFindService.fetchProductGroups(filter, pageable)`
- **설명**: 상품그룹 목록 조회 (다중 필터 + 커서 기반 페이징)

**Request Filter 구조** (`ProductFilter extends AbstractItemFilter`):
```
Long lastDomainId        // 커서 기반 페이징용 마지막 ID
String cursorValue       // 커서 값
Long lowestPrice         // 최저가 필터
Long highestPrice        // 최고가 필터
Long categoryId          // 단일 카테고리 ID 필터
Long brandId             // 단일 브랜드 ID 필터
Long sellerId            // 셀러 ID 필터
List<Long> categoryIds   // 다중 카테고리 ID 필터
List<Long> brandIds      // 다중 브랜드 ID 필터
OrderType orderType      // 정렬 타입
```

**Response DTO 구조** (`ProductGroupThumbnail`):
```
long productGroupId
long sellerId
String productGroupName
BrandDto brand
String productImageUrl
Price price
LocalDateTime insertDate
double averageRating
long reviewCount
double score
boolean isFavorite
ProductStatus productStatus
```

---

#### 4. ProductController.fetchProductGroupWithBrand

- **HTTP**: `GET /api/v1/product/group/brand/{brandId}`
- **Request**: `@PathVariable("brandId") long brandId` + `Pageable`
- **Response**: `ApiResponse<List<ProductGroupThumbnail>>`
- **Service**: `ProductGroupFindService.fetchProductGroupWithBrand(brandId, pageable)`
- **설명**: 특정 브랜드에 속한 상품그룹 목록 조회

---

#### 5. ProductController.fetchProductGroupWithSeller

- **HTTP**: `GET /api/v1/product/group/seller/{sellerId}`
- **Request**: `@PathVariable("sellerId") long sellerId` + `Pageable`
- **Response**: `ApiResponse<List<ProductGroupThumbnail>>`
- **Service**: `ProductGroupFindService.fetchProductGroupWithSeller(sellerId, pageable)`
- **설명**: 특정 셀러에 속한 상품그룹 목록 조회

---

## Command (커맨드성) - 0개

> 레거시 Web API ProductController에는 productgroup Command 엔드포인트가 없습니다.
> Command 엔드포인트(생성/수정/삭제)는 bootstrap-legacy-web-api-admin의 ProductController에 존재합니다.

---

## 의존성 정보

### Controller

- **Class**: `com.setof.connectly.module.product.controller.ProductController`
- **Annotation**: `@RestController`
- **Base Path**: `/api/v1`

### Service

- **ProductGroupFindService**: 상품그룹 조회 전용 서비스

### DTO Classes

| DTO | Package | 용도 |
|-----|---------|------|
| ProductFilter | `dto.filter` | 상품 목록 필터 조건 |
| ProductGroupResponse | `dto` | 상품그룹 단건 상세 응답 |
| ProductGroupThumbnail | `dto` | 상품그룹 목록 썸네일 응답 |
| CustomSlice | `common.dto` | 커서 기반 페이징 래퍼 |

---

## 다음 단계

### 개별 엔드포인트 플로우 분석

```
/legacy-flow web:ProductController.fetchProductGroup
/legacy-flow web:ProductController.fetchProductGroups
/legacy-flow web:ProductController.fetchProductGroupLikes
/legacy-flow web:ProductController.fetchProductGroupWithBrand
/legacy-flow web:ProductController.fetchProductGroupWithSeller
```

### 헥사고날 변환 대상

```
/legacy-convert web:ProductController.fetchProductGroup
/legacy-convert web:ProductController.fetchProductGroups
```

---

## 특이사항

1. **Query Only**: 모든 5개 엔드포인트가 GET 메서드로, 레거시 Web API는 상품그룹 조회 전담
2. **단수/복수 Path 불일치**: 단건 조회는 `/product/group/...`, 목록 조회는 `/products/group/...` 형식 혼재
3. **커서 기반 페이징**: `fetchProductGroups`는 `CustomSlice` + `ProductFilter(lastDomainId, cursorValue)`로 커서 페이징 지원
4. **다중 필터 지원**: 가격 범위, 단일/다중 카테고리, 단일/다중 브랜드, 셀러 등 복합 필터
5. **DiscountOffer 구현**: `ProductGroupThumbnail`이 `DiscountOffer` 인터페이스 구현 - 응답 후 할인 후처리 가능성
6. **QueryDSL 직접 매핑**: `@QueryProjection`으로 DB → DTO 직접 프로젝션
