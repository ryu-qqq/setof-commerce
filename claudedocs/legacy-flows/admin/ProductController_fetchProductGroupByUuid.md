# API Flow Documentation: ProductController.fetchProductGroup (UUID 기반)

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin

---

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/product/group/uuid/{externalProductUuId}` |
| Controller | `ProductController` |
| Service | `ProductGroupFetchService` → `ProductGroupFetchServiceImpl` |
| Repository | `ProductGroupFetchRepository` → `ProductGroupFetchRepositoryImpl` |
| 권한 | `HAS_ANY_AUTHORITY_MASTER_SELLER` |

---

## 📥 Request

### Request Parameters

| 파라미터 | 타입 | 필수 | 설명 | Validation |
|----------|------|------|------|------------|
| externalProductUuId | String | ✅ | 외부 상품 UUID | @PathVariable |

### Request 예시

```http
GET /api/v1/product/group/uuid/abc-123-def-456
```

---

## 📤 Response

### Response DTO 구조

```java
public record ProductGroupInfoDto(
    long productGroupId,
    String productGroupName
) {}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": [
    {
      "productGroupId": 12345,
      "productGroupName": "프리미엄 티셔츠"
    },
    {
      "productGroupId": 12346,
      "productGroupName": "프리미엄 티셔츠 (색상변형)"
    }
  ]
}
```

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────┐
│   ProductController                                          │
│   fetchProductGroup(@PathVariable String externalProductUuId)│
│   @GetMapping("/product/group/uuid/{externalProductUuId}")   │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   ProductGroupFetchServiceImpl                               │
│   fetchProductGroup(String externalProductUuId)              │
│   @Transactional(readOnly = true)                            │
│                                                              │
│   1. productGroupFetchRepository.fetchProductGroup(uuid)     │
│   2. Stream map → ProductGroupInfoDto 변환                   │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   ProductGroupFetchRepositoryImpl                            │
│   fetchProductGroup(String externalProductUuId)              │
│   (UUID 기반 조회 - List 반환)                                │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Database                                                   │
│   Tables: product_group, brand, seller, product_notice,      │
│           product_delivery, product_group_image,             │
│           product_group_detail_description, crawl_product,   │
│           external_product                                   │
└──────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| product_group | productGroup | FROM | - |
| brand | brand | LEFT JOIN | `brand.id = productGroup.brandId` |
| seller | seller | LEFT JOIN | `seller.id = productGroup.sellerId` |
| product_notice | productNotice | LEFT JOIN | `productGroup.productNotice` |
| product_delivery | productDelivery | LEFT JOIN | `productGroup.productDelivery` |
| product_group_image | productGroupImage | LEFT JOIN | `productGroup.images` + `deleteYn = 'N'` |
| product_group_detail_description | productGroupDetailDescription | INNER JOIN | `productGroup.detailDescription` |
| crawl_product | crawlProduct | LEFT JOIN | `crawlProduct.productGroupId = productGroup.id` |
| external_product | externalProduct | LEFT JOIN | `externalProduct.productGroupId = productGroup.id` |

### QueryDSL 코드 (핵심)

```java
getQueryFactory()
    .from(productGroup)
    .leftJoin(brand).on(brand.id.eq(productGroup.productGroupDetails.brandId))
    .leftJoin(seller).on(seller.id.eq(productGroup.productGroupDetails.sellerId))
    .leftJoin(productGroup.productNotice, productNotice)
    .leftJoin(productGroup.productDelivery, productDelivery)
    .leftJoin(productGroup.images, productGroupImage)
        .on(productGroupImage.deleteYn.eq(Yn.N))
    .innerJoin(productGroup.detailDescription, productGroupDetailDescription)
    .leftJoin(crawlProduct).on(crawlProduct.productGroupId.eq(productGroup.id))
    .leftJoin(externalProduct).on(externalProduct.productGroupId.eq(productGroup.id))
    .where(productGroup.productGroupDetails.externalProductUuId.eq(externalProductUuId))
    .transform(GroupBy.groupBy(productGroup.id).list(...))
```

### WHERE 조건

| 조건 | 필드 | 설명 |
|------|------|------|
| externalProductUuIdEq | `productGroup.externalProductUuId` | 외부 UUID 일치 |

---

## 📝 특이사항

1. **List 반환**: 하나의 UUID에 여러 상품그룹이 매핑될 수 있음
2. **간소화된 응답**: `ProductGroupInfoDto` (id, name만 반환)
3. **권한 체크 없음**: 단건 조회와 달리 sellerId 조건 없음

---

## 🔗 다음 단계

```bash
# DTO 변환
/legacy-convert admin:ProductController.fetchProductGroupByUuid
```
