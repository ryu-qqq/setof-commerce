# API Flow Documentation: ContentController.getBannerGroups (목록)

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin

---

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/content/banners` |
| Controller | `ContentController` |
| Service | `BannerFetchService` → `BannerFetchServiceImpl` |
| Repository | `BannerFindRepository` → `BannerFindRepositoryImpl` |
| 권한 | `@PreAuthorize(HAS_AUTHORITY_MASTER)` |

---

## 📥 Request

### Request Parameters (BannerFilter)

| 파라미터 | 타입 | 필수 | 설명 | Validation |
|----------|------|------|------|------------|
| bannerType | BannerType | ❌ | 배너 타입 | - |
| displayYn | Yn | ❌ | 전시 여부 | - |
| lastDomainId | Long | ❌ | 커서 페이징용 마지막 ID | - |
| startDate | LocalDateTime | ❌ | 검색 시작일 | @DateTimeFormat |
| endDate | LocalDateTime | ❌ | 검색 종료일 | @DateTimeFormat |
| searchKeyword | SearchKeyword | ❌ | 검색 키워드 타입 | - |
| searchWord | String | ❌ | 검색어 | - |

### Request Parameters (Pageable)

| 파라미터 | 타입 | 필수 | 설명 |
|----------|------|------|------|
| page | int | ❌ | 페이지 번호 (0부터 시작) |
| size | int | ❌ | 페이지 크기 |
| sort | String | ❌ | 정렬 조건 |

### Request DTO 구조

```java
// BannerFilter extends SearchAndDateFilter
public class BannerFilter extends SearchAndDateFilter {
    private BannerType bannerType;   // 배너 타입
    private Yn displayYn;            // 전시 여부
    private Long lastDomainId;       // 커서 페이징용
}

// 부모 클래스 - SearchAndDateFilter
public abstract class SearchAndDateFilter {
    @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    protected LocalDateTime startDate;
    @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    protected LocalDateTime endDate;
    protected SearchKeyword searchKeyword;
    protected String searchWord;
}
```

---

## 📤 Response

### Response DTO 구조

```java
// CustomPageable<BannerGroupDto>
public class CustomPageable<T> {
    private List<T> content;
    private int pageNumber;
    private int pageSize;
    private long totalElements;
    private int totalPages;
}

// BannerGroupDto
public class BannerGroupDto {
    private long bannerId;               // 배너 ID
    private String title;                // 제목
    private BannerType bannerType;       // 배너 타입
    private DisplayPeriod displayPeriod; // 전시 기간
    private Yn displayYn;                // 전시 여부
    private LocalDateTime insertDate;    // 등록일시
    private LocalDateTime updateDate;    // 수정일시
    private String insertOperator;       // 등록자
    private String updateOperator;       // 수정자
}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": {
    "content": [
      {
        "bannerId": 1,
        "title": "메인 배너",
        "bannerType": "MAIN",
        "displayPeriod": {
          "displayStartDate": "2024-01-01 00:00:00",
          "displayEndDate": "2024-12-31 23:59:59"
        },
        "displayYn": "Y",
        "insertDate": "2024-01-01 10:00:00",
        "updateDate": "2024-01-15 14:30:00",
        "insertOperator": "admin",
        "updateOperator": "admin"
      }
    ],
    "pageNumber": 0,
    "pageSize": 20,
    "totalElements": 50,
    "totalPages": 3
  }
}
```

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────────┐
│   Controller                                                      │
│   ContentController.getBannerGroups(                              │
│       @ModelAttribute BannerFilter,                               │
│       Pageable pageable                                           │
│   )                                                               │
│   @GetMapping("/content/banners")                                 │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│   Service                                                         │
│   BannerFetchServiceImpl.fetchBannerGroups(filter, pageable)      │
│   @Transactional(readOnly = true)                                 │
│                                                                   │
│   1. bannerFindRepository.fetchBannerGroups(filter, pageable)     │
│   2. fetchBannerCountQuery(filter) → totalCount                   │
│   3. bannerMapper.toBannerGroupDtos(results, pageable, total)     │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│   Repository                                                      │
│   BannerFindRepositoryImpl                                        │
│                                                                   │
│   Step 1: fetchBannerGroups() - 배너 목록 조회                     │
│   Step 2: fetchBannerGroupCountQuery() - COUNT 쿼리               │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│   Database                                                        │
│   Table: banner                                                   │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| banner | banner | FROM | - |

### QueryDSL 코드 - 목록 조회

```java
getQueryFactory().from(banner)
    .where(
        displayYnEq(filter.getDisplayYn()),
        bannerTypeEq(filter.getBannerType()),
        isCursorRead(filter.getLastDomainId()),
        searchKeywordEq(filter.getSearchKeyword(), filter.getSearchWord()),
        betweenTime(filter),
        notDeleted()
    )
    .orderBy(ORDERS.toArray(OrderSpecifier[]::new))
    .limit(pageable.getPageSize())
    .transform(
        GroupBy.groupBy(banner.id).list(
            new QBannerGroupDto(
                banner.id,
                banner.title,
                banner.bannerType,
                banner.displayPeriod,
                banner.displayYn,
                banner.insertDate,
                banner.updateDate,
                banner.insertOperator,
                banner.updateOperator
            )
        )
    );
```

### QueryDSL 코드 - COUNT 쿼리

```java
getQueryFactory()
    .select(banner.count())
    .from(banner)
    .where(
        displayYnEq(filter.getDisplayYn()),
        bannerTypeEq(filter.getBannerType()),
        isCursorRead(filter.getLastDomainId()),
        searchKeywordEq(filter.getSearchKeyword(), filter.getSearchWord()),
        betweenTime(filter),
        notDeleted()
    )
    .distinct();
```

### WHERE 조건

| 조건 | 필드 | 설명 |
|------|------|------|
| displayYnEq | banner.displayYn | 전시 여부 필터 |
| bannerTypeEq | banner.bannerType | 배너 타입 필터 |
| isCursorRead | banner.id | 커서 페이징 (lastDomainId < id) |
| searchKeywordEq | 동적 | 검색어 조건 (Strategy 패턴) |
| betweenTime | banner.displayPeriod | 전시 기간 범위 |
| notDeleted | banner.deleteYn | 삭제되지 않은 레코드 (N) |

### 기간 검색 로직 (betweenTime)

```java
private BooleanExpression betweenTime(BannerFilter filterDto){
    return banner.displayPeriod.displayStartDate.between(filterDto.getStartDate(), filterDto.getEndDate())
            .or(banner.displayPeriod.displayEndDate.between(filterDto.getStartDate(), filterDto.getEndDate()))
            .or(banner.displayPeriod.displayStartDate.loe(filterDto.getStartDate())
                .and(banner.displayPeriod.displayEndDate.goe(filterDto.getEndDate())));
}
```

### 기본 정렬

```java
// 정렬 조건이 없으면 기본값 사용
if(allOrderSpecifiers.isEmpty()){
    allOrderSpecifiers.add(banner.id.asc());
}
```

---

## 📦 관련 클래스

### Entity
- `Banner` - 배너 엔티티 (banner 테이블)

### Filter / DTO
- `BannerFilter` - 배너 검색 필터
- `SearchAndDateFilter` - 공통 검색/날짜 필터 (부모 클래스)
- `BannerGroupDto` - 응답 DTO
- `CustomPageable<T>` - 페이징 래퍼

### Mapper
- `BannerMapper.toBannerGroupDtos()` - 페이징 응답 변환

---

## 🔗 다음 단계

```bash
# DTO 변환
/legacy-convert admin:ContentController.getBannerGroups_list

# Persistence Layer 생성
/legacy-query admin:ContentController.getBannerGroups_list
```
