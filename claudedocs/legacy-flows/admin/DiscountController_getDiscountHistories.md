# API Flow Documentation: DiscountController.getDiscountHistories

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/discounts/history` |
| Controller | `DiscountController` |
| Service | `DiscountHistoryFetchService` → `DiscountHistoryFetchServiceImpl` |
| Repository | `DiscountPolicyHistoryFetchRepository` → `DiscountPolicyHistoryFetchRepositoryImpl` |
| 보안 | `@PreAuthorize(HAS_AUTHORITY_MASTER)` |

---

## 📥 Request

### Request Parameters

| 파라미터 | 타입 | 필수 | 설명 | Validation |
|----------|------|------|------|------------|
| discountPolicyId | Long | ❌ | 할인 정책 ID 필터 | - |
| periodType | PeriodType | ❌ | 기간 유형 | - |
| startDate | LocalDateTime | ❌ | 조회 시작일 | - |
| endDate | LocalDateTime | ❌ | 조회 종료일 | - |
| activeYn | Yn | ❌ | 활성화 여부 | - |
| publisherType | PublisherType | ❌ | 발행자 유형 | - |
| issueType | IssueType | ❌ | 적용 대상 유형 | - |
| page | int | ❌ | 페이지 번호 | Pageable |
| size | int | ❌ | 페이지 크기 | Pageable |
| sort | String | ❌ | 정렬 | Pageable |

### Request DTO 구조

```java
public class DiscountFilter extends SearchAndDateFilter {
    private Long discountPolicyId;
    private PeriodType periodType;
    private Yn activeYn;
    private PublisherType publisherType;
    private IssueType issueType;
    private Long userId;
}
```

---

## 📤 Response

### Response DTO 구조

```java
// Page<DiscountPolicyResponseDto>
public class DiscountPolicyResponseDto {
    private long discountPolicyId;
    private DiscountDetails discountDetails;
    private LocalDateTime insertDate;
    private LocalDateTime updateDate;
    private String insertOperator;
    private String updateOperator;
}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": {
    "content": [
      {
        "discountPolicyId": 1,
        "discountDetails": {
          "discountPolicyName": "여름 시즌 할인",
          "discountType": "RATIO",
          "publisherType": "PLATFORM",
          "issueType": "PRODUCT",
          "discountLimitYn": "Y",
          "maxDiscountPrice": 50000,
          "shareYn": "N",
          "shareRatio": 0.0,
          "discountRatio": 0.2,
          "policyStartDate": "2026-06-01 00:00:00",
          "policyEndDate": "2026-08-31 23:59:59",
          "memo": "여름 시즌 20% 할인",
          "priority": 1,
          "activeYn": "N"
        },
        "insertDate": "2026-05-01 10:00:00",
        "updateDate": "2026-09-01 00:00:00",
        "insertOperator": "admin",
        "updateOperator": "system"
      }
    ],
    "totalElements": 25,
    "totalPages": 2,
    "number": 0,
    "size": 20
  }
}
```

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────┐
│   Controller                                                  │
│   DiscountController.getDiscountHistories(filter, pageable)  │
│   @GetMapping("/discounts/history")                          │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Service                                                     │
│   DiscountHistoryFetchServiceImpl.fetchDiscountPolicyHisto..│
│   @Transactional(readOnly = true)                            │
│   1. fetchDiscountPolicyHistories() → 이력 목록 조회          │
│   2. fetchDiscountPolicyCountQuery() → 전체 카운트            │
│   3. PageableExecutionUtils.getPage() → 페이지 변환          │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Repository                                                  │
│   DiscountPolicyHistoryFetchRepositoryImpl                   │
│   - fetchDiscountPolicyHistories()                           │
│   - fetchDiscountPolicyCountQuery()                          │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Database                                                    │
│   Tables: discount_use_history, discount_policy,             │
│           discount_target                                    │
└──────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| discount_use_history | discountUseHistory | FROM | - |
| discount_policy | discountPolicy | JOIN | discountPolicy.id = discountUseHistory.discountPolicyId |
| discount_target | discountTarget | JOIN | discountTarget.discountPolicyId = discountPolicy.id |

### QueryDSL 코드 (목록 조회)

```java
getQueryFactory()
    .from(discountUseHistory)
    .join(discountPolicy).on(discountPolicy.id.eq(discountUseHistory.discountPolicyId))
    .join(discountTarget).on(discountTarget.discountPolicyId.eq(discountPolicy.id))
    .where(
        discountUseHistoryHasPolicyId(filter.getDiscountPolicyId()),
        periodTypeEq(filter.getStartDate(), filter.getEndDate()),
        policyActiveYn(filter.getActiveYn()),
        publisherTypeEq(filter.getPublisherType()),
        issueTypeEq(filter.getIssueType())
    )
    .offset(pageable.getOffset())
    .limit(pageable.getPageSize())
    .orderBy(ORDERS.toArray(OrderSpecifier[]::new))
    .distinct()
    .transform(
        groupBy(discountPolicy.id).list(
            new QDiscountPolicyResponseDto(
                discountPolicy.id,
                discountPolicy.discountDetails,
                discountPolicy.insertDate,
                discountPolicy.updateDate,
                discountPolicy.insertOperator,
                discountPolicy.updateOperator
            )
        )
    );
```

### WHERE 조건

| 조건 | 필드 | 설명 |
|------|------|------|
| discountUseHistoryHasPolicyId | discountUseHistory.discountPolicyId | 정책 ID 필터 (optional) |
| periodTypeEq | discountUseHistory.insertDate | 기간 범위 (between) |
| policyActiveYn | discountDetails.activeYn | 활성화 여부 |
| publisherTypeEq | discountDetails.publisherType | 발행자 유형 |
| issueTypeEq | discountDetails.issueType | 적용 대상 유형 |

### 특징

- `discount_use_history` 테이블에서 **사용 이력이 있는 할인 정책**만 조회
- `groupBy(discountPolicy.id)`로 중복 제거
- `distinct()` 적용

---

## 🔗 다음 단계

```bash
# DTO 변환
/legacy-convert admin:DiscountController.getDiscountHistories

# Persistence Layer 생성
/legacy-query admin:DiscountController.getDiscountHistories
```
