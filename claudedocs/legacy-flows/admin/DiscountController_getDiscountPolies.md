# API Flow Documentation: DiscountController.getDiscountPolies

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/discounts` |
| Controller | `DiscountController` |
| Service | `DiscountFetchService` → `DiscountFetchServiceImpl` |
| Repository | `DiscountPolicyFetchRepository` → `DiscountPolicyFetchRepositoryImpl` |
| 보안 | `@PreAuthorize(HAS_AUTHORITY_MASTER)` |

---

## 📥 Request

### Request Parameters

| 파라미터 | 타입 | 필수 | 설명 | Validation |
|----------|------|------|------|------------|
| discountPolicyId | Long | ❌ | 할인 정책 ID 필터 | - |
| periodType | PeriodType | ❌ | 기간 유형 (POLICY/INSERT) | - |
| startDate | LocalDateTime | ❌ | 조회 시작일 (SearchAndDateFilter 상속) | - |
| endDate | LocalDateTime | ❌ | 조회 종료일 (SearchAndDateFilter 상속) | - |
| activeYn | Yn | ❌ | 활성화 여부 (Y/N) | - |
| publisherType | PublisherType | ❌ | 발행자 유형 | - |
| issueType | IssueType | ❌ | 적용 대상 유형 (PRODUCT/SELLER) | - |
| userId | Long | ❌ | 사용자 ID 필터 | - |
| searchKeyword | String | ❌ | 검색 키워드 유형 (SearchAndDateFilter 상속) | - |
| searchWord | String | ❌ | 검색어 (SearchAndDateFilter 상속) | - |
| page | int | ❌ | 페이지 번호 (0부터 시작) | Pageable |
| size | int | ❌ | 페이지 크기 | Pageable |
| sort | String | ❌ | 정렬 (예: id,desc) | Pageable |

### Request DTO 구조

```java
public class DiscountFilter extends SearchAndDateFilter {
    private Long discountPolicyId;
    private PeriodType periodType;
    private Yn activeYn;
    private PublisherType publisherType;
    private IssueType issueType;
    private Long userId;
}
```

---

## 📤 Response

### Response DTO 구조

```java
public class CustomPageable<DiscountPolicyResponseDto> {
    private List<DiscountPolicyResponseDto> content;
    private int pageNumber;
    private int pageSize;
    private long totalElements;
    private int totalPages;
    private boolean first;
    private boolean last;
}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": {
    "content": [
      {
        "discountPolicyId": 1,
        "discountDetails": {
          "discountPolicyName": "신규 회원 할인",
          "discountType": "RATIO",
          "publisherType": "PLATFORM",
          "issueType": "PRODUCT",
          "discountLimitYn": "Y",
          "maxDiscountPrice": 10000,
          "shareYn": "Y",
          "shareRatio": 0.5,
          "discountRatio": 0.1,
          "policyStartDate": "2026-01-01 00:00:00",
          "policyEndDate": "2026-12-31 23:59:59",
          "memo": "신규 회원 대상 10% 할인",
          "priority": 1,
          "activeYn": "Y"
        },
        "insertDate": "2026-01-01 10:00:00",
        "updateDate": "2026-01-15 14:30:00",
        "insertOperator": "admin",
        "updateOperator": "admin"
      }
    ],
    "pageNumber": 0,
    "pageSize": 20,
    "totalElements": 100,
    "totalPages": 5,
    "first": true,
    "last": false
  }
}
```

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────┐
│   Controller                                                  │
│   DiscountController.getDiscountPolies(filter, pageable)     │
│   @GetMapping("/discounts")                                  │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Service                                                     │
│   DiscountFetchServiceImpl.fetchDiscountPolicies(...)        │
│   @Transactional(readOnly = true)                            │
│   1. fetchDiscountPolicies() → 목록 조회                      │
│   2. fetchDiscountPolicyCountQuery() → 전체 카운트            │
│   3. toProductCategoryContext() → 페이지 변환                 │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Repository                                                  │
│   DiscountPolicyFetchRepositoryImpl                          │
│   - fetchDiscountPolicies(filter, pageable) → List 조회      │
│   - fetchDiscountPolicyCountQuery(filter) → Count 조회       │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Database                                                    │
│   Tables: discount_policy, discount_target,                  │
│           product_group (LEFT), seller (LEFT)                │
└──────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| discount_policy | discountPolicy | FROM | - |
| discount_target | discountTarget | JOIN | discountTarget.discountPolicyId = discountPolicy.id |
| product_group | productGroup | LEFT JOIN | productGroup.id = discountTarget.targetId |
| seller | seller | LEFT JOIN | seller.id = discountTarget.targetId |

### QueryDSL 코드 (목록 조회)

```java
getQueryFactory()
    .select(
        new QDiscountPolicyResponseDto(
            discountPolicy.id,
            discountPolicy.discountDetails,
            discountPolicy.insertDate,
            discountPolicy.updateDate,
            discountPolicy.insertOperator,
            discountPolicy.updateOperator
        )
    )
    .from(discountPolicy)
    .join(discountPolicy)  // Note: 버그 - discountTarget이어야 함
    .on(discountTarget.discountPolicyId.eq(discountPolicy.id))
    .leftJoin(productGroup).on(productGroup.id.eq(discountTarget.targetId))
    .leftJoin(seller).on(seller.id.eq(discountTarget.targetId))
    .where(
        periodTypeEq(filter.getPeriodType(), filter.getStartDate(), filter.getEndDate()),
        policyActiveYn(filter.getActiveYn()),
        publisherTypeEq(filter.getPublisherType()),
        issueTypeEq(filter.getIssueType()),
        searchKeywordEq(filter.getSearchKeyword(), filter.getSearchWord())
    )
    .orderBy(ORDERS.toArray(OrderSpecifier[]::new))
    .fetch();
```

### WHERE 조건

| 조건 | 필드 | 설명 |
|------|------|------|
| periodTypeEq | policyStartDate, policyEndDate 또는 insertDate | 기간 필터 (POLICY: 정책 기간, 그 외: 등록일) |
| policyActiveYn | discountDetails.activeYn | 활성화 여부 |
| publisherTypeEq | discountDetails.publisherType | 발행자 유형 |
| issueTypeEq | discountDetails.issueType | 적용 대상 유형 |
| searchKeywordEq | (상속) | 키워드 검색 |

### 기본 정렬

```java
discountPolicy.id.desc()  // ID 내림차순 (최신순)
```

---

## 🔗 다음 단계

```bash
# DTO 변환
/legacy-convert admin:DiscountController.getDiscountPolies

# Persistence Layer 생성
/legacy-query admin:DiscountController.getDiscountPolies
```
