# API Flow Documentation: DiscountController.getDiscountTargets

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/discount/{discountPolicyId}/targets` |
| Controller | `DiscountController` |
| Service | `DiscountTargetFetchService` → `DiscountTargetFetchServiceImpl` |
| Repository | `DiscountTargetFetchRepository` → `DiscountTargetFetchRepositoryImpl` |
| 보안 | `@PreAuthorize(HAS_AUTHORITY_MASTER)` |

---

## 📥 Request

### Request Parameters

| 파라미터 | 타입 | 필수 | 설명 | Validation |
|----------|------|------|------|------------|
| discountPolicyId | long | ✅ | 할인 정책 ID | @PathVariable |
| issueType | IssueType | ✅ | 적용 대상 유형 (PRODUCT/SELLER) | @RequestParam |
| page | int | ❌ | 페이지 번호 (0부터 시작) | Pageable |
| size | int | ❌ | 페이지 크기 | Pageable |

---

## 📤 Response

### Response DTO 구조 (다형성)

```java
// 인터페이스
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type")
@JsonSubTypes({
    @JsonSubTypes.Type(value = ProductDiscountTarget.class, name = "productDiscountTarget"),
    @JsonSubTypes.Type(value = SellerDiscountTarget.class, name = "sellerDiscountTarget")
})
public interface DiscountTargetResponseDto {
    IssueType getType();
}

// issueType = PRODUCT 일 때
@JsonTypeName("productDiscountTarget")
public class ProductDiscountTarget implements DiscountTargetResponseDto {
    private long discountPolicyId;
    private long discountTargetId;
    private long productGroupId;
    private String insertOperator;
    private LocalDateTime insertDate;
    private ProductGroupInfo productGroup;    // 추가 조회
    private Set<ProductFetchResponse> products; // 추가 조회
}

// issueType = SELLER 일 때
@JsonTypeName("sellerDiscountTarget")
public class SellerDiscountTarget implements DiscountTargetResponseDto {
    private long discountPolicyId;
    private long discountTargetId;
    private String sellerName;
    private String insertOperator;
    private LocalDateTime insertDate;
}
```

### Response JSON 예시 (PRODUCT)

```json
{
  "success": true,
  "data": {
    "content": [
      {
        "type": "productDiscountTarget",
        "discountPolicyId": 1,
        "discountTargetId": 101,
        "productGroupId": 5001,
        "insertOperator": "admin",
        "insertDate": "2026-01-01 10:00:00",
        "productGroup": {
          "id": 5001,
          "name": "상품 그룹 A",
          "...": "..."
        },
        "products": [
          { "productId": 10001, "productName": "상품 1" }
        ]
      }
    ],
    "totalElements": 50,
    "totalPages": 3,
    "number": 0,
    "size": 20
  }
}
```

### Response JSON 예시 (SELLER)

```json
{
  "success": true,
  "data": {
    "content": [
      {
        "type": "sellerDiscountTarget",
        "discountPolicyId": 1,
        "discountTargetId": 201,
        "sellerName": "판매자 A",
        "insertOperator": "admin",
        "insertDate": "2026-01-01 10:00:00"
      }
    ],
    "totalElements": 10,
    "totalPages": 1,
    "number": 0,
    "size": 20
  }
}
```

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────┐
│   Controller                                                  │
│   DiscountController.getDiscountTargets(id, issueType, page) │
│   @GetMapping("/discount/{discountPolicyId}/targets")        │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Service                                                     │
│   DiscountTargetFetchServiceImpl.fetchDiscountTargets(...)   │
│   @Transactional(readOnly = true)                            │
│                                                               │
│   switch (issueType) {                                       │
│     case PRODUCT → fetchProductTargets()                     │
│     case SELLER  → fetchSellerTargets()                      │
│   }                                                           │
└────────────────────────┬─────────────────────────────────────┘
                         │
          ┌──────────────┴──────────────┐
          │                             │
          ▼ (PRODUCT)                   ▼ (SELLER)
┌─────────────────────────┐   ┌─────────────────────────┐
│ 1. fetchProductTargets  │   │ fetchSellerTargets      │
│ 2. ProductGroupFetch    │   │ (단순 조회)              │
│    Service 추가 조회     │   │                         │
└─────────────────────────┘   └─────────────────────────┘
          │                             │
          └──────────────┬──────────────┘
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Repository                                                  │
│   DiscountTargetFetchRepositoryImpl                          │
│   - fetchProductTargets() / fetchSellerTargets()             │
│   - fetchProductTargetCountQuery() / fetchSellerTargetCount  │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Database                                                    │
│   Tables: discount_target + (product_group 또는 seller)      │
└──────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Query 분석

### PRODUCT 타입 조회

#### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| discount_target | discountTarget | FROM | - |
| product_group | productGroup | INNER JOIN | discountTarget.targetId = productGroup.id |

#### QueryDSL 코드

```java
queryFactory
    .select(
        new QProductDiscountTarget(
            discountTarget.discountPolicyId,
            discountTarget.id,
            discountTarget.targetId,   // productGroupId
            discountTarget.insertOperator,
            discountTarget.insertDate
        )
    )
    .from(discountTarget)
    .innerJoin(productGroup).on(discountTarget.targetId.eq(productGroup.id))
    .where(
        discountTargetHasPolicyEq(discountPolicyId),
        deleteYn(),    // deleteYn = N
        activeYn()     // activeYn = Y
    )
    .offset(pageable.getOffset())
    .limit(pageable.getPageSize())
    .fetch();
```

#### 추가 조회 (Service Layer)

```java
// ProductGroupFetchService를 통해 상품 그룹 상세 정보 추가 조회
List<ProductGroupDetailResponse> productGroupDetails =
    productGroupFetchService.fetchProductGroups(productGroupIds);
```

### SELLER 타입 조회

#### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| discount_target | discountTarget | FROM | - |
| seller | seller | JOIN | seller.id = discountTarget.targetId |

#### QueryDSL 코드

```java
queryFactory
    .select(
        new QSellerDiscountTarget(
            discountTarget.discountPolicyId,
            discountTarget.targetId,   // sellerId
            seller.sellerName,
            discountTarget.insertOperator,
            discountTarget.insertDate
        )
    )
    .from(discountTarget)
    .join(seller).on(seller.id.eq(discountTarget.targetId))
    .where(
        discountTargetHasPolicyEq(discountPolicyId),
        deleteYn(),
        activeYn()
    )
    .offset(pageable.getOffset())
    .limit(pageable.getPageSize())
    .fetch();
```

### WHERE 조건

| 조건 | 필드 | 설명 |
|------|------|------|
| discountTargetHasPolicyEq | discountTarget.discountPolicyId | 정책 ID 일치 |
| deleteYn | discountTarget.deleteYn | 삭제되지 않은 것 (N) |
| activeYn | discountTarget.activeYn | 활성화된 것 (Y) |

---

## 🔗 다음 단계

```bash
# DTO 변환
/legacy-convert admin:DiscountController.getDiscountTargets

# Persistence Layer 생성
/legacy-query admin:DiscountController.getDiscountTargets
```
