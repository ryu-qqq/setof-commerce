# API Flow Documentation: ContentController.getContents

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin

---

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/contents` |
| Controller | `ContentController` |
| Service | `ContentFetchService` → `ContentFetchServiceImpl` |
| Repository | `ContentFetchRepository` → `ContentFetchRepositoryImpl` |
| 권한 | `@PreAuthorize(HAS_AUTHORITY_MASTER)` |

---

## 📥 Request

### Request Parameters (ContentFilter)

| 파라미터 | 타입 | 필수 | 설명 | Validation |
|----------|------|------|------|------------|
| displayYn | Yn | ❌ | 전시 여부 (Y/N) | - |
| lastDomainId | Long | ❌ | 커서 페이징용 마지막 ID | - |
| periodType | PeriodType | ✅ | 기간 검색 타입 | @NotNull |
| startDate | LocalDateTime | ❌ | 검색 시작일 | @DateTimeFormat |
| endDate | LocalDateTime | ❌ | 검색 종료일 | @DateTimeFormat |
| searchKeyword | SearchKeyword | ❌ | 검색 키워드 타입 | - |
| searchWord | String | ❌ | 검색어 | - |

### Request Parameters (Pageable)

| 파라미터 | 타입 | 필수 | 설명 |
|----------|------|------|------|
| page | int | ❌ | 페이지 번호 (0부터 시작) |
| size | int | ❌ | 페이지 크기 |
| sort | String | ❌ | 정렬 조건 (예: id,desc) |

### Request DTO 구조

```java
// ContentFilter extends SearchAndDateFilter
public class ContentFilter extends SearchAndDateFilter {
    private Yn displayYn;         // 전시 여부
    private Long lastDomainId;    // 커서 페이징용
    @NotNull(message = "periodType은 필수입니다.")
    private PeriodType periodType; // 기간 타입
}

// 부모 클래스 - SearchAndDateFilter
@ValidDateRange(start = "startDate", end = "endDate")
public abstract class SearchAndDateFilter {
    @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    protected LocalDateTime startDate;
    @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    protected LocalDateTime endDate;
    protected SearchKeyword searchKeyword;
    protected String searchWord;
}
```

---

## 📤 Response

### Response DTO 구조

```java
// CustomPageable<ContentResponse>
public class CustomPageable<T> {
    private List<T> content;      // 데이터 목록
    private int pageNumber;       // 현재 페이지
    private int pageSize;         // 페이지 크기
    private long totalElements;   // 전체 요소 수
    private int totalPages;       // 전체 페이지 수
}

// ContentResponse
public class ContentResponse {
    private long contentId;              // 콘텐츠 ID
    private Yn displayYn;                // 전시 여부
    private String title;                // 제목
    private DisplayPeriod displayPeriod; // 전시 기간
    private String insertOperator;       // 등록자
    private String updateOperator;       // 수정자
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private LocalDateTime insertDate;    // 등록일시
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private LocalDateTime updateDate;    // 수정일시
}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": {
    "content": [
      {
        "contentId": 1,
        "displayYn": "Y",
        "title": "메인 콘텐츠",
        "displayPeriod": {
          "displayStartDate": "2024-01-01 00:00:00",
          "displayEndDate": "2024-12-31 23:59:59"
        },
        "insertOperator": "admin",
        "updateOperator": "admin",
        "insertDate": "2024-01-01 10:00:00",
        "updateDate": "2024-01-15 14:30:00"
      }
    ],
    "pageNumber": 0,
    "pageSize": 20,
    "totalElements": 100,
    "totalPages": 5
  }
}
```

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────────┐
│   Controller                                                      │
│   ContentController.getContents(                                  │
│       @ModelAttribute ContentFilter,                              │
│       Pageable pageable                                           │
│   )                                                               │
│   @GetMapping("/contents")                                        │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│   Service                                                         │
│   ContentFetchServiceImpl.fetchContents(filter, pageable)         │
│   @Transactional(readOnly = true)                                 │
│                                                                   │
│   1. contentFetchRepository.fetchContents(filter, pageable)       │
│   2. fetchContentCountQuery(filter, pageable) → totalCount        │
│   3. contentMapper.toContentResponses(results, pageable, total)   │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│   Repository                                                      │
│   ContentFetchRepositoryImpl                                      │
│                                                                   │
│   Step 1: fetchContentsId() - ID 목록 조회                         │
│   Step 2: fetchContents() - ID 기반 상세 조회                      │
│   Step 3: fetchContentQuery() - COUNT 쿼리                        │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│   Database                                                        │
│   Table: content                                                  │
│   Strategy: 2-Phase Query (ID 조회 → 상세 조회)                    │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| content | content | FROM | - |

### QueryDSL 코드 - ID 조회

```java
// Step 1: ID 목록 조회
getQueryFactory().select(content.id)
    .from(content)
    .orderBy(ORDERS.toArray(OrderSpecifier[]::new))
    .limit(pageable.getPageSize())
    .offset(pageable.getOffset())
    .where(
        displayYnEq(filter.getDisplayYn()),
        periodTypeEq(filter.getPeriodType(), filter.getStartDate(), filter.getEndDate()),
        searchKeywordEq(filter.getSearchKeyword(), filter.getSearchWord()),
        deleteYnEq()
    )
    .fetch();
```

### QueryDSL 코드 - 상세 조회

```java
// Step 2: 상세 조회
getQueryFactory().selectFrom(content)
    .where(
        contentIdIn(contentIds),
        deleteYnEq()
    )
    .orderBy(ORDERS.toArray(OrderSpecifier[]::new))
    .transform(
        GroupBy.groupBy(content.id).list(
            new QContentResponse(
                content.id,
                content.displayYn,
                content.title,
                content.displayPeriod,
                content.insertOperator,
                content.insertDate,
                content.updateOperator,
                content.updateDate
            )
        )
    );
```

### QueryDSL 코드 - COUNT 쿼리

```java
// Step 3: COUNT 쿼리
getQueryFactory()
    .select(content.count())
    .from(content)
    .where(
        displayYnEq(filter.getDisplayYn()),
        periodTypeEq(filter.getPeriodType(), filter.getStartDate(), filter.getEndDate()),
        searchKeywordEq(filter.getSearchKeyword(), filter.getSearchWord()),
        deleteYnEq()
    )
    .distinct();
```

### WHERE 조건

| 조건 | 필드 | 설명 |
|------|------|------|
| displayYnEq | content.displayYn | 전시 여부 필터 |
| periodTypeEq | content.insertDate / content.displayPeriod | 기간 타입별 조건 |
| searchKeywordEq | 동적 | 검색어 조건 (Strategy 패턴) |
| deleteYnEq | content.deleteYn | 삭제되지 않은 레코드 (N) |

### 기간 검색 로직 (periodTypeEq)

```java
private BooleanExpression periodTypeEq(PeriodType periodType, LocalDateTime startDate, LocalDateTime endDate) {
    // INSERT 타입: 등록일 기준
    if (periodType != null && periodType.isInsert()) {
        return content.insertDate.goe(startDate)
                .and(content.insertDate.loe(endDate));
    }
    // DISPLAY 타입: 전시 기간 기준
    return content.displayPeriod.displayStartDate.between(startDate, endDate)
            .or(content.displayPeriod.displayEndDate.between(startDate, endDate))
            .or(content.displayPeriod.displayStartDate.loe(startDate)
                .and(content.displayPeriod.displayEndDate.goe(endDate)));
}
```

---

## 📦 관련 클래스

### Entity
- `Content` - 콘텐츠 엔티티 (content 테이블)

### Filter / DTO
- `ContentFilter` - 검색 필터
- `SearchAndDateFilter` - 공통 검색/날짜 필터 (부모 클래스)
- `ContentResponse` - 응답 DTO
- `CustomPageable<T>` - 페이징 래퍼

### Mapper
- `ContentMapper.toContentResponses()` - 페이징 응답 변환

---

## 🔗 다음 단계

```bash
# DTO 변환
/legacy-convert admin:ContentController.getContents

# Persistence Layer 생성
/legacy-query admin:ContentController.getContents
```
