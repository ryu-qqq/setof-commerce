# API Flow Documentation: DiscountController.getDiscountUseHistories

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/discount/history/{discountPolicyId}/use` |
| Controller | `DiscountController` |
| Service | `DiscountHistoryFetchService` → `DiscountHistoryFetchServiceImpl` |
| Repository | `DiscountPolicyHistoryFetchRepository` → `DiscountPolicyHistoryFetchRepositoryImpl` |
| 보안 | `@PreAuthorize(HAS_AUTHORITY_MASTER)` |

---

## 📥 Request

### Request Parameters

| 파라미터 | 타입 | 필수 | 설명 | Validation |
|----------|------|------|------|------------|
| discountPolicyId | long | ✅ | 할인 정책 ID | @PathVariable |
| startDate | LocalDateTime | ❌ | 조회 시작일 | - |
| endDate | LocalDateTime | ❌ | 조회 종료일 | - |
| userId | Long | ❌ | 사용자 ID 필터 | - |
| searchKeyword | String | ❌ | 검색 키워드 유형 | - |
| searchWord | String | ❌ | 검색어 | - |
| page | int | ❌ | 페이지 번호 | Pageable |
| size | int | ❌ | 페이지 크기 | Pageable |

### Request DTO 구조

```java
public class DiscountFilter extends SearchAndDateFilter {
    private Long discountPolicyId;
    private PeriodType periodType;
    private Yn activeYn;
    private PublisherType publisherType;
    private IssueType issueType;
    private Long userId;  // 사용자 ID 필터
}
```

---

## 📤 Response

### Response DTO 구조

```java
public class DiscountUseHistoryDto {
    @JsonIgnore
    private long discountUseHistoryId;     // 내부 ID (응답에서 제외)
    private long userId;                    // 사용자 ID
    private String name;                    // 사용자 이름
    private long orderId;                   // 주문 ID
    private BigDecimal orderAmount;         // 주문 금액
    private long paymentId;                 // 결제 ID
    private long productGroupId;            // 상품 그룹 ID
    private BigDecimal directDiscountPrice; // 직접 할인 금액
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private LocalDateTime useDate;          // 사용 일시
    private BigDecimal totalDirectDiscountPrice; // 총 직접 할인 금액 (합계)
}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": {
    "content": [
      {
        "userId": 1001,
        "name": "홍길동",
        "orderId": 50001,
        "orderAmount": 150000,
        "paymentId": 60001,
        "productGroupId": 5001,
        "directDiscountPrice": 15000,
        "useDate": "2026-01-15 14:30:00",
        "totalDirectDiscountPrice": 15000
      },
      {
        "userId": 1002,
        "name": "김영희",
        "orderId": 50002,
        "orderAmount": 200000,
        "paymentId": 60002,
        "productGroupId": 5002,
        "directDiscountPrice": 20000,
        "useDate": "2026-01-16 10:15:00",
        "totalDirectDiscountPrice": 20000
      }
    ],
    "totalElements": 150,
    "totalPages": 8,
    "number": 0,
    "size": 20
  }
}
```

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────┐
│   Controller                                                  │
│   DiscountController.getDiscountUseHistories(id, filter, p)  │
│   @GetMapping("/discount/history/{discountPolicyId}/use")    │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Service                                                     │
│   DiscountHistoryFetchServiceImpl.fetchDiscountUseHistories  │
│   @Transactional(readOnly = true)                            │
│   1. fetchDiscountUseHistories() → 사용 이력 조회             │
│   2. fetchDiscountUsePolicyCountQuery() → 전체 카운트         │
│   3. PageableExecutionUtils.getPage() → 페이지 변환          │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Repository                                                  │
│   DiscountPolicyHistoryFetchRepositoryImpl                   │
│   - fetchDiscountUseHistories()                              │
│   - fetchDiscountUsePolicyCountQuery()                       │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Database                                                    │
│   Tables: discount_use_history, order, order_snapshot_*,     │
│           users                                               │
└──────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| discount_use_history | discountUseHistory | FROM | - |
| order | order | INNER JOIN | order.id = discountUseHistory.orderId |
| order_snapshot_product_group | orderSnapShotProductGroup | INNER JOIN (FETCH) | order.orderSnapShotProductGroup |
| users | users | INNER JOIN | users.id = discountUseHistory.userId |

### QueryDSL 코드 (목록 조회)

```java
getQueryFactory()
    .select(
        discountUseHistory.id,
        discountUseHistory.userId,
        users.name,
        discountUseHistory.orderId,
        order.orderAmount,
        discountUseHistory.paymentId,
        discountUseHistory.productGroupId,
        orderSnapShotProductGroup.snapShotProductGroup.price.directDiscountPrice,
        discountUseHistory.insertDate
    )
    .from(discountUseHistory)
    .innerJoin(order).on(order.id.eq(discountUseHistory.orderId))
    .innerJoin(order.orderSnapShotProductGroup, orderSnapShotProductGroup).fetchJoin()
    .innerJoin(users).on(users.id.eq(discountUseHistory.userId))
    .where(
        discountUseHistoryHasPolicyId(discountPolicyId),
        periodTypeEq(filter.getStartDate(), filter.getEndDate()),
        userIdEq(filter.getUserId())
    )
    .distinct()
    .offset(pageable.getOffset())
    .limit(pageable.getPageSize())
    .groupBy(
        discountUseHistory.id,
        discountUseHistory.userId,
        users.name,
        discountUseHistory.orderId,
        order.orderAmount,
        discountUseHistory.paymentId,
        discountUseHistory.productGroupId,
        orderSnapShotProductGroup.snapShotProductGroup.price.directDiscountPrice,
        discountUseHistory.insertDate
    )
    .transform(
        groupBy(discountUseHistory.id).list(
            new QDiscountUseHistoryDto(
                discountUseHistory.id,
                discountUseHistory.userId,
                users.name,
                discountUseHistory.orderId,
                order.orderAmount,
                discountUseHistory.paymentId,
                discountUseHistory.productGroupId,
                orderSnapShotProductGroup.snapShotProductGroup.price.directDiscountPrice,
                discountUseHistory.insertDate,
                orderSnapShotProductGroup.snapShotProductGroup.price.directDiscountPrice.sum()
            )
        )
    );
```

### WHERE 조건

| 조건 | 필드 | 설명 |
|------|------|------|
| discountUseHistoryHasPolicyId | discountUseHistory.discountPolicyId | 정책 ID 일치 (필수) |
| periodTypeEq | discountUseHistory.insertDate | 기간 범위 (between) |
| userIdEq | discountUseHistory.userId | 사용자 ID 필터 (optional) |

### 특징

- `order_snapshot_product_group`에서 **스냅샷된 할인 금액** 조회
- `directDiscountPrice.sum()`으로 **총 할인 금액 집계**
- `groupBy` + `transform`으로 중복 제거 및 집계

---

## 📊 관련 Entity

### DiscountUseHistory

```java
@Entity
@Table(name = "discount_use_history")
public class DiscountUseHistory {
    private long id;
    private long discountPolicyId;
    private long userId;
    private long orderId;
    private long paymentId;
    private long productGroupId;
    private LocalDateTime useDate;
}
```

---

## 🔗 다음 단계

```bash
# DTO 변환
/legacy-convert admin:DiscountController.getDiscountUseHistories

# Persistence Layer 생성
/legacy-query admin:DiscountController.getDiscountUseHistories
```
