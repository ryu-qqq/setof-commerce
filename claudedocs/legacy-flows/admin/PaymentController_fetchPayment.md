# API Flow Documentation: PaymentController.fetchPayment

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin

---

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/payment/{paymentId}` |
| Controller | `PaymentController` |
| Service | `PaymentFetchService` → `PaymentFetchServiceImpl` |
| Repository | `PaymentFetchRepository` → `PaymentFetchRepositoryImpl` |
| 보조 Service | `OrderSnapShotFetchService` → `OrderSnapShotFetchServiceImpl` |
| 보조 Repository | `OrderSnapShotFetchRepository` → `OrderSnapShotFetchRepositoryImpl` |
| 인증 | `@PreAuthorize(HAS_ANY_AUTHORITY_MASTER_SELLER)` |

---

## 📥 Request

### Request Parameters

| 파라미터 | 타입 | 필수 | 설명 | Validation |
|----------|------|------|------|------------|
| paymentId | long | ✅ | 결제 ID | @PathVariable |

---

## 📤 Response

### Response DTO 구조

```java
public class PaymentResponse {
    private BuyerInfo buyerInfo;              // 구매자 정보 (Embedded)
    private PaymentDetail payment;             // 결제 상세 정보
    private ReceiverInfo receiverInfo;         // 수령인 정보
    private PaymentShipmentInfo shipmentInfo;  // 배송 정보
    private List<OrderProduct> orderProducts;  // 주문 상품 목록
}
```

### 중첩 DTO 상세

#### BuyerInfo (Embedded)
```java
@Embeddable
public class BuyerInfo {
    private String buyerName;        // 구매자명
    private String buyerEmail;       // 구매자 이메일
    private String buyerPhoneNumber; // 구매자 전화번호
}
```

#### PaymentDetail
```java
public class PaymentDetail {
    private long paymentId;                  // 결제 ID
    private String paymentAgencyId;          // PG사 결제 ID
    private PaymentStatus paymentStatus;     // 결제 상태 (Enum)
    private PaymentMethodEnum paymentMethod; // 결제 수단 (Enum)
    private LocalDateTime paymentDate;       // 결제일시
    private LocalDateTime canceledDate;      // 취소일시
    private long userId;                     // 사용자 ID
    private SiteName siteName;               // 사이트명 (Enum)
    private Money billAmount;                // 청구 금액
    private Money paymentAmount;             // 실 결제 금액
    private Money usedMileageAmount;         // 사용 마일리지
}
```

#### ReceiverInfo
```java
public class ReceiverInfo {
    private String receiverName;        // 수령인명
    private String receiverPhoneNumber; // 수령인 전화번호
    private String addressLine1;        // 주소1
    private String addressLine2;        // 주소2
    private String zipCode;             // 우편번호
    private Origin country;             // 국가 (Enum)
    private String deliveryRequest;     // 배송 요청사항
}
```

#### PaymentShipmentInfo
```java
public class PaymentShipmentInfo {
    private DeliveryStatus deliveryStatus;     // 배송 상태 (Enum)
    private String shipmentCompanyCode;        // 택배사 코드
    private String invoice;                    // 운송장 번호
    private LocalDateTime shipmentCompletedDate; // 배송 완료일
}
```

#### OrderProduct
```java
public class OrderProduct {
    private long orderId;                           // 주문 ID
    private ProductGroupSnapShotDetails productGroupDetails; // 상품그룹 스냅샷
    private BaseBrandContext brand;                 // 브랜드 정보
    private long productGroupId;                    // 상품그룹 ID
    private long productId;                         // 상품 ID
    private String sellerName;                      // 판매자명
    private String productGroupMainImageUrl;        // 상품 대표 이미지
    private String deliveryArea;                    // 배송 지역
    private int productQuantity;                    // 상품 수량
    private OrderStatus orderStatus;                // 주문 상태 (Enum)
    private Money regularPrice;                     // 정가
    private Money orderAmount;                      // 주문 금액
    private Money totalExpectedRefundMileageAmount; // 환불 예상 마일리지
    private String option;                          // 옵션 문자열
    private String skuNumber;                       // SKU 번호
    private Set<OptionDto> options;                 // 옵션 상세
}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": {
    "buyerInfo": {
      "buyerName": "홍길동",
      "buyerEmail": "hong@example.com",
      "buyerPhoneNumber": "010-1234-5678"
    },
    "payment": {
      "paymentId": 12345,
      "paymentAgencyId": "pg_abc123",
      "paymentStatus": "PAID",
      "paymentMethod": "CARD",
      "paymentDate": "2026-02-01 14:30:00",
      "canceledDate": null,
      "userId": 100,
      "siteName": "SETOF",
      "billAmount": { "amount": 150000 },
      "paymentAmount": { "amount": 140000 },
      "usedMileageAmount": { "amount": 10000 }
    },
    "receiverInfo": {
      "receiverName": "김수령",
      "receiverPhoneNumber": "010-9876-5432",
      "addressLine1": "서울시 강남구",
      "addressLine2": "테헤란로 123",
      "zipCode": "06234",
      "country": "KOREA",
      "deliveryRequest": "문 앞에 놓아주세요"
    },
    "shipmentInfo": {
      "deliveryStatus": "DELIVERED",
      "shipmentCompanyCode": "CJ",
      "invoice": "123456789012",
      "shipmentCompletedDate": "2026-02-03 10:00:00"
    },
    "orderProducts": [
      {
        "orderId": 1001,
        "productGroupId": 500,
        "productId": 501,
        "sellerName": "판매자A",
        "productGroupMainImageUrl": "https://...",
        "productQuantity": 2,
        "orderStatus": "DELIVERED",
        "option": "블랙 M",
        "options": [
          { "optionName": "색상", "optionValue": "블랙" },
          { "optionName": "사이즈", "optionValue": "M" }
        ]
      }
    ]
  }
}
```

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────┐
│   PaymentController                                           │
│   GET /api/v1/payment/{paymentId}                            │
│   @PreAuthorize(HAS_ANY_AUTHORITY_MASTER_SELLER)             │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   PaymentFetchServiceImpl                                     │
│   @Transactional(readOnly = true)                            │
│                                                               │
│   1. paymentFetchRepository.fetchPayment(paymentId, sellerId)│
│   2. orderSnapShotFetchService.fetchOrderSnapShotProducts()  │
│   3. paymentResponse.setOrderProducts(orderProducts)         │
└────────────────────────┬─────────────────────────────────────┘
                         │
         ┌───────────────┼───────────────┐
         ▼                               ▼
┌────────────────────────┐   ┌────────────────────────────────┐
│ PaymentFetchRepository │   │ OrderSnapShotFetchRepository   │
│ Impl                   │   │ Impl                           │
│                        │   │                                │
│ fetchPayment()         │   │ fetchOrderProductOption()      │
│ - 결제 기본 정보 조회    │   │ - 주문 상품 목록 조회           │
└────────────────────────┘   └────────────────────────────────┘
```

---

## 🗄️ Database Query 분석

### Query 1: PaymentFetchRepositoryImpl.fetchPayment()

#### 조회 테이블

| 테이블 | Q클래스 | 조인 유형 | 조인 조건 |
|--------|---------|----------|----------|
| payment | payment | FROM | - |
| payment_bill | paymentBill | INNER JOIN | payment.paymentBill |
| payment_method | paymentMethod | INNER JOIN | paymentBill.paymentMethod |
| order | order | INNER JOIN | payment.orders |
| shipment | shipment | INNER JOIN | order.shipment |
| payment_snap_shot_shipping_address | paymentSnapShotShippingAddress | INNER JOIN | payment.paymentSnapShotShippingAddress |
| payment_snap_shot_mileage | paymentSnapShotMileage | LEFT JOIN | payment.paymentSnapShotMileage |

#### QueryDSL 코드

```java
queryFactory
    .from(payment)
    .innerJoin(payment.paymentBill, paymentBill)
    .innerJoin(paymentBill.paymentMethod, paymentMethod)
    .innerJoin(payment.orders, order)
    .innerJoin(order.shipment, shipment)
    .innerJoin(payment.paymentSnapShotShippingAddress, paymentSnapShotShippingAddress)
    .leftJoin(payment.paymentSnapShotMileage, paymentSnapShotMileage)
    .where(paymentIdEq(paymentId), sellerIdEq(sellerId))
    .transform(
        GroupBy.groupBy(payment.id).as(
            new QPaymentResponse(...)
        )
    )
```

#### WHERE 조건

| 조건 메서드 | 필드 | 설명 |
|------------|------|------|
| paymentIdEq | payment.id | 결제 ID 일치 |
| sellerIdEq | order.sellerId | 판매자 권한 시 sellerId 필터 |

#### 권한별 조건 분기

```java
private BooleanExpression sellerIdEq(Optional<Long> sellerId) {
    RoleType authorization = SecurityUtils.getAuthorization();
    if (authorization.isSeller()) {
        return order.sellerId.eq(sellerId.get());
    }
    return null;  // MASTER는 전체 조회
}
```

---

### Query 2: OrderSnapShotFetchRepositoryImpl.fetchOrderProductOption()

#### 조회 테이블

| 테이블 | Q클래스 | 조인 유형 | 조인 조건 |
|--------|---------|----------|----------|
| order | order | FROM | - |
| order_snap_shot_product_group | orderSnapShotProductGroup | INNER JOIN | order.orderSnapShotProductGroup |
| order_snap_shot_product_delivery | orderSnapShotProductDelivery | INNER JOIN | order.orderSnapShotProductDelivery |
| order_snap_shot_product_group_image | orderSnapShotProductGroupImage | INNER JOIN | order.orderSnapShotProductGroupImages + imageType=MAIN |
| brand | brand | INNER JOIN | brandId 매핑 |
| seller | seller | INNER JOIN | order.sellerId |
| order_snap_shot_product_option | orderSnapShotProductOption | LEFT JOIN | order.orderSnapShotProductOptions |
| order_snap_shot_option_group | orderSnapShotOptionGroup | LEFT JOIN | optionGroupId 매핑 |
| order_snap_shot_option_detail | orderSnapShotOptionDetail | LEFT JOIN | optionDetailId 매핑 |
| order_snap_shot_mileage | orderSnapShotMileage | LEFT JOIN | order.orderSnapShotMileage |
| order_snap_shot_mileage_detail | orderSnapShotMileageDetail | LEFT JOIN | mileageDetails |

#### QueryDSL 코드

```java
queryFactory.from(order)
    .innerJoin(order.orderSnapShotProductGroup, orderSnapShotProductGroup)
    .innerJoin(order.orderSnapShotProductDelivery, orderSnapShotProductDelivery)
    .innerJoin(order.orderSnapShotProductGroupImages, orderSnapShotProductGroupImage)
        .on(orderSnapShotProductGroupImage.snapShotProductGroupImage.imageDetail
            .productGroupImageType.eq(ProductGroupImageType.MAIN))
    .innerJoin(brand)
        .on(brand.id.eq(orderSnapShotProductGroup.snapShotProductGroup.brandId))
    .innerJoin(seller)
        .on(order.sellerId.eq(seller.id))
    .leftJoin(order.orderSnapShotProductOptions, orderSnapShotProductOption)
    .leftJoin(order.orderSnapShotOptionGroups, orderSnapShotOptionGroup)
        .on(orderSnapShotOptionGroup.snapShotOptionGroup.optionGroupId
            .eq(orderSnapShotProductOption.snapShotProductOption.optionGroupId))
    .leftJoin(order.orderSnapShotOptionDetails, orderSnapShotOptionDetail)
        .on(orderSnapShotOptionDetail.snapShotOptionDetail.optionDetailId
            .eq(orderSnapShotProductOption.snapShotProductOption.optionDetailId))
    .leftJoin(order.orderSnapShotMileage, orderSnapShotMileage)
    .leftJoin(orderSnapShotMileage.mileageDetails, orderSnapShotMileageDetail)
    .where(paymentIdEq(paymentId), orderIdIn(orderIds), exclusiveOrderStatus())
    .groupBy(...)
    .distinct()
    .transform(GroupBy.groupBy(order.id).list(...))
```

#### WHERE 조건

| 조건 메서드 | 필드 | 설명 |
|------------|------|------|
| paymentIdEq | order.payment.id | 결제 ID 일치 |
| orderIdIn | order.id | 주문 ID 목록 (빈 경우 무시) |
| exclusiveOrderStatus | order.orderStatus | 제외할 주문 상태 필터 |

---

## 📊 Entity 관계도

```
Payment (1)
    │
    ├── PaymentBill (1)
    │       └── PaymentMethod (1)
    │       └── BuyerInfo (Embedded)
    │
    ├── PaymentSnapShotShippingAddress (1)
    │       └── ShippingDetails (Embedded)
    │
    ├── PaymentSnapShotMileage (0..1)
    │
    └── Order (N)
            │
            ├── Shipment (1)
            ├── Seller (1)
            ├── Brand (1)
            │
            ├── OrderSnapShotProductGroup (1)
            ├── OrderSnapShotProductDelivery (1)
            ├── OrderSnapShotProductGroupImage (N)
            │
            ├── OrderSnapShotProductOption (N)
            ├── OrderSnapShotOptionGroup (N)
            ├── OrderSnapShotOptionDetail (N)
            │
            └── OrderSnapShotMileage (0..1)
                    └── OrderSnapShotMileageDetail (N)
```

---

## 🔗 다음 단계

DTO 변환 (새 컨벤션):
```bash
/legacy-convert admin:PaymentController.fetchPayment
```

Persistence Layer 생성:
```bash
/legacy-query admin:PaymentController.fetchPayment
```

---

## 📁 관련 파일

| 분류 | 파일 경로 |
|------|----------|
| Controller | `module/payment/controller/PaymentController.java` |
| Service Interface | `module/payment/service/PaymentFetchService.java` |
| Service Impl | `module/payment/service/PaymentFetchServiceImpl.java` |
| Repository Interface | `module/payment/repository/PaymentFetchRepository.java` |
| Repository Impl | `module/payment/repository/PaymentFetchRepositoryImpl.java` |
| Response DTO | `module/payment/dto/PaymentResponse.java` |
| PaymentDetail DTO | `module/payment/dto/payment/PaymentDetail.java` |
| ReceiverInfo DTO | `module/payment/dto/receiver/ReceiverInfo.java` |
| PaymentShipmentInfo DTO | `module/payment/dto/shipment/PaymentShipmentInfo.java` |
| BuyerInfo Embedded | `module/payment/entity/embedded/BuyerInfo.java` |
| OrderProduct DTO | `module/order/dto/OrderProduct.java` |
| OrderSnapShotFetchService | `module/order/service/OrderSnapShotFetchService.java` |
| OrderSnapShotFetchServiceImpl | `module/order/service/OrderSnapShotFetchServiceImpl.java` |
| OrderSnapShotFetchRepository | `module/order/repository/OrderSnapShotFetchRepository.java` |
| OrderSnapShotFetchRepositoryImpl | `module/order/repository/OrderSnapShotFetchRepositoryImpl.java` |
