# API Flow Documentation: CategoryController.fetchAllChildCategories

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin

---

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/category/{categoryId}` |
| Controller | `CategoryController` |
| Service | `CategoryFetchService` → `CategoryFetchServiceImpl` |
| Repository | `CategoryFetchRepository` → `CategoryFetchRepositoryImpl` |
| 인증 | `@PreAuthorize(HAS_ANY_AUTHORITY_MASTER_SELLER)` |

---

## 📥 Request

### Request Parameters

| 파라미터 | 타입 | 필수 | 설명 | 바인딩 |
|----------|------|------|------|--------|
| categoryId | long | ✅ | 기준 카테고리 ID | @PathVariable |

---

## 📤 Response

### Response 구조

```java
ResponseEntity<ApiResponse<List<TreeCategoryContext>>>
```

### TreeCategoryContext 필드

| 필드 | 타입 | 설명 |
|------|------|------|
| categoryId | long | 카테고리 ID |
| categoryName | String | 카테고리명 |
| displayName | String | 노출명 |
| categoryDepth | int | 카테고리 depth |
| parentCategoryId | long | 부모 카테고리 ID |
| children | List | 하위 카테고리 (이 API에서는 비어있음) |

### Response JSON 예시

```json
{
  "success": true,
  "data": [
    {
      "categoryId": 1,
      "categoryName": "의류",
      "displayName": "의류",
      "categoryDepth": 1,
      "parentCategoryId": 0
    },
    {
      "categoryId": 10,
      "categoryName": "상의",
      "displayName": "상의",
      "categoryDepth": 2,
      "parentCategoryId": 1
    },
    {
      "categoryId": 100,
      "categoryName": "티셔츠",
      "displayName": "티셔츠",
      "categoryDepth": 3,
      "parentCategoryId": 10
    }
  ]
}
```

> ⚠️ **주의**: 이 API는 트리 구조가 아닌 **flat list**로 반환됩니다. depth 오름차순 정렬.

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────┐
│   CategoryController                                          │
│   fetchAllChildCategories(@PathVariable categoryId)           │
│   @GetMapping("/category/{categoryId}")                      │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   CategoryFetchServiceImpl                                    │
│   fetchAllChildCategories(categoryId)                        │
│   @Transactional(readOnly = true)                            │
│                                                               │
│   → 직접 Repository 호출 (변환 로직 없음)                     │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   CategoryFetchRepositoryImpl                                 │
│   fetchAllChildCategories(categoryId)                        │
│                                                               │
│   ⚡ Recursive CTE (WITH RECURSIVE) 사용                      │
│   - JPASQLQuery + SQLExpressions.unionAll                    │
│   - 기준 카테고리부터 모든 하위 카테고리 재귀 탐색             │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Database: category                                          │
│                                                               │
│   WITH RECURSIVE sub AS (                                    │
│     -- Base case: 기준 카테고리                               │
│     SELECT id, categoryName, displayName,                    │
│            parentCategoryId, categoryDepth                   │
│     FROM category WHERE id = :categoryId                     │
│                                                               │
│     UNION ALL                                                 │
│                                                               │
│     -- Recursive case: 하위 카테고리                          │
│     SELECT c.id, c.categoryName, c.displayName,              │
│            c.parentCategoryId, c.categoryDepth               │
│     FROM sub                                                  │
│     INNER JOIN category c ON sub.id = c.parentCategoryId     │
│   )                                                           │
│   SELECT * FROM sub ORDER BY categoryDepth ASC               │
└──────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 설명 |
|--------|-------|------|
| category | c, sub | 카테고리 마스터 (재귀 조회) |

### QueryDSL 코드 (Recursive CTE)

```java
// CategoryFetchRepositoryImpl.fetchAllChildCategories()
public List<TreeCategoryContext> fetchAllChildCategories(long categoryId) {
    JPASQLQuery<Category> q = new JPASQLQuery<>(em, SQLTemplates.DEFAULT);
    QCategory c = new QCategory("c");
    QCategory sub = new QCategory("sub");
    EntityPathBase<QCategory> rec = new EntityPathBase<>(QCategory.class, "sub");

    // Base case: 기준 카테고리
    JPQLQuery<Category> query1 = JPAExpressions
        .select(Projections.fields(Category.class,
            c.id, c.categoryName, c.displayName, c.parentCategoryId, c.categoryDepth))
        .from(c)
        .where(c.id.eq(categoryId));

    // Recursive case: parentCategoryId = sub.id 인 하위 카테고리
    JPQLQuery<Category> query2 = JPAExpressions
        .select(Projections.fields(Category.class,
            c.id, c.categoryName, c.displayName, c.parentCategoryId, c.categoryDepth))
        .from(rec)
        .innerJoin(c).on(sub.id.eq(c.parentCategoryId));  // 🔑 하위 방향 조인

    Union<Category> union = SQLExpressions.unionAll(query1, query2);

    return q.withRecursive(rec, c.id, c.categoryName, c.displayName, c.parentCategoryId, c.categoryDepth)
        .as(union)
        .select(new QTreeCategoryContext(
            sub.id, sub.categoryName, sub.displayName, sub.categoryDepth, sub.parentCategoryId))
        .from(rec)
        .orderBy(sub.categoryDepth.asc())  // depth 오름차순
        .fetch();
}
```

### 조인 조건 (핵심)

```sql
-- 하위 카테고리 탐색
INNER JOIN category c ON sub.id = c.parentCategoryId

-- 의미: 현재 노드(sub)의 ID가 자식(c)의 parentCategoryId와 같은 것
-- 결과: 기준 카테고리 → 자식 → 손자 → ... 순으로 탐색
```

---

## 📊 쿼리 실행 예시

### 입력: categoryId = 1

```
카테고리 구조:
1 (의류)
├── 10 (상의)
│   ├── 100 (티셔츠)
│   └── 101 (셔츠)
└── 20 (하의)
    └── 200 (바지)
```

### 출력 (depth 오름차순)

| categoryId | categoryName | categoryDepth | parentCategoryId |
|------------|--------------|---------------|------------------|
| 1 | 의류 | 1 | 0 |
| 10 | 상의 | 2 | 1 |
| 20 | 하의 | 2 | 1 |
| 100 | 티셔츠 | 3 | 10 |
| 101 | 셔츠 | 3 | 10 |
| 200 | 바지 | 3 | 20 |

---

## 📝 마이그레이션 노트

### 특이사항

1. **Recursive CTE 사용**
   - `JPASQLQuery` + `SQLExpressions.unionAll` 사용
   - 네이티브 SQL에 가까운 QueryDSL 확장
   - 새 아키텍처에서 동일 패턴 또는 Native Query 고려

2. **Flat List 반환**
   - 트리 구조가 아닌 flat list로 반환
   - `children` 필드는 비어있음
   - 클라이언트에서 트리 구성 필요시 별도 처리

3. **기준 카테고리 포함**
   - 결과에 기준 카테고리 자체도 포함됨
   - 필요시 필터링 로직 추가

### 신규 컴포넌트 매핑

| 레거시 | 신규 |
|--------|------|
| fetchAllChildCategories | CategoryQueryUseCase.findChildCategories() |
| JPASQLQuery (Recursive CTE) | LegacyCategoryCompositeRepository (Native Query) |
| TreeCategoryContext | CategoryTreeResult (Application DTO) |

### 쿼리 이식 옵션

**Option A: Native Query**
```java
@Query(value = """
    WITH RECURSIVE sub AS (
        SELECT * FROM category WHERE category_id = :categoryId
        UNION ALL
        SELECT c.* FROM sub
        INNER JOIN category c ON sub.category_id = c.parent_category_id
    )
    SELECT * FROM sub ORDER BY category_depth ASC
    """, nativeQuery = true)
List<CategoryEntity> findAllChildCategories(@Param("categoryId") Long categoryId);
```

**Option B: QueryDSL JPASQLQuery (레거시와 동일)**
- 기존 코드 그대로 이식 가능
- `SQLTemplates.DEFAULT` 사용 시 DB 종류에 따라 조정 필요
