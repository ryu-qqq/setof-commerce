# API Flow Documentation: CategoryController.fetchAllCategoriesAsTree

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin

---

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/category` |
| Controller | `CategoryController` |
| Service | `CategoryFetchService` → `CategoryFetchServiceImpl` |
| Repository | `CategoryFetchRepository` → `CategoryFetchRepositoryImpl` |
| 인증 | `@PreAuthorize(HAS_ANY_AUTHORITY_MASTER_SELLER)` |

---

## 📥 Request

### Request Parameters

없음 (파라미터 없이 전체 카테고리 조회)

---

## 📤 Response

### Response 구조

```java
ResponseEntity<ApiResponse<List<TreeCategoryContext>>>
```

### TreeCategoryContext 필드

| 필드 | 타입 | 설명 |
|------|------|------|
| categoryId | long | 카테고리 ID |
| categoryName | String | 카테고리명 |
| displayName | String | 노출명 |
| categoryDepth | int | 카테고리 depth (1=루트) |
| parentCategoryId | long | 부모 카테고리 ID |
| children | List | 하위 카테고리 목록 (재귀, 비어있으면 미포함) |

### Response JSON 예시

```json
{
  "success": true,
  "data": [
    {
      "categoryId": 1,
      "categoryName": "의류",
      "displayName": "의류",
      "categoryDepth": 1,
      "parentCategoryId": 0,
      "children": [
        {
          "categoryId": 10,
          "categoryName": "상의",
          "displayName": "상의",
          "categoryDepth": 2,
          "parentCategoryId": 1,
          "children": [
            {
              "categoryId": 100,
              "categoryName": "티셔츠",
              "displayName": "티셔츠",
              "categoryDepth": 3,
              "parentCategoryId": 10
            }
          ]
        }
      ]
    }
  ]
}
```

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────┐
│   CategoryController                                          │
│   fetchAllCategoriesAsTree()                                  │
│   @GetMapping("/category")                                    │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   CategoryFetchServiceImpl                                    │
│   fetchAllCategories()                                        │
│   @Transactional(readOnly = true)                            │
│                                                               │
│   1. categoryFetchRepository.fetchAllCategories(new HashSet<>()) │
│   2. categoryMapper.constructTree(categories)                 │
└────────────────────────┬─────────────────────────────────────┘
                         │
          ┌──────────────┴──────────────┐
          ▼                             ▼
┌─────────────────────────┐   ┌─────────────────────────────┐
│ CategoryFetchRepository │   │ CategoryMapperImpl          │
│ fetchAllCategories()    │   │ constructTree()             │
│                         │   │                             │
│ - 전체 카테고리 flat 조회 │   │ - Map<categoryId, context>  │
│ - categoryIds 빈 Set    │   │ - depth=1 → roots          │
│                         │   │ - else → parent.children    │
└────────────┬────────────┘   └─────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────────────┐
│   Database: category                                          │
│   - SELECT: id, categoryName, displayName,                   │
│             categoryDepth, parentCategoryId                  │
│   - WHERE: id NOT IN (1828)                                  │
└──────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 설명 |
|--------|-------|------|
| category | category | 카테고리 마스터 |

### QueryDSL 코드

```java
// CategoryFetchRepositoryImpl.fetchAllCategories()
queryFactory
    .select(
        new QTreeCategoryContext(
            category.id,
            category.categoryName,
            category.displayName,
            category.categoryDepth,
            category.parentCategoryId
        )
    )
    .from(category)
    .where(exclusiveNonDisplayCategory(), categoryIdIn(categoryIds))
    .fetch();
```

### WHERE 조건

| 조건 | 필드 | 설명 |
|------|------|------|
| exclusiveNonDisplayCategory | category.id | `id NOT IN (1828)` - 비노출 카테고리 제외 |
| categoryIdIn | category.id | categoryIds가 빈 Set이면 조건 없음 (전체 조회) |

### 트리 구성 로직 (CategoryMapperImpl)

```java
// 1. 전체 카테고리를 Map으로 변환
Map<Long, TreeCategoryContext> categoryMap = categories.stream()
    .collect(Collectors.toMap(TreeCategoryContext::getCategoryId, Function.identity()));

// 2. depth=1이면 루트, 아니면 부모의 children에 추가
for (TreeCategoryContext category : categories) {
    if (category.getCategoryDepth() == 1) {
        roots.add(dto);
    } else {
        TreeCategoryContext parent = categoryMap.get(category.getParentCategoryId());
        parent.getChildren().add(dto);
    }
}
```

---

## 🏷️ 관련 Entity

### Category Entity

```java
@Entity
@Table(name = "category")
public class Category extends BaseEntity {
    @Id
    @Column(name = "category_id")
    private long id;

    @Column(name = "CATEGORY_NAME")
    private String categoryName;

    @Column(name = "CATEGORY_DEPTH")
    private int categoryDepth;

    @Column(name = "PARENT_CATEGORY_ID")
    private long parentCategoryId;

    @Column(name = "DISPLAY_NAME")
    private String displayName;

    @Column(name = "DISPLAY_YN")
    @Enumerated(EnumType.STRING)
    private Yn displayYn;

    @Column(name = "TARGET_GROUP")
    @Enumerated(EnumType.STRING)
    private TargetGroup targetGroup;

    @Column(name = "CATEGORY_TYPE")
    @Enumerated(EnumType.STRING)
    private CategoryType categoryType;

    @Column(name = "PATH")
    private String path;
}
```

---

## 📝 마이그레이션 노트

### 특이사항

1. **트리 구성 로직이 Service 레이어에 존재**
   - Repository는 flat list 반환
   - CategoryMapper가 트리 구조로 변환
   - 새 아키텍처에서는 Assembler 또는 Domain Service로 이동 고려

2. **하드코딩된 제외 조건**
   - `id NOT IN (1828)` - 비노출 카테고리 ID 하드코딩
   - 설정 또는 DB 필드로 관리하도록 개선 필요

3. **@JsonInclude(NON_EMPTY)**
   - children이 비어있으면 JSON에서 제외
   - 새 아키텍처에서 동일하게 처리 필요

### 신규 컴포넌트 매핑

| 레거시 | 신규 |
|--------|------|
| CategoryFetchService | CategoryQueryUseCase (Port) |
| CategoryFetchServiceImpl | CategoryQueryService (Application) |
| CategoryFetchRepository | LegacyCategoryCompositeQueryDslRepository |
| CategoryMapper | CategoryAssembler |
| TreeCategoryContext | TreeCategoryResult (Application DTO) |
