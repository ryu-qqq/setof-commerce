# API Flow Documentation: ContentController.getGnbs

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin

---

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/content/gnbs` |
| Controller | `ContentController` |
| Service | `GnbFetchService` → `GnbFetchServiceImpl` |
| Repository | `GnbFetchRepository` → `GnbFetchRepositoryImpl` |
| 권한 | `@PreAuthorize(HAS_AUTHORITY_MASTER)` |

---

## 📥 Request

### Request Parameters (GnbFilter)

| 파라미터 | 타입 | 필수 | 설명 | Validation |
|----------|------|------|------|------------|
| startDate | LocalDateTime | ❌ | 검색 시작일 | @DateTimeFormat |
| endDate | LocalDateTime | ❌ | 검색 종료일 | @DateTimeFormat |
| searchKeyword | SearchKeyword | ❌ | 검색 키워드 타입 | - |
| searchWord | String | ❌ | 검색어 | - |

### Request DTO 구조

```java
// GnbFilter extends SearchAndDateFilter
public class GnbFilter extends SearchAndDateFilter {
    // 부모 클래스 필드만 사용
}

// 부모 클래스 - SearchAndDateFilter
@ValidDateRange(start = "startDate", end = "endDate")
public abstract class SearchAndDateFilter {
    @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    protected LocalDateTime startDate;
    @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    protected LocalDateTime endDate;
    protected SearchKeyword searchKeyword;
    protected String searchWord;
}
```

---

## 📤 Response

### Response DTO 구조

```java
// GnbResponse
public class GnbResponse {
    private long gnbId;           // GNB ID
    private GnbDetails gnbDetails; // GNB 상세 (Embedded)
}

// Embedded - GnbDetails
public class GnbDetails {
    private String title;                // 제목
    private String linkUrl;              // 링크 URL
    private int displayOrder;            // 전시 순서
    private DisplayPeriod displayPeriod; // 전시 기간
    private Yn displayYn;                // 전시 여부
}

// Embedded - DisplayPeriod
public class DisplayPeriod {
    private LocalDateTime displayStartDate;
    private LocalDateTime displayEndDate;
}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": [
    {
      "gnbId": 1,
      "gnbDetails": {
        "title": "홈",
        "linkUrl": "/",
        "displayOrder": 1,
        "displayPeriod": {
          "displayStartDate": "2024-01-01 00:00:00",
          "displayEndDate": "2099-12-31 23:59:59"
        },
        "displayYn": "Y"
      }
    },
    {
      "gnbId": 2,
      "gnbDetails": {
        "title": "카테고리",
        "linkUrl": "/category",
        "displayOrder": 2,
        "displayPeriod": {
          "displayStartDate": "2024-01-01 00:00:00",
          "displayEndDate": "2099-12-31 23:59:59"
        },
        "displayYn": "Y"
      }
    }
  ]
}
```

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────────┐
│   Controller                                                      │
│   ContentController.getGnbs(@ModelAttribute GnbFilter filter)     │
│   @GetMapping("/content/gnbs")                                    │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│   Service                                                         │
│   GnbFetchServiceImpl.fetchGnbs(filter)                           │
│   @Transactional(readOnly = true)                                 │
│                                                                   │
│   → gnbFetchRepository.fetchGnbs(filter)                          │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│   Repository                                                      │
│   GnbFetchRepositoryImpl.fetchGnbs(filter)                        │
│   QueryDSL with GroupBy Transform                                 │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│   Database                                                        │
│   Table: gnb                                                      │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| gnb | gnb | FROM | - |

### QueryDSL 코드

```java
queryFactory.from(gnb)
    .where(
        isBetweenDisplayPeriod(filter.getStartDate(), filter.getEndDate()),
        notDelete()
    )
    .distinct()
    .orderBy(gnb.gnbDetails.displayOrder.asc())
    .transform(
        GroupBy.groupBy(gnb.id).list(
            new QGnbResponse(
                gnb.id,
                gnb.gnbDetails
            )
        )
    );
```

### WHERE 조건

| 조건 | 필드 | 설명 |
|------|------|------|
| isBetweenDisplayPeriod | gnb.gnbDetails.displayPeriod | 전시 기간 범위 |
| notDelete | gnb.deleteYn | 삭제되지 않은 레코드 (N) |

### 기간 검색 로직 (isBetweenDisplayPeriod)

```java
private BooleanExpression isBetweenDisplayPeriod(LocalDateTime startDate, LocalDateTime endDate){
    return gnb.gnbDetails.displayPeriod.displayStartDate.between(startDate, endDate)
            .or(gnb.gnbDetails.displayPeriod.displayEndDate.between(startDate, endDate))
            .or(gnb.gnbDetails.displayPeriod.displayStartDate.loe(startDate)
                .and(gnb.gnbDetails.displayPeriod.displayEndDate.goe(endDate)));
}
```

### 정렬 조건

```java
.orderBy(gnb.gnbDetails.displayOrder.asc())
```

---

## 📦 관련 클래스

### Entity
- `Gnb` - GNB 엔티티 (gnb 테이블)
- `GnbDetails` - GNB 상세 Embedded 객체

### Filter / DTO
- `GnbFilter` - GNB 검색 필터
- `SearchAndDateFilter` - 공통 검색/날짜 필터 (부모 클래스)
- `GnbResponse` - 응답 DTO

---

## 🔗 다음 단계

```bash
# DTO 변환
/legacy-convert admin:ContentController.getGnbs

# Persistence Layer 생성
/legacy-query admin:ContentController.getGnbs
```
