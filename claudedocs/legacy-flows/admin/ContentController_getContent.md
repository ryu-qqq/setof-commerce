# API Flow Documentation: ContentController.getContent

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin

---

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/content/{contentId}` |
| Controller | `ContentController` |
| Service | `ContentFetchService` → `ContentFetchServiceImpl` |
| Repository | `ContentFetchRepository` → `ContentFetchRepositoryImpl` |
| 권한 | `@PreAuthorize(HAS_AUTHORITY_MASTER)` |

---

## 📥 Request

### Request Parameters

| 파라미터 | 타입 | 필수 | 설명 | 어노테이션 |
|----------|------|------|------|------------|
| contentId | long | ✅ | 콘텐츠 ID | @PathVariable |

---

## 📤 Response

### Response DTO 구조

```java
public class ContentGroupResponse {
    private long contentId;              // 콘텐츠 ID
    private DisplayPeriod displayPeriod; // 전시 기간 (Embedded)
    private String title;                // 제목
    private String memo;                 // 메모
    private String imageUrl;             // 이미지 URL
    private Yn displayYn;                // 전시 여부 (Y/N)
    private List<SubComponent> components; // 컴포넌트 목록
}

// Embedded - DisplayPeriod
public class DisplayPeriod {
    private LocalDateTime displayStartDate;  // 전시 시작일시
    private LocalDateTime displayEndDate;    // 전시 종료일시
}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": {
    "contentId": 1,
    "displayPeriod": {
      "displayStartDate": "2024-01-01 00:00:00",
      "displayEndDate": "2024-12-31 23:59:59"
    },
    "title": "메인 콘텐츠",
    "memo": "관리자 메모",
    "imageUrl": "https://cdn.example.com/image.jpg",
    "displayYn": "Y",
    "components": [
      {
        "componentType": "BANNER",
        "componentName": "메인 배너",
        "displayOrder": 1
      }
    ]
  }
}
```

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────────┐
│   Controller                                                      │
│   ContentController.getContent(@PathVariable long contentId)      │
│   @GetMapping("/content/{contentId}")                             │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│   Service                                                         │
│   ContentFetchServiceImpl.fetchContent(contentId)                 │
│   @Transactional(readOnly = true)                                 │
│                                                                   │
│   1. contentFetchRepository.fetchContentQueryInfo(contentId)      │
│   2. 컴포넌트 분류 (Product/NonProduct)                            │
│   3. notRelatedProductComponentRepository.fetchNonRelatedProduct..│
│   4. relatedProductComponentRepository.fetchRelatedProductComponent│
│   5. contentMapper.toContentGroupResponse()                       │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│   Repository                                                      │
│   ContentFetchRepositoryImpl.fetchContentQueryInfo(contentId)     │
│   QueryDSL with GroupBy Transform                                 │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│   Database                                                        │
│   Tables: content, component, view_extension                      │
│   JOINs: LEFT JOIN component, LEFT JOIN view_extension            │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| content | content | FROM | - |
| component | component | LEFT JOIN | component.contentId = content.id AND component.deleteYn = 'N' |
| view_extension | viewExtension | LEFT JOIN | viewExtension.id = component.viewExtension.id |

### QueryDSL 코드

```java
getQueryFactory()
    .select(content.id,
            content.title.coalesce(""),
            content.memo.coalesce(""),
            content.displayYn,
            content.displayPeriod,
            content.imageUrl.coalesce(""),
            component.id,
            component.componentName.coalesce(""),
            component.exposedProducts,
            component.componentDetails,
            component.displayOrder,
            viewExtension.viewExtensionDetails.viewExtensionType,
            viewExtension.viewExtensionDetails)
    .from(content)
    .leftJoin(component)
        .on(component.contentId.eq(content.id))
        .on(component.deleteYn.eq(Yn.N))
    .leftJoin(viewExtension)
        .on(viewExtension.id.eq(component.viewExtension.id))
    .where(contentIdEq(contentId), deleteYnEq())
    .transform(
        GroupBy.groupBy(content.id).as(
            new QContentQueryDto(...)
        )
    )
```

### WHERE 조건

| 조건 | 필드 | 설명 |
|------|------|------|
| contentIdEq | content.id | 콘텐츠 ID 일치 |
| deleteYnEq | content.deleteYn | 삭제되지 않은 콘텐츠 (N) |
| deleteYnEq | component.deleteYn | 삭제되지 않은 컴포넌트 (N) |

---

## 📦 관련 클래스

### Entity
- `Content` - 콘텐츠 엔티티 (content 테이블)
- `Component` - 컴포넌트 엔티티 (component 테이블)
- `ViewExtension` - 뷰 확장 엔티티 (view_extension 테이블)

### DTO
- `ContentQueryDto` - 쿼리 결과 매핑용 DTO
- `ComponentQueryDto` - 컴포넌트 쿼리 DTO
- `ContentGroupResponse` - 최종 응답 DTO

### Mapper
- `ContentMapper.toContentGroupResponse()` - DTO 변환

---

## 🔗 다음 단계

```bash
# DTO 변환
/legacy-convert admin:ContentController.getContent

# Persistence Layer 생성
/legacy-query admin:ContentController.getContent
```
