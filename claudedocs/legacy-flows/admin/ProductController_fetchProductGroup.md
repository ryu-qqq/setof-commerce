# API Flow Documentation: ProductController.fetchProductGroup (단건)

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin

---

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/product/group/{productGroupId}` |
| Controller | `ProductController` |
| Service | `ProductGroupFetchService` → `ProductGroupFetchServiceImpl` |
| Repository | `ProductGroupFetchRepository` → `ProductGroupFetchRepositoryImpl` |
| 권한 | `HAS_ANY_AUTHORITY_MASTER_SELLER` |

---

## 📥 Request

### Request Parameters

| 파라미터 | 타입 | 필수 | 설명 | Validation |
|----------|------|------|------|------------|
| productGroupId | long | ✅ | 상품그룹 ID | @PathVariable |

### Request 예시

```http
GET /api/v1/product/group/12345
```

---

## 📤 Response

### Response DTO 구조

```java
public class ProductGroupFetchResponse extends ProductGroupDetailResponse {
    // 상속: ProductGroupInfo productGroup
    // 상속: Set<ProductFetchResponse> products

    private ProductNoticeDto productNotices;        // 상품 고시정보
    private List<ProductImageDto> productGroupImages; // 상품 이미지 목록
    private String detailDescription;               // 상세 설명 이미지 URL
    private List<TreeCategoryContext> categories;   // 카테고리 경로 (Service에서 세팅)
}
```

### ProductGroupInfo 구조

```java
public class ProductGroupInfo {
    private long productGroupId;
    private String productGroupName;
    private long sellerId;
    private String sellerName;
    private long categoryId;
    private OptionType optionType;
    private ManagementType managementType;
    private BaseBrandContext brand;           // { id, brandName }
    private Price price;                      // 가격 정보 (embedded)
    private ClothesDetail clothesDetailInfo;  // 의류 상세정보
    private DeliveryNotice deliveryNotice;    // 배송 안내 (embedded)
    private RefundNotice refundNotice;        // 환불 안내 (embedded)
    private String productGroupMainImageUrl;
    private ProductStatus productStatus;      // { soldOutYn, displayYn, sellStartDate, sellEndDate }
    private LocalDateTime insertDate;
    private LocalDateTime updateDate;
    private String insertOperator;
    private String updateOperator;
    private long crawlProductSku;
    private String externalProductUuId;
    private CrawlProductDto crawlProductInfo;
    private Set<ExternalProductDto> externalProductInfos;
}
```

### 중첩 DTO 구조

```java
// 상품 고시정보
public class ProductNoticeDto {
    private String material;
    private String color;
    private String size;
    private String maker;
    private Origin origin;
    private String washingMethod;
    private String yearMonth;
    private String assuranceStandard;
    private String asPhone;
}

// 상품 이미지
public class ProductImageDto {
    private ProductGroupImageType type;  // MAIN, SUB, DETAIL 등
    private String productImageUrl;
}

// 개별 상품 (옵션별)
public class ProductFetchResponse {
    private long productId;
    private int stockQuantity;
    private ProductStatus productStatus;
    private String option;
    private Set<OptionDto> options;
    private BigDecimal additionalPrice;
}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": {
    "productGroup": {
      "productGroupId": 12345,
      "productGroupName": "프리미엄 티셔츠",
      "sellerId": 100,
      "sellerName": "브랜드샵",
      "categoryId": 500,
      "optionType": "COMBINATION",
      "managementType": "NORMAL",
      "brand": {
        "id": 50,
        "brandName": "나이키"
      },
      "price": {
        "regularPrice": 50000,
        "salePrice": 39000,
        "discountRate": 22
      },
      "productStatus": {
        "soldOutYn": "N",
        "displayYn": "Y"
      },
      "insertDate": "2024-01-15 10:30:00",
      "updateDate": "2024-01-20 14:20:00"
    },
    "productNotices": {
      "material": "면 100%",
      "color": "블랙, 화이트, 네이비",
      "size": "S, M, L, XL",
      "maker": "나이키코리아",
      "origin": "KOREA"
    },
    "productGroupImages": [
      { "type": "MAIN", "productImageUrl": "https://..." },
      { "type": "SUB", "productImageUrl": "https://..." }
    ],
    "detailDescription": "https://cdn.example.com/detail/12345.html",
    "categories": [
      { "categoryId": 1, "categoryName": "패션" },
      { "categoryId": 100, "categoryName": "남성의류" },
      { "categoryId": 500, "categoryName": "티셔츠" }
    ],
    "products": [
      {
        "productId": 10001,
        "stockQuantity": 50,
        "productStatus": { "soldOutYn": "N", "displayYn": "Y" },
        "option": "블랙/M",
        "additionalPrice": 0
      }
    ]
  }
}
```

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────┐
│   ProductController                                          │
│   fetchProductGroup(@PathVariable long productGroupId)       │
│   @GetMapping("/product/group/{productGroupId}")             │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   ProductGroupFetchServiceImpl                               │
│   fetchProductGroup(long productGroupId)                     │
│   @Transactional(readOnly = true)                            │
│                                                              │
│   1. productGroupFetchRepository.fetchProductGroup()         │
│   2. setCategories() → CategoryFetchService                  │
│   3. setProducts() → ProductFetchService                     │
└────────────────────────┬─────────────────────────────────────┘
                         │
          ┌──────────────┼──────────────┐
          ▼              ▼              ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│ ProductGroup    │ │ CategoryFetch   │ │ ProductFetch    │
│ FetchRepository │ │ Service         │ │ Service         │
│ (Main Query)    │ │ (Category Path) │ │ (Products)      │
└────────┬────────┘ └────────┬────────┘ └────────┬────────┘
         │                   │                   │
         ▼                   ▼                   ▼
┌──────────────────────────────────────────────────────────────┐
│   Database                                                   │
│   Tables: product_group, brand, seller, product_notice,      │
│           product_delivery, product_group_image,             │
│           product_group_detail_description, crawl_product,   │
│           external_product, category, product                │
└──────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Query 분석

### 조회 테이블 (Main Query)

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| product_group | productGroup | FROM | - |
| brand | brand | LEFT JOIN | `brand.id = productGroup.brandId` |
| seller | seller | LEFT JOIN | `seller.id = productGroup.sellerId` |
| product_notice | productNotice | LEFT JOIN | `productGroup.productNotice` (OneToOne) |
| product_delivery | productDelivery | LEFT JOIN | `productGroup.productDelivery` (OneToOne) |
| product_group_image | productGroupImage | LEFT JOIN | `productGroup.images` (OneToMany) + `deleteYn = 'N'` |
| product_group_detail_description | productGroupDetailDescription | INNER JOIN | `productGroup.detailDescription` |
| crawl_product | crawlProduct | LEFT JOIN | `crawlProduct.productGroupId = productGroup.id` |
| external_product | externalProduct | LEFT JOIN | `externalProduct.productGroupId = productGroup.id` |

### QueryDSL 코드 (핵심)

```java
getQueryFactory()
    .from(productGroup)
    .leftJoin(brand).on(brand.id.eq(productGroup.productGroupDetails.brandId))
    .leftJoin(seller).on(seller.id.eq(productGroup.productGroupDetails.sellerId))
    .leftJoin(productGroup.productNotice, productNotice)
    .leftJoin(productGroup.productDelivery, productDelivery)
    .leftJoin(productGroup.images, productGroupImage)
        .on(productGroupImage.deleteYn.eq(Yn.N))
    .innerJoin(productGroup.detailDescription, productGroupDetailDescription)
    .leftJoin(crawlProduct).on(crawlProduct.productGroupId.eq(productGroup.id))
    .leftJoin(externalProduct).on(externalProduct.productGroupId.eq(productGroup.id))
    .distinct()
    .where(productGroupIdEq(productGroupId), sellerIdEq(sellerId))
    .transform(GroupBy.groupBy(productGroup.id).as(
        new QProductGroupFetchResponse(...)
    ))
```

### WHERE 조건

| 조건 | 필드 | 설명 |
|------|------|------|
| productGroupIdEq | `productGroup.id` | 상품그룹 ID 일치 |
| sellerIdEq | `productGroup.sellerId` | 판매자 ID 일치 (권한 체크) |

---

## 📝 특이사항

1. **GroupBy Transform 사용**: 다대일/일대다 관계 조인 결과를 단일 DTO로 변환
2. **권한 체크**: `SecurityUtils.currentSellerIdOpt()`로 현재 로그인한 판매자 ID 확인
3. **추가 조회**:
   - `CategoryFetchService.fetchAllParentCategories()`: 카테고리 경로 조회
   - `ProductFetchService.fetchProducts()`: 개별 상품 (SKU) 목록 조회
4. **외부 연동 정보 포함**: `crawlProduct`, `externalProduct` 정보 포함

---

## 🔗 다음 단계

```bash
# DTO 변환
/legacy-convert admin:ProductController.fetchProductGroup

# Persistence Layer 생성
/legacy-query admin:ProductController.fetchProductGroup
```
