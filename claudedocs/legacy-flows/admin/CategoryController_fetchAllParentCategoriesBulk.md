# API Flow Documentation: CategoryController.fetchAllParentCategories (Bulk)

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin

---

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/category/parents` |
| Controller | `CategoryController` |
| Service | `CategoryFetchService` → `CategoryFetchServiceImpl` |
| Repository | `CategoryFetchRepository` → `CategoryFetchRepositoryImpl` |
| 인증 | `@PreAuthorize(HAS_ANY_AUTHORITY_MASTER_SELLER)` |

---

## 📥 Request

### Request Parameters

| 파라미터 | 타입 | 필수 | 설명 | 바인딩 |
|----------|------|------|------|--------|
| categoryIds | List<Long> | ✅ | 조회할 카테고리 ID 목록 | @RequestParam |

### 요청 예시

```
GET /api/v1/category/parents?categoryIds=100,200,300
```

---

## 📤 Response

### Response 구조

```java
ResponseEntity<ApiResponse<List<TreeCategoryContext>>>
```

### TreeCategoryContext 필드

| 필드 | 타입 | 설명 |
|------|------|------|
| categoryId | long | 카테고리 ID |
| categoryName | String | 카테고리명 |
| displayName | String | 노출명 |
| categoryDepth | int | 카테고리 depth |
| parentCategoryId | long | 부모 카테고리 ID |
| children | List | 하위 카테고리 (이 API에서는 비어있음) |

### Response JSON 예시

```json
{
  "success": true,
  "data": [
    {
      "categoryId": 100,
      "categoryName": "티셔츠",
      "displayName": "티셔츠",
      "categoryDepth": 3,
      "parentCategoryId": 10
    },
    {
      "categoryId": 200,
      "categoryName": "바지",
      "displayName": "바지",
      "categoryDepth": 3,
      "parentCategoryId": 20
    },
    {
      "categoryId": 300,
      "categoryName": "운동화",
      "displayName": "운동화",
      "categoryDepth": 3,
      "parentCategoryId": 30
    }
  ]
}
```

> ⚠️ **주의**: 이 API는 상위 카테고리를 조회하지 않고, **요청한 카테고리 목록만** flat하게 반환합니다.

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────┐
│   CategoryController                                          │
│   fetchAllParentCategories(@RequestParam List<Long> ids)      │
│   @GetMapping("/category/parents")                           │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   CategoryFetchServiceImpl                                    │
│   fetchAllCategories(Set<Long> categoryIds)                  │
│   @Transactional(readOnly = true)                            │
│                                                               │
│   ⚠️ 메서드명과 다르게 fetchAllCategories() 호출             │
│   → 상위 카테고리 재귀 탐색 없음!                             │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   CategoryFetchRepositoryImpl                                 │
│   fetchAllCategories(Set<Long> categoryIds)                  │
│                                                               │
│   - 단순 IN 조건 조회                                         │
│   - Recursive CTE 사용 안함                                   │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Database: category                                          │
│                                                               │
│   SELECT id, categoryName, displayName,                      │
│          categoryDepth, parentCategoryId                     │
│   FROM category                                               │
│   WHERE id NOT IN (1828)                                     │
│     AND id IN (:categoryIds)                                 │
└──────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 설명 |
|--------|-------|------|
| category | category | 카테고리 마스터 |

### QueryDSL 코드

```java
// CategoryFetchRepositoryImpl.fetchAllCategories()
queryFactory
    .select(
        new QTreeCategoryContext(
            category.id,
            category.categoryName,
            category.displayName,
            category.categoryDepth,
            category.parentCategoryId
        )
    )
    .from(category)
    .where(exclusiveNonDisplayCategory(), categoryIdIn(categoryIds))
    .fetch();
```

### WHERE 조건

| 조건 | 메서드 | 로직 |
|------|--------|------|
| exclusiveNonDisplayCategory | `id.notIn(1828)` | 비노출 카테고리 제외 |
| categoryIdIn | `id.in(categoryIds)` | 요청한 ID만 조회 |

---

## 📊 쿼리 실행 예시

### 입력: categoryIds = [100, 200, 300]

### 출력

| categoryId | categoryName | categoryDepth | parentCategoryId |
|------------|--------------|---------------|------------------|
| 100 | 티셔츠 | 3 | 10 |
| 200 | 바지 | 3 | 20 |
| 300 | 운동화 | 3 | 30 |

> ⚠️ 상위 카테고리 (10, 20, 30, 1 등)는 포함되지 않음!

---

## 📝 마이그레이션 노트

### ⚠️ 주의: API 명칭과 동작 불일치

**API 경로**: `/api/v1/category/parents`
**메서드명**: `fetchAllParentCategories`
**실제 동작**: 요청한 카테고리 ID만 조회 (상위 카테고리 탐색 없음)

```java
// Controller
@GetMapping("/category/parents")
public ResponseEntity<ApiResponse<List<TreeCategoryContext>>> fetchAllParentCategories(
        @RequestParam List<Long> categoryIds) {
    // ⚠️ fetchAllParentCategories가 아닌 fetchAllCategories 호출!
    return ResponseEntity.ok(ApiResponse.success(
        categoryFetchService.fetchAllCategories(new HashSet<>(categoryIds))
    ));
}
```

### 의도 추정

1. **의도된 동작**: 여러 카테고리의 상위 카테고리를 일괄 조회
2. **실제 동작**: 요청한 카테고리만 조회 (부모 탐색 없음)
3. **가능한 사유**: 구현 미완료 또는 네이밍 실수

### 신규 아키텍처에서의 결정 필요

| 옵션 | 설명 |
|------|------|
| A | 현재 동작 유지 (ID 목록으로 카테고리 조회) |
| B | API 의도대로 수정 (다건 상위 카테고리 조회) |

**Option B 구현 시**:
```java
// 각 categoryId에 대해 상위 카테고리 조회 후 merge
public List<TreeCategoryContext> fetchAllParentCategoriesBulk(Set<Long> categoryIds) {
    return categoryIds.stream()
        .flatMap(id -> fetchAllParentCategories(id).stream())
        .distinct()
        .toList();
}
```

### 신규 컴포넌트 매핑

| 레거시 | 신규 |
|--------|------|
| fetchAllParentCategories (Bulk) | CategoryQueryUseCase.findCategoriesByIds() |
| TreeCategoryContext | CategoryResult (Application DTO) |

### 사용처 분석 필요

- 이 API를 호출하는 클라이언트 확인
- 실제로 상위 카테고리가 필요한지 검증
- 필요시 별도 API 신설 또는 기존 API 수정
