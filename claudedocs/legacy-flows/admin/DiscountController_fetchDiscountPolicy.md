# API Flow Documentation: DiscountController.fetchDiscountPolicy

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api-admin

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/discount/{discountPolicyId}` |
| Controller | `DiscountController` |
| Service | `DiscountFetchService` → `DiscountFetchServiceImpl` |
| Repository | `DiscountPolicyFetchRepository` → `DiscountPolicyFetchRepositoryImpl` |
| 보안 | `@PreAuthorize(HAS_AUTHORITY_MASTER)` |

---

## 📥 Request

### Request Parameters

| 파라미터 | 타입 | 필수 | 설명 | Validation |
|----------|------|------|------|------------|
| discountPolicyId | long | ✅ | 할인 정책 ID | @PathVariable |

---

## 📤 Response

### Response DTO 구조

```java
public class DiscountPolicyResponseDto {
    private long discountPolicyId;
    private DiscountDetails discountDetails;  // Embedded
    private LocalDateTime insertDate;
    private LocalDateTime updateDate;
    private String insertOperator;
    private String updateOperator;
}
```

### DiscountDetails (Embedded) 구조

```java
@Embeddable
public class DiscountDetails {
    private String discountPolicyName;       // 정책명
    private DiscountType discountType;       // 할인 유형 (Enum)
    private PublisherType publisherType;     // 발행자 유형 (Enum)
    private IssueType issueType;             // 적용 대상 유형 (PRODUCT/SELLER)
    private Yn discountLimitYn;              // 할인 한도 적용 여부
    private long maxDiscountPrice;           // 최대 할인 금액
    private Yn shareYn;                      // 분담 여부
    private double shareRatio;               // 분담 비율
    private double discountRatio;            // 할인율
    private LocalDateTime policyStartDate;   // 정책 시작일
    private LocalDateTime policyEndDate;     // 정책 종료일
    private String memo;                     // 메모
    private int priority;                    // 우선순위
    private Yn activeYn;                     // 활성화 여부
}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": {
    "discountPolicyId": 1,
    "discountDetails": {
      "discountPolicyName": "신규 회원 할인",
      "discountType": "RATIO",
      "publisherType": "PLATFORM",
      "issueType": "PRODUCT",
      "discountLimitYn": "Y",
      "maxDiscountPrice": 10000,
      "shareYn": "Y",
      "shareRatio": 0.5,
      "discountRatio": 0.1,
      "policyStartDate": "2026-01-01 00:00:00",
      "policyEndDate": "2026-12-31 23:59:59",
      "memo": "신규 회원 대상 10% 할인",
      "priority": 1,
      "activeYn": "Y"
    },
    "insertDate": "2026-01-01 10:00:00",
    "updateDate": "2026-01-15 14:30:00",
    "insertOperator": "admin",
    "updateOperator": "admin"
  }
}
```

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────┐
│   Controller                                                  │
│   DiscountController.fetchDiscountPolicy(discountPolicyId)   │
│   @GetMapping("/discount/{discountPolicyId}")                │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Service                                                     │
│   DiscountFetchServiceImpl.fetchDiscountPolicy(id)           │
│   @Transactional(readOnly = true)                            │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Repository                                                  │
│   DiscountPolicyFetchRepositoryImpl.fetchDiscountPolicy(id)  │
│   QueryDSL + GroupBy Transform                               │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Database                                                    │
│   Table: discount_policy                                     │
└──────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| discount_policy | discountPolicy | FROM | - |

### QueryDSL 코드

```java
getQueryFactory().from(discountPolicy)
    .where(discountPolicyIdEq(discountPolicyId))
    .transform(
        GroupBy.groupBy(discountPolicy.id).as(
            new QDiscountPolicyResponseDto(
                discountPolicy.id,
                discountPolicy.discountDetails,
                discountPolicy.insertDate,
                discountPolicy.updateDate,
                discountPolicy.insertOperator,
                discountPolicy.updateOperator
            )
        )
    ).get(discountPolicyId);
```

### WHERE 조건

| 조건 | 필드 | 설명 |
|------|------|------|
| discountPolicyIdEq | discountPolicy.id | 정책 ID 일치 |

---

## 🔗 다음 단계

```bash
# DTO 변환
/legacy-convert admin:DiscountController.fetchDiscountPolicy

# Persistence Layer 생성
/legacy-query admin:DiscountController.fetchDiscountPolicy
```
