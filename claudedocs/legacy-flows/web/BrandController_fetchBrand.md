# API Flow Documentation: BrandController.fetchBrand

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api

---

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/brand/{brandId}` |
| Controller | `BrandController` |
| Service | `BrandFindService` → `BrandFindServiceImpl` |
| Repository | `BrandFindRepository` → `BrandFindRepositoryImpl` |

---

## 📥 Request

### Request Parameters

| 파라미터 | 타입 | 필수 | 설명 | Validation |
|----------|------|------|------|------------|
| brandId | long | ✅ | 브랜드 ID (Path Variable) | - |

### Request 예시

```http
GET /api/v1/brand/123
```

---

## 📤 Response

### Response DTO 구조

```java
// BrandDisplayDto.java
public class BrandDisplayDto {
    private final long brandId;           // 브랜드 ID
    private final String brandName;       // 브랜드명 (영문 코드)
    private final String korBrandName;    // 브랜드명 (한글)
    private final String brandIconImageUrl; // 브랜드 아이콘 이미지 URL

    @QueryProjection
    public BrandDisplayDto(
            long brandId,
            String brandName,
            String korBrandName,
            String brandIconImageUrl) {
        this.brandId = brandId;
        this.brandName = brandName;
        this.korBrandName = korBrandName;
        this.brandIconImageUrl = brandIconImageUrl;
    }
}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": {
    "brandId": 123,
    "brandName": "NIKE",
    "korBrandName": "나이키",
    "brandIconImageUrl": "https://cdn.example.com/brands/nike.png"
  }
}
```

### Error Response (404)

```json
{
  "success": false,
  "error": {
    "code": "BRAND_NOT_FOUND",
    "message": "Brand not found with id: 123"
  }
}
```

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────┐
│   Controller                                                  │
│   BrandController.fetchBrand(@PathVariable long brandId)     │
│   @GetMapping("/brand/{brandId}")                            │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Service                                                     │
│   BrandFindServiceImpl.fetchBrand(long brandId)              │
│   @Transactional(readOnly = true)                            │
│                                                               │
│   비즈니스 로직:                                               │
│   - Repository에서 브랜드 조회                                 │
│   - Optional.empty() → BrandNotFoundException 발생            │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Repository                                                  │
│   BrandFindRepositoryImpl.fetchBrand(long brandId)           │
│   - Optional<BrandDisplayDto> 반환                            │
│   - fetchFirst() 사용 (단건 조회)                             │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Database                                                    │
│   Table: brand                                                │
│   WHERE: brand_id = ?                                         │
└──────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| brand | brand | FROM | - |

### QueryDSL 코드

```java
@Override
public Optional<BrandDisplayDto> fetchBrand(long brandId) {
    return Optional.ofNullable(
            queryFactory
                    .select(
                            new QBrandDisplayDto(
                                    brand.id,
                                    brand.brandName,
                                    brand.displayKoreanName,
                                    brand.brandIconImageUrl))
                    .from(brand)
                    .where(brandIdEq(brandId))
                    .fetchFirst());
}

private BooleanExpression brandIdEq(long brandId) {
    return brand.id.eq(brandId);
}
```

### WHERE 조건

| 조건 메서드 | 필드 | 설명 |
|------------|------|------|
| brandIdEq | brand.id | 브랜드 ID 일치 |

### 생성되는 SQL (예상)

```sql
SELECT
    b.brand_id,
    b.brand_name,
    b.display_korean_name,
    b.brand_icon_image_url
FROM brand b
WHERE b.brand_id = ?
LIMIT 1;
```

---

## ⚠️ 예외 처리

### BrandNotFoundException

```java
// Service에서 발생
@Override
public BrandDisplayDto fetchBrand(long brandId) {
    return brandFindRepository
            .fetchBrand(brandId)
            .orElseThrow(() -> new BrandNotFoundException(brandId));
}
```

| 예외 | 발생 조건 | HTTP Status |
|------|----------|-------------|
| BrandNotFoundException | 해당 brandId의 브랜드가 없을 때 | 404 |

---

## 📁 소스 파일 경로

| 구분 | 파일 경로 |
|------|----------|
| Controller | `bootstrap/bootstrap-legacy-web-api/src/main/java/com/setof/connectly/module/brand/controller/BrandController.java` |
| Service Interface | `bootstrap/bootstrap-legacy-web-api/src/main/java/com/setof/connectly/module/brand/service/BrandFindService.java` |
| Service Impl | `bootstrap/bootstrap-legacy-web-api/src/main/java/com/setof/connectly/module/brand/service/BrandFindServiceImpl.java` |
| Repository Interface | `bootstrap/bootstrap-legacy-web-api/src/main/java/com/setof/connectly/module/brand/repository/BrandFindRepository.java` |
| Repository Impl | `bootstrap/bootstrap-legacy-web-api/src/main/java/com/setof/connectly/module/brand/repository/BrandFindRepositoryImpl.java` |
| Response DTO | `bootstrap/bootstrap-legacy-web-api/src/main/java/com/setof/connectly/module/brand/dto/BrandDisplayDto.java` |
| Entity | `bootstrap/bootstrap-legacy-web-api/src/main/java/com/setof/connectly/module/brand/entity/Brand.java` |
| Exception | `bootstrap/bootstrap-legacy-web-api/src/main/java/com/setof/connectly/module/exception/brand/BrandNotFoundException.java` |

---

## 🔗 다음 단계

### DTO 변환
```bash
/legacy-convert web:BrandController.fetchBrand
```

### Persistence Layer 생성
```bash
/legacy-query web:BrandController.fetchBrand
```
