# API Flow Documentation: SellerController.fetchSeller

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api

---

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/seller/{sellerId}` |
| Controller | `SellerController` |
| Service | `SellerFindService` → `SellerFindServiceImpl` |
| Repository | `SellerFindRepository` → `SellerFindRepositoryImpl` |
| Cache | `SellerRedisFindService` → `SellerRedisFindServiceImpl` |

---

## 📥 Request

### Request Parameters

| 파라미터 | 타입 | 필수 | 설명 | 바인딩 |
|----------|------|------|------|--------|
| sellerId | long | ✅ | 판매자 ID | @PathVariable |

---

## 📤 Response

### Response DTO 구조

```java
// SellerInfo.java
@Getter
@NoArgsConstructor(access = AccessLevel.PROTECTED)
public class SellerInfo {
    private long sellerId;
    private String sellerName;
    private String logoUrl;
    private String sellerDescription;
    private String address;
    private String csPhoneNumber;
    private String alimTalkPhoneNumber;
    private String registrationNumber;
    private String saleReportNumber;
    private String representative;
    private String email;

    @QueryProjection
    public SellerInfo(long sellerId, String sellerName, String logoUrl,
                      String sellerDescription, String address, String csPhoneNumber,
                      String alimTalkPhoneNumber, String registrationNumber,
                      String saleReportNumber, String representative, String email) {
        // 전체 필드 생성자
    }

    @QueryProjection
    public SellerInfo(long sellerId, String sellerName, String logoUrl,
                      String sellerDescription) {
        // 간략 생성자 (4개 필드만)
    }
}
```

### Response 필드 매핑

| DTO 필드 | DB 소스 | 설명 |
|----------|---------|------|
| sellerId | seller.id | 판매자 ID |
| sellerName | seller_business_info.company_name | 회사명 |
| logoUrl | seller.seller_logo_url | 로고 URL |
| sellerDescription | seller.seller_description (COALESCE "") | 판매자 설명 |
| address | 조합: business_address_line1 + line2 + zipcode | 주소 |
| csPhoneNumber | seller_business_info.cs_number COALESCE cs_phone_number | CS 전화번호 |
| alimTalkPhoneNumber | seller_business_info.cs_phone_number | 알림톡 전화번호 |
| registrationNumber | seller_business_info.registration_number | 사업자등록번호 |
| saleReportNumber | seller_business_info.sale_report_number | 통신판매업 신고번호 |
| representative | seller_business_info.representative | 대표자명 |
| email | seller_business_info.cs_email | 이메일 |

### Response JSON 예시

```json
{
  "success": true,
  "data": {
    "sellerId": 123,
    "sellerName": "테스트 회사",
    "logoUrl": "https://example.com/logo.png",
    "sellerDescription": "테스트 판매자입니다",
    "address": "서울시 강남구 테헤란로 123 12345",
    "csPhoneNumber": "02-1234-5678",
    "alimTalkPhoneNumber": "010-1234-5678",
    "registrationNumber": "123-45-67890",
    "saleReportNumber": "2023-서울강남-0001",
    "representative": "홍길동",
    "email": "cs@example.com"
  }
}
```

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────────┐
│   Controller                                                      │
│   SellerController.fetchSeller(@PathVariable sellerId)           │
│   @GetMapping("/{sellerId}")                                     │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│   Service                                                         │
│   SellerFindServiceImpl.fetchSeller(sellerId)                    │
│   @Transactional(readOnly = true)                                │
│                                                                   │
│   ┌─────────────────────────────────────────────────────────┐    │
│   │  1. Redis 캐시 조회 (선행)                                │    │
│   │     sellerRedisFindService.fetchSellerInRedis(sellerId) │    │
│   │     - Key: SELLER:{sellerId}                             │    │
│   │     - Hit → 바로 반환                                     │    │
│   └─────────────────────────────────────────────────────────┘    │
│                         │                                         │
│                    Cache Miss                                     │
│                         ▼                                         │
│   ┌─────────────────────────────────────────────────────────┐    │
│   │  2. DB 조회 (폴백)                                        │    │
│   │     sellerFindRepository.fetchSeller(sellerId)           │    │
│   │     - 조회 후 Redis 저장                                  │    │
│   │     sellerRedisQueryService.saveSellerInRedis(sellerInfo)│    │
│   └─────────────────────────────────────────────────────────┘    │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│   Repository                                                      │
│   SellerFindRepositoryImpl.fetchSeller(sellerId)                 │
│   QueryDSL                                                        │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│   Database                                                        │
│   Tables: seller, seller_business_info                           │
│   JOIN: seller INNER JOIN seller_business_info ON id             │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| seller | seller | FROM | - |
| seller_business_info | sellerBusinessInfo | INNER JOIN | sellerBusinessInfo.id = seller.id |

### Entity 정보

#### seller 테이블
| 컬럼 | 필드명 | 타입 | 설명 |
|------|--------|------|------|
| seller_id | id | Long (PK) | 판매자 ID |
| seller_name | sellerName | String | 판매자명 |
| seller_logo_url | sellerLogoUrl | String | 로고 URL |
| seller_description | sellerDescription | String | 판매자 설명 |
| commission_rate | commissionRate | double | 수수료율 |
| privacy_agreement_yn | privacyAgreementYn | Yn | 개인정보 동의 |
| terms_use_agreement_yn | termsUseAgreementYn | Yn | 이용약관 동의 |

#### seller_business_info 테이블
| 컬럼 | 필드명 | 타입 | 설명 |
|------|--------|------|------|
| seller_id | id | Long (PK, FK) | 판매자 ID |
| registration_number | registrationNumber | String | 사업자등록번호 |
| company_name | companyName | String | 회사명 |
| business_address_line1 | businessAddressLine1 | String | 주소1 |
| business_address_line2 | businessAddressLine2 | String | 주소2 |
| business_address_zip_code | businessAddressZipCode | String | 우편번호 |
| bank_name | bankName | String | 은행명 |
| account_number | accountNumber | String | 계좌번호 |
| account_holder_name | accountHolderName | String | 예금주 |
| cs_number | csNumber | String | CS 번호 |
| cs_phone_number | csPhoneNumber | String | CS 전화번호 |
| cs_email | csEmail | String | CS 이메일 |
| sale_report_number | saleReportNumber | String | 통신판매업 신고번호 |
| representative | representative | String | 대표자명 |

### QueryDSL 코드

```java
// SellerFindRepositoryImpl.java:24-50
queryFactory
    .select(
        new QSellerInfo(
            seller.id,
            sellerBusinessInfo.companyName,
            seller.sellerLogoUrl,
            seller.sellerDescription.coalesce(""),
            sellerBusinessInfo.businessAddressLine1
                .concat(" ")
                .append(sellerBusinessInfo.businessAddressLine2)
                .concat(" ")
                .append(sellerBusinessInfo.businessAddressZipCode),
            sellerBusinessInfo.csNumber.coalesce(sellerBusinessInfo.csPhoneNumber),
            sellerBusinessInfo.csPhoneNumber,
            sellerBusinessInfo.registrationNumber,
            sellerBusinessInfo.saleReportNumber,
            sellerBusinessInfo.representative,
            sellerBusinessInfo.csEmail))
    .from(seller)
    .innerJoin(sellerBusinessInfo).on(sellerBusinessInfo.id.eq(seller.id))
    .where(sellerIdEq(sellerId))
    .fetchFirst()
```

### WHERE 조건

| 조건 | 필드 | 설명 |
|------|------|------|
| sellerIdEq | seller.id | 판매자 ID 일치 |

### 생성되는 SQL (예상)

```sql
SELECT
    s.seller_id,
    sbi.company_name,
    s.seller_logo_url,
    COALESCE(s.seller_description, ''),
    CONCAT(sbi.business_address_line1, ' ', sbi.business_address_line2, ' ', sbi.business_address_zip_code),
    COALESCE(sbi.cs_number, sbi.cs_phone_number),
    sbi.cs_phone_number,
    sbi.registration_number,
    sbi.sale_report_number,
    sbi.representative,
    sbi.cs_email
FROM seller s
INNER JOIN seller_business_info sbi ON sbi.seller_id = s.seller_id
WHERE s.seller_id = ?
LIMIT 1
```

---

## 🔴 Redis 캐싱 전략

### 캐시 키 패턴

```
SELLER:{sellerId}
```

### 캐시 흐름

1. **Cache Hit**: Redis에서 조회 성공 → 바로 반환
2. **Cache Miss**: DB 조회 → Redis 저장 → 반환

### 캐시 저장

```java
// SellerRedisQueryService
sellerRedisQueryService.saveSellerInRedis(sellerInfo)
```

---

## ⚠️ 예외 처리

| 예외 | 발생 조건 | HTTP Status |
|------|----------|-------------|
| SellerNotFoundException | sellerId로 조회 결과 없음 | 404 (추정) |

---

## 📁 파일 위치

| 유형 | 파일 경로 |
|------|----------|
| Controller | `module/seller/controller/SellerController.java` |
| Service Interface | `module/seller/service/SellerFindService.java` |
| Service Impl | `module/seller/service/SellerFindServiceImpl.java` |
| Repository Interface | `module/seller/repository/SellerFindRepository.java` |
| Repository Impl | `module/seller/repository/SellerFindRepositoryImpl.java` |
| Redis Service | `module/seller/service/SellerRedisFindService[Impl].java` |
| DTO | `module/seller/dto/SellerInfo.java` |
| Entity | `module/seller/entity/Seller.java`, `SellerBusinessInfo.java` |

---

## 🔗 다음 단계

```bash
# DTO 변환 (새 컨벤션으로)
/legacy-convert web:SellerController.fetchSeller

# Persistence Layer 생성
/legacy-query SellerController.fetchSeller
```
