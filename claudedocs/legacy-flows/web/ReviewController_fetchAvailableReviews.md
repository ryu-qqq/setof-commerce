# API Flow Documentation: ReviewController.fetchAvailableReviews

> Generated by `/legacy-flow` command
> Date: 2026-02-04
> Source: bootstrap-legacy-web-api

---

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/reviews/my-page/available` |
| 인증 | `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")` |
| Controller | `ReviewController` |
| Service | `ReviewFindService` → `ReviewFindServiceImpl` → `OrderFindService` → `OrderFindServiceImpl` |
| Repository | `OrderFindRepository` → `OrderFindRepositoryImpl` |

---

## 📥 Request

### Request Parameters

| 파라미터 | 타입 | 필수 | 설명 |
|----------|------|------|------|
| orderType | OrderType | ❌ | 정렬 타입 (미사용) |
| productGroupId | Long | ❌ | 상품그룹 ID (미사용) |
| lastDomainId | Long | ❌ | 커서 페이징용 마지막 ID |
| page | int | ❌ | 페이지 번호 (Pageable) |
| size | int | ❌ | 페이지 크기 (Pageable) |

### Request DTO 구조

```java
public class ReviewFilter {
    private OrderType orderType;    // 미사용
    private Long productGroupId;    // 미사용
    private Long lastDomainId;      // 커서 페이징용 (orderId 기준)
}
```

---

## 📤 Response

### Response DTO 구조

```java
public class CustomSlice<T> {
    private List<T> content;
    private boolean hasNext;
    private long totalElements;
    private Long lastDomainId;
}

public class ReviewOrderProductDto extends OrderProductDto {
    private LocalDateTime paymentDate;

    // OrderProductDto 상속 필드
    private long paymentId;
    private long sellerId;
    private long orderId;
    private BrandDto brand;           // { brandId, brandName }
    private long productGroupId;
    private String productGroupName;
    private long productId;
    private String sellerName;
    private String productGroupMainImageUrl;
    private int productQuantity;
    private OrderStatus orderStatus;
    private long regularPrice;
    private long currentPrice;
    private long orderAmount;
    private Set<OptionDto> options;   // { optionGroupId, optionDetailId, optionName, optionValue }
}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": {
    "content": [
      {
        "paymentId": 1001,
        "sellerId": 50,
        "orderId": 456,
        "brand": {
          "brandId": 10,
          "brandName": "NIKE"
        },
        "productGroupId": 789,
        "productGroupName": "프리미엄 티셔츠",
        "productId": 1000,
        "sellerName": "나이키 공식스토어",
        "productGroupMainImageUrl": "https://...",
        "productQuantity": 2,
        "orderStatus": "DELIVERED",
        "regularPrice": 50000,
        "currentPrice": 45000,
        "orderAmount": 90000,
        "options": [
          {
            "optionGroupId": 1,
            "optionDetailId": 10,
            "optionName": "색상",
            "optionValue": "Black"
          },
          {
            "optionGroupId": 2,
            "optionDetailId": 20,
            "optionName": "사이즈",
            "optionValue": "L"
          }
        ],
        "paymentDate": "2024-01-10 14:20:00"
      }
    ],
    "hasNext": true,
    "totalElements": 25,
    "lastDomainId": 456
  }
}
```

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────┐
│   Controller                                                  │
│   ReviewController.fetchAvailableReviews()                   │
│   @GetMapping("/api/v1/reviews/my-page/available")           │
│   @PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")           │
│   Params: ReviewFilter, Pageable                             │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Service (Review)                                            │
│   ReviewFindServiceImpl.fetchAvailableReviews()              │
│   @Transactional(readOnly = true)                            │
│                                                               │
│   → 위임: orderFindService.fetchAvailableReviewOrders()      │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Service (Order)                                             │
│   OrderFindServiceImpl.fetchAvailableReviewOrders()          │
│                                                               │
│   1. SecurityUtils.currentUserId() - 현재 사용자 ID          │
│   2. orderFindRepository.fetchAvailableReviews()             │
│   3. orderFindRepository.fetchAvailableReviewsCountQuery()   │
│   4. orderSliceMapper.toSlice()                              │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Repository                                                  │
│   OrderFindRepositoryImpl                                    │
│                                                               │
│   Step 1: fetchAvailableReviewOrderIds() - 주문 ID 목록      │
│   Step 2: fetchAvailableReviews() - 상세 정보 조회           │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Database                                                    │
│   Tables: order, payment, seller,                            │
│           order_snap_shot_product_group,                     │
│           order_snap_shot_product_group_image,               │
│           order_snap_shot_product,                           │
│           brand,                                              │
│           order_snap_shot_product_option,                    │
│           order_snap_shot_option_group,                      │
│           order_snap_shot_option_detail                      │
└──────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Query 분석

### Query 1: fetchAvailableReviewOrderIds (주문 ID 목록)

#### 조회 테이블

| 테이블 | Alias | 조인 유형 |
|--------|-------|----------|
| order | order | FROM |

#### QueryDSL 코드

```java
queryFactory
    .select(order.id)
    .from(order)
    .where(
        userIdEq(userId),              // order.userId = ?
        reviewNotYet(),                // order.reviewYn = 'N'
        orderIdLt(lastDomainId),       // order.id < lastDomainId (커서 페이징)
        orderStatusIn(OrderStatus.getAvailableReviewOrderStatus()),  // 리뷰 가능 상태
        isLesserThan3Months()          // 3개월 이내 주문
    )
    .orderBy(order.id.desc())
    .limit(pageable.getPageSize() + 1)
    .distinct()
```

#### 필터 조건

| 조건 | 설명 |
|------|------|
| `userIdEq(userId)` | 현재 로그인 사용자 |
| `reviewNotYet()` | 아직 리뷰 미작성 (reviewYn = 'N') |
| `orderIdLt(lastDomainId)` | 커서 기반 페이징 |
| `orderStatusIn(...)` | 배송완료 등 리뷰 가능 상태 |
| `isLesserThan3Months()` | 주문 후 3개월 이내 |

---

### Query 2: fetchAvailableReviews (상세 정보)

#### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| order | order | FROM | - |
| payment | payment | INNER JOIN | payment.id = order.paymentId |
| seller | seller | INNER JOIN | seller.id = order.sellerId |
| order_snap_shot_product_group | orderSnapShotProductGroup | INNER JOIN | orderId = order.id |
| brand | brand | INNER JOIN | brand.id = brandId |
| order_snap_shot_product_group_image | orderSnapShotProductGroupImage | INNER JOIN | orderId, imageType = MAIN |
| order_snap_shot_product | orderSnapShotProduct | INNER JOIN | orderId = order.id |
| order_snap_shot_product_option | orderSnapShotProductOption | LEFT JOIN | orderId = order.id |
| order_snap_shot_option_group | orderSnapShotOptionGroup | LEFT JOIN | orderId, optionGroupId 매칭 |
| order_snap_shot_option_detail | orderSnapShotOptionDetail | LEFT JOIN | orderId, optionDetailId 매칭 |

#### QueryDSL 코드

```java
queryFactory
    .from(order)
    .innerJoin(payment).on(payment.id.eq(order.paymentId))
    .innerJoin(seller).on(seller.id.eq(order.sellerId))
    .innerJoin(orderSnapShotProductGroup).on(orderSnapShotProductGroup.orderId.eq(order.id))
    .innerJoin(brand).on(brand.id.eq(orderSnapShotProductGroup...brandId))
    .innerJoin(orderSnapShotProductGroupImage)
        .on(orderSnapShotProductGroupImage.orderId.eq(order.id))
        .on(orderSnapShotProductGroupImage...productGroupImageType.eq(ProductGroupImageType.MAIN))
    .innerJoin(orderSnapShotProduct).on(orderSnapShotProduct.orderId.eq(order.id))
    .leftJoin(orderSnapShotProductOption).on(orderSnapShotProductOption.orderId.eq(order.id))
    .leftJoin(orderSnapShotOptionGroup)...
    .leftJoin(orderSnapShotOptionDetail)...
    .where(orderIdIn(orderIds))
    .transform(GroupBy.groupBy(order.id).list(new QReviewOrderProductDto(...)))
```

---

### Query 3: fetchAvailableReviewsCountQuery (총 개수)

```java
queryFactory
    .select(order.count())
    .from(order)
    .where(
        userIdEq(userId),
        reviewNotYet(),
        orderStatusIn(OrderStatus.getAvailableReviewOrderStatus())
    )
    .distinct()
```

---

## 🔍 WHERE 조건

| 조건 메서드 | 필드 | 설명 |
|------------|------|------|
| `userIdEq` | order.userId | 로그인 사용자 필터 |
| `reviewNotYet` | order.reviewYn = 'N' | 리뷰 미작성 주문 |
| `orderIdLt` | order.id < lastDomainId | 커서 페이징 |
| `orderStatusIn` | order.orderStatus IN (...) | 리뷰 가능 상태 |
| `isLesserThan3Months` | 주문일 >= 3개월 전 | 리뷰 작성 기한 |
| `orderIdIn` | order.id IN (...) | ID 목록 필터 |

---

## 📊 특이사항

1. **서비스 위임 패턴**: ReviewFindService → OrderFindService로 위임
2. **커서 기반 페이징**: lastDomainId로 무한 스크롤 지원
3. **3개월 제한**: 주문 후 3개월 이내만 리뷰 작성 가능
4. **주문 스냅샷 사용**: order_snap_shot_* 테이블로 주문 시점 정보 보존

---

## 🔗 다음 단계

```bash
# DTO 변환
/legacy-convert web:ReviewController.fetchAvailableReviews

# Persistence Layer 생성
/legacy-query web:ReviewController.fetchAvailableReviews
```
