# API Flow Documentation: MileageController.fetchMileageHistories

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api

---

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/mileage/my-page/mileage-histories` |
| Controller | `MileageController` |
| Service | `UserMileageFindService` → `UserMileageFindServiceImpl` |
| Repository | `MileageHistoryFindRepository` → `MileageHistoryFindRepositoryImpl` |
| Repository (보조) | `UserMileageFindRepository` → `UserMileageFindRepositoryImpl` |
| 인증 | `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")` |

---

## 📥 Request

### Request Parameters

| 파라미터 | 타입 | 필수 | 설명 | Validation |
|----------|------|------|------|------------|
| lastDomainId | Long | ❌ | 마지막 조회 ID (커서 기반 페이징) | - |
| reasons | List\<Reason\> | ❌ | 마일리지 사유 필터 | SAVE, USE, REFUND, EXPIRED |
| page | int | ❌ | 페이지 번호 (0부터 시작) | - |
| size | int | ❌ | 페이지 크기 | - |
| sort | String | ❌ | 정렬 조건 | - |

### Request DTO 구조

```java
// MileageFilter.java
@Getter
@NoArgsConstructor(access = AccessLevel.PROTECTED)
@AllArgsConstructor
public class MileageFilter {
    private Long lastDomainId;        // 커서 기반 페이징용 (미사용)
    private List<Reason> reasons;     // 마일리지 사유 필터
}

// Reason enum
public enum Reason {
    SAVE,     // 적립
    USE,      // 사용
    REFUND,   // 환불
    EXPIRED;  // 만료
}
```

---

## 📤 Response

### Response DTO 구조

```java
// MileagePage<T> - Page 인터페이스 구현체
@Builder
@AllArgsConstructor
@Getter
public class MileagePage<T> implements Page<T> {
    private UserMileageDto userMileage;   // 사용자 마일리지 요약 정보
    private final List<T> content;         // 마일리지 이력 목록
    private final Pageable pageable;
    private final boolean last;
    private final int totalPages;
    private final long totalElements;
    private final boolean first;
    private final int number;
    private final Sort sort;
    private final int size;
    private final int numberOfElements;
    private final boolean empty;
    private final Long lastDomainId;       // 다음 페이지 조회용
    private final Page<T> originalPage;    // @JsonIgnore
}

// UserMileageDto - 사용자 마일리지 요약
@Getter
public class UserMileageDto {
    private long userId;
    private double currentMileage;         // 현재 사용 가능 마일리지
    private double expectedSaveMileage;    // 적립 예정 마일리지
    private double expectedExpireMileage;  // 소멸 예정 마일리지
}

// UserMileageHistoryDto - 마일리지 이력 상세
@Getter
public class UserMileageHistoryDto {
    @JsonIgnore
    private long mileageHistoryId;         // 이력 ID (응답에서 제외)
    private long mileageId;                // 마일리지 ID
    private String title;                  // 이력 제목 (동적 설정)
    private long paymentId;                // 결제 ID
    private long orderId;                  // 주문 ID
    private double changeAmount;           // 변동 금액
    private Reason reason;                 // 사유 (SAVE, USE, REFUND, EXPIRED)
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private LocalDateTime usedDate;        // 사용/적립 일시
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private LocalDateTime expirationDate;  // 만료일
}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": {
    "userMileage": {
      "userId": 12345,
      "currentMileage": 15000.0,
      "expectedSaveMileage": 0.0,
      "expectedExpireMileage": 3000.0
    },
    "content": [
      {
        "mileageId": 1001,
        "title": "회원가입 축하 적립금",
        "paymentId": 0,
        "orderId": 0,
        "changeAmount": 5000.0,
        "reason": "SAVE",
        "usedDate": "2026-01-15 10:30:00",
        "expirationDate": "2027-01-15 23:59:59"
      },
      {
        "mileageId": 1002,
        "title": "주문 결제 시 사용",
        "paymentId": 5001,
        "orderId": 3001,
        "changeAmount": -2000.0,
        "reason": "USE",
        "usedDate": "2026-01-20 14:22:15",
        "expirationDate": null
      }
    ],
    "totalElements": 25,
    "totalPages": 3,
    "size": 10,
    "number": 0,
    "first": true,
    "last": false,
    "empty": false
  }
}
```

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────────┐
│   MileageController                                              │
│   @GetMapping("/my-page/mileage-histories")                      │
│   fetchMileageHistories(MileageFilter, Pageable)                 │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│   UserMileageFindServiceImpl                                     │
│   @Transactional(readOnly = true)                                │
│   fetchMyMileageHistories(filter, pageable)                      │
│                                                                  │
│   1. SecurityUtils.currentUserId() → 현재 로그인 사용자 ID 획득    │
│   2. mileageHistoryFindRepository.fetchMileageHistories()        │
│   3. userMileageMapper.setTitle() → 사유별 제목 동적 설정          │
│   4. mileageHistoryFindRepository.fetchMileageHistoryCount()     │
│   5. PageableExecutionUtils.getPage() → 페이지 객체 생성          │
│   6. fetchUserMileage(userId) → 사용자 마일리지 요약 조회          │
│   7. userMileageMapper.toMileagePage() → 최종 응답 객체 조립       │
└────────────────────────┬─────────────────────────────────────────┘
                         │
           ┌─────────────┴─────────────┐
           ▼                           ▼
┌─────────────────────────┐  ┌─────────────────────────┐
│ MileageHistoryFind      │  │ UserMileageFind         │
│ RepositoryImpl          │  │ RepositoryImpl          │
│                         │  │                         │
│ fetchMileageHistories() │  │ fetchUserMileageQuery   │
│ fetchMileageHistoryCount│  │ InMyPage()              │
└───────────┬─────────────┘  └───────────┬─────────────┘
            │                            │
            ▼                            ▼
┌─────────────────────────┐  ┌─────────────────────────┐
│      Database           │  │      Database           │
│ Tables:                 │  │ Table:                  │
│  - mileage              │  │  - mileage              │
│  - mileage_history      │  │                         │
│  - order_snapshot_      │  │ (사용자별 마일리지 잔액) │
│    mileage              │  │                         │
│  - order_snapshot_      │  │                         │
│    mileage_detail       │  │                         │
└─────────────────────────┘  └─────────────────────────┘
```

---

## 🗄️ Database Query 분석

### Query 1: 마일리지 이력 조회 (fetchMileageHistories)

#### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| mileage | mileage | FROM | - |
| mileage_history | mileageHistory | INNER JOIN | mileage.id = mileageHistory.mileageId |
| order_snapshot_mileage_detail | orderSnapShotMileageDetail | LEFT JOIN | orderSnapShotMileageDetail.mileageId = mileage.id |
| order_snapshot_mileage | orderSnapShotMileage | LEFT JOIN | orderSnapShotMileage.id = orderSnapShotMileageDetail.orderSnapShotMileageId AND orderSnapShotMileage.orderId = mileageHistory.targetId |

#### QueryDSL 코드

```java
queryFactory
    .select(
        new QUserMileageHistoryDto(
            mileageHistory.id,
            mileage.id,
            mileage.title,
            orderSnapShotMileage.paymentId,
            orderSnapShotMileage.orderId,
            mileageHistory.changeAmount,
            mileageHistory.reason,
            mileageHistory.insertDate,
            mileage.expirationDate))
    .from(mileage)
    .innerJoin(mileageHistory)
        .on(mileage.id.eq(mileageHistory.mileageId))
    .leftJoin(orderSnapShotMileageDetail)
        .on(orderSnapShotMileageDetail.mileageId.eq(mileage.id))
    .leftJoin(orderSnapShotMileage)
        .on(orderSnapShotMileage.id.eq(orderSnapShotMileageDetail.orderSnapShotMileageId))
        .on(orderSnapShotMileage.orderId.eq(mileageHistory.targetId))
    .where(
        userIdEq(userId),
        reasonEq(filter),
        targetIdIsZero()
            .or(hasValidOrderIdAndPaymentId().or(noOrderIdAndPaymentId())))
    .offset(pageable.getOffset())
    .limit(pageable.getPageSize())
    .distinct()
    .orderBy(mileageHistory.insertDate.desc())
    .groupBy(
        mileageHistory.id, mileage.id, mileage.title,
        orderSnapShotMileage.paymentId, orderSnapShotMileage.orderId,
        mileageHistory.changeAmount, mileageHistory.reason,
        mileageHistory.insertDate, mileage.expirationDate)
    .fetch();
```

#### WHERE 조건

| 조건 함수 | 필드 | 설명 |
|----------|------|------|
| userIdEq | mileage.userId | 사용자 ID 일치 |
| reasonEq | mileageHistory.reason | 사유 필터 (IN 조건, nullable) |
| targetIdIsZero | mileageHistory.targetId | targetId가 0인 경우 (비주문 마일리지) |
| hasValidOrderIdAndPaymentId | orderSnapShotMileage.orderId, paymentId | 유효한 주문/결제 ID 존재 |
| noOrderIdAndPaymentId | orderSnapShotMileage.orderId, paymentId | 주문/결제 ID 없음 |

---

### Query 2: 마일리지 이력 카운트 (fetchMileageHistoryCount)

```java
queryFactory
    .select(mileageHistory.count())
    .from(mileageHistory)
    .where(
        mileageHistory.userId.eq(userId),
        reasonEq(filter))
    .distinct();
```

---

### Query 3: 사용자 마일리지 조회 (fetchUserMileageQueryInMyPage)

#### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| mileage | mileage | FROM | - |

#### QueryDSL 코드

```java
queryFactory
    .from(mileage)
    .where(
        userIdEq(userId),
        isBiggerThanZero(),    // mileageAmount - usedMileageAmount > 0
        activeYn())            // activeYn = 'Y'
    .transform(
        GroupBy.groupBy(mileage.id)
            .list(
                new QUserMileageQueryDto(
                    mileage.userId,
                    mileage.id,
                    mileage.mileageAmount,
                    mileage.usedMileageAmount,
                    mileage.activeYn,
                    mileage.issuedDate,
                    mileage.expirationDate)));
```

#### WHERE 조건

| 조건 함수 | 필드 | 설명 |
|----------|------|------|
| userIdEq | mileage.userId | 사용자 ID 일치 |
| isBiggerThanZero | mileageAmount - usedMileageAmount | 잔여 마일리지 > 0 |
| activeYn | mileage.activeYn | 활성 상태 (Y) |

---

## 📊 Entity 구조

### mileage 테이블

| 컬럼 | 타입 | 설명 |
|------|------|------|
| mileage_id | BIGINT | PK |
| user_id | BIGINT | 사용자 ID |
| mileage_amount | DOUBLE | 적립 금액 |
| used_mileage_amount | DOUBLE | 사용 금액 |
| issued_date | DATETIME | 적립 일시 |
| expiration_date | DATETIME | 만료 일시 |
| active_yn | VARCHAR | 활성 여부 (Y/N) |
| title | VARCHAR | 적립금 명칭 |

### mileage_history 테이블

| 컬럼 | 타입 | 설명 |
|------|------|------|
| mileage_history_id | BIGINT | PK |
| user_id | BIGINT | 사용자 ID |
| mileage_id | BIGINT | 마일리지 ID (FK) |
| change_amount | DOUBLE | 변동 금액 |
| reason | VARCHAR | 사유 (SAVE, USE, REFUND, EXPIRED) |
| issue_type | VARCHAR | 발급 유형 (JOIN, ORDER, REVIEW, ETC) |
| target_id | BIGINT | 대상 ID (주문 ID 등) |

### order_snapshot_mileage 테이블

| 컬럼 | 타입 | 설명 |
|------|------|------|
| order_snapshot_mileage_id | BIGINT | PK |
| order_id | BIGINT | 주문 ID |
| payment_id | BIGINT | 결제 ID |

### order_snapshot_mileage_detail 테이블

| 컬럼 | 타입 | 설명 |
|------|------|------|
| order_snapshot_mileage_detail_id | BIGINT | PK |
| order_snapshot_mileage_id | BIGINT | FK |
| mileage_id | BIGINT | 마일리지 ID |
| used_amount | DOUBLE | 사용 금액 |
| mileage_balance | DOUBLE | 잔액 |

---

## 🔧 Mapper 로직

### UserMileageMapperImpl

#### setTitle() - 사유별 제목 동적 설정

```java
public void setTitle(List<UserMileageHistoryDto> userMileageHistories) {
    userMileageHistories.forEach(m -> {
        if (m.getReason().isUse())     m.setTitle("주문 결제 시 사용");
        if (m.getReason().isExpired()) m.setTitle("적립금 유효기간 만료");
        if (m.getReason().isRefund())  m.setTitle("적립금 사용주문 취소");
        // SAVE는 DB의 title 값 그대로 사용
    });
}
```

#### toMyMileages() - 사용자 마일리지 요약 계산

```java
public UserMileageDto toMyMileages(long userId, List<UserMileageQueryDto> mileageQueries, double pendingMileages) {
    // 만료 예정 마일리지 계산 (현재 시점 이전에 만료된 마일리지)
    double expiringMileageAmount = getExpiringMileageAmount(mileageQueries);

    // 현재 마일리지 합계
    double currentMileageAmount = mileageQueries.stream()
        .mapToDouble(UserMileageQueryDto::getCurrentMileage)
        .sum();

    // 실제 사용 가능 금액 = 현재 - 만료 예정
    double mileageAmount = Math.max(currentMileageAmount - expiringMileageAmount, 0.0);

    return new UserMileageDto(userId, mileageAmount, pendingMileages, expiringMileageAmount);
}
```

---

## 🔗 다음 단계

### DTO 변환
```bash
/legacy-convert web:MileageController.fetchMileageHistories
```

### Persistence Layer 생성
```bash
/legacy-query web:MileageController.fetchMileageHistories
```

---

## 📝 특이사항

### 1. 복합 응답 구조
- 마일리지 요약(`UserMileageDto`)과 이력 목록(`UserMileageHistoryDto`)을 함께 반환
- 별도의 2개 쿼리를 실행하여 데이터 조합

### 2. 동적 제목 설정
- `SAVE`: DB에 저장된 `title` 값 사용
- `USE`, `REFUND`, `EXPIRED`: 하드코딩된 상수 값으로 대체

### 3. 복잡한 조인 조건
- 주문 관련 마일리지 이력 조회 시 4개 테이블 조인
- `targetId == 0` (비주문) 또는 유효한 주문/결제 ID 조건 분기

### 4. 만료 마일리지 계산
- `expirationDate < now()` 조건으로 만료 예정 금액 계산
- 현재 잔액에서 만료 예정 금액을 차감하여 실제 사용 가능 금액 산출

### 5. 커서 기반 페이징 미사용
- `lastDomainId` 필드는 존재하나 실제 쿼리에서 미사용
- 표준 Spring Pageable 기반 offset/limit 페이징 적용
