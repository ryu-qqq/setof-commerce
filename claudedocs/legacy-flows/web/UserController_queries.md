# API Flow Documentation: UserController (All Query Endpoints)

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api

---

## 📋 분석 대상 Query 엔드포인트 (7개)

| # | Method | HTTP | Path | Service |
|---|--------|------|------|---------|
| 1 | fetchUser | GET | /api/v1/user | UserFindService |
| 2 | isExistUser | GET | /api/v1/user/exists | UserFindService |
| 3 | fetchAddressBook | GET | /api/v1/user/address-book | ShippingAddressFindService |
| 4 | fetchAddressBook | GET | /api/v1/user/address-book/{id} | ShippingAddressFindService |
| 5 | fetchRefundAccount | GET | /api/v1/user/refund-account | RefundAccountFindService |
| 6 | fetchMyPage | GET | /api/v1/user/my-page | MyPageFindService |
| 7 | fetchMyFavorites | GET | /api/v1/user/my-favorites | UserFavoriteFindService |

---

# 1. fetchUser - 현재 사용자 정보 조회

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/user` |
| Controller | UserController |
| Service | UserFindService → UserFindServiceImpl |
| Repository | UserFindRepository → UserFindRepositoryImpl |
| 인증 | `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")` |

## 📥 Request

없음 (SecurityContext에서 userId 추출)

## 📤 Response

### Response DTO: JoinedUser

```java
public class JoinedUser {
    private boolean isJoined;
    private JoinedDto joinedUser;
}
```

### Nested DTO: JoinedDto

```java
public class JoinedDto {
    private String name;
    private long userId;
    private SocialLoginType socialLoginType;
    private String phoneNumber;
    private String socialPkId;
    private double currentMileage;
    private LocalDateTime joinedDate;
    private Yn deleteYn;
}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": {
    "isJoined": true,
    "joinedUser": {
      "name": "홍길동",
      "userId": 12345,
      "socialLoginType": "KAKAO",
      "phoneNumber": "01012345678",
      "socialPkId": "kakao_123456",
      "currentMileage": 5000.0,
      "joinedDate": "2024-01-15T10:30:00"
    }
  }
}
```

## 🔄 호출 흐름

```
┌──────────────────────┐
│     Controller       │  UserController.fetchUser()
│   GET /api/v1/user   │  @PreAuthorize("NORMAL_GRADE")
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│       Service        │  UserFindServiceImpl.fetchJoinedUser()
│   @Transactional     │  userId = SecurityUtils.currentUserId()
│   (readOnly=true)    │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│     Repository       │  UserFindRepositoryImpl.fetchUser(userId)
│      QueryDSL        │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│      Database        │  Tables: users, user_grade, user_mileage
│                      │  JOINs: user_grade, user_mileage
└──────────────────────┘
```

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| users | users | FROM | - |
| user_grade | userGrade | JOIN | userGrade.id = users.userGradeId |
| user_mileage | userMileage | JOIN | userMileage.id = users.id |

### QueryDSL 코드

```java
queryFactory
    .select(new QUserDto(
        users.id,
        users.socialLoginType,
        users.phoneNumber,
        users.name,
        userGrade.gradeName,
        userMileage.currentMileage,
        users.insertDate))
    .from(users)
    .join(userGrade).on(userGrade.id.eq(users.userGradeId))
    .join(userMileage).on(userMileage.id.eq(users.id))
    .where(userIdEq(userId))
    .fetchOne()
```

---

# 2. isExistUser - 사용자 존재 여부 확인

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/user/exists` |
| Controller | UserController |
| Service | UserFindService → UserFindServiceImpl |
| Repository | UserFindRepository → UserFindRepositoryImpl |
| 인증 | 없음 (공개 API) |

## 📥 Request

### Request DTO: IsJoinedUser

```java
public class IsJoinedUser {
    @Pattern(regexp = "010[0-9]{8}", message = "유효하지 않은 전화번호 형식입니다.")
    private String phoneNumber;
}
```

### Request Parameters

| 파라미터 | 타입 | 필수 | 설명 | Validation |
|----------|------|------|------|------------|
| phoneNumber | String | ✅ | 전화번호 | @Pattern(010xxxxxxxx) |

## 📤 Response

### Response DTO: JoinedUser

```java
public class JoinedUser {
    private boolean isJoined;      // 가입 여부
    private JoinedDto joinedUser;  // 가입된 경우 사용자 정보
}
```

### Response JSON 예시 (가입된 사용자)

```json
{
  "success": true,
  "data": {
    "isJoined": true,
    "joinedUser": {
      "name": "홍길동",
      "userId": 12345,
      "socialLoginType": "KAKAO",
      "phoneNumber": "01012345678",
      "socialPkId": "kakao_123456",
      "currentMileage": 5000.0,
      "joinedDate": "2024-01-15T10:30:00"
    }
  }
}
```

### Response JSON 예시 (미가입 사용자)

```json
{
  "success": true,
  "data": {
    "isJoined": false,
    "joinedUser": null
  }
}
```

## 🔄 호출 흐름

```
┌────────────────────────────┐
│        Controller          │  UserController.isExistUser()
│  GET /api/v1/user/exists   │  @ModelAttribute IsJoinedUser
└────────────┬───────────────┘
             │
             ▼
┌────────────────────────────┐
│          Service           │  UserFindServiceImpl.fetchJoinedUser(isJoinedUser)
│      @Transactional        │  1. Redis 캐시 확인
│      (readOnly=true)       │  2. 없으면 DB 조회
└────────────┬───────────────┘
             │
             ▼
┌────────────────────────────┐
│        Repository          │  UserFindRepositoryImpl.isJoinedMember(phoneNumber)
│         QueryDSL           │
└────────────┬───────────────┘
             │
             ▼
┌────────────────────────────┐
│         Database           │  Tables: users, user_mileage
└────────────────────────────┘
```

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| users | users | FROM | - |
| user_mileage | userMileage | INNER JOIN | users.id = userMileage.id |

### QueryDSL 코드

```java
queryFactory
    .select(new QJoinedDto(
        users.name,
        users.id,
        users.socialLoginType,
        users.phoneNumber,
        users.socialPkId.coalesce(""),
        userMileage.currentMileage,
        users.insertDate,
        users.deleteYn))
    .from(users)
    .innerJoin(userMileage).on(users.id.eq(userMileage.id))
    .where(phoneNumberEq(phoneNumber))
    .fetchFirst()
```

---

# 3. fetchAddressBook (목록) - 배송지 목록 조회

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/user/address-book` |
| Controller | UserController |
| Service | ShippingAddressFindService → ShippingAddressFindServiceImpl |
| Repository | ShippingAddressFindRepository → ShippingAddressFindRepositoryImpl |
| 인증 | `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")` |

## 📥 Request

없음 (SecurityContext에서 userId 추출)

## 📤 Response

### Response DTO: List<UserShippingInfo>

```java
public class UserShippingInfo {
    private Long shippingAddressId;
    private ShippingDetails shippingDetails;  // Embedded
    private Yn defaultYn;
}
```

### Embedded: ShippingDetails

```java
@Embeddable
public class ShippingDetails {
    private String receiverName;        // 수취인명 (max 10)
    private String shippingAddressName; // 배송지명 (max 30)
    private String addressLine1;        // 주소1 (max 100)
    private String addressLine2;        // 주소2 (max 100)
    private String zipCode;             // 우편번호 (max 10)
    private Origin country;             // 국가 (enum)
    private String deliveryRequest;     // 배송요청사항 (max 100)
    private String phoneNumber;         // 전화번호 (010xxxxxxxx)
}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": [
    {
      "shippingAddressId": 1,
      "shippingDetails": {
        "receiverName": "홍길동",
        "shippingAddressName": "집",
        "addressLine1": "서울특별시 강남구 테헤란로 123",
        "addressLine2": "456호",
        "zipCode": "06234",
        "country": "KR",
        "deliveryRequest": "문 앞에 놓아주세요",
        "phoneNumber": "01012345678"
      },
      "defaultYn": "Y"
    }
  ]
}
```

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| shipping_address | shippingAddress | FROM | - |

### QueryDSL 코드

```java
queryFactory
    .select(new QUserShippingInfo(
        shippingAddress.id,
        shippingAddress.shippingDetails,
        shippingAddress.defaultYn))
    .from(shippingAddress)
    .where(userIdEq(userId))
    .fetch()
```

---

# 4. fetchAddressBook (단건) - 특정 배송지 조회

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/user/address-book/{shippingAddressId}` |
| Controller | UserController |
| Service | ShippingAddressFindService → ShippingAddressFindServiceImpl |
| 인증 | `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")` |

## 📥 Request

| 파라미터 | 타입 | 필수 | 설명 |
|----------|------|------|------|
| shippingAddressId | long | ✅ | 배송지 ID (PathVariable) |

## 📤 Response

UserShippingInfo (목록 조회와 동일)

## 🔄 특이사항

⚠️ **비효율적인 구현**: 단건 조회 시 전체 목록을 조회한 후 Stream filter로 찾음

```java
public UserShippingInfo fetchShippingInfo(long shippingAddressId) {
    return fetchShippingInfo().stream()  // 전체 목록 조회
            .filter(s -> s.getShippingAddressId() == shippingAddressId)
            .findFirst()
            .orElseThrow();
}
```

---

# 5. fetchRefundAccount - 환불 계좌 조회

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/user/refund-account` |
| Controller | UserController |
| Service | RefundAccountFindService → RefundAccountFindServiceImpl |
| Repository | RefundAccountFindRepository → RefundAccountFindRepositoryImpl |
| 인증 | `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")` |

## 📥 Request

없음 (SecurityContext에서 userId 추출)

## 📤 Response

### Response DTO: RefundAccountInfo

```java
public class RefundAccountInfo {
    @JsonIgnore private Boolean isAuthenticated;
    private Long refundAccountId;
    private String bankName;
    private String accountNumber;
    private String accountHolderName;
}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": {
    "refundAccountId": 1,
    "bankName": "신한은행",
    "accountNumber": "110-123-456789",
    "accountHolderName": "홍길동"
  }
}
```

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| refund_account | refundAccount | FROM | - |

### WHERE 조건

| 조건 | 필드 | 설명 |
|------|------|------|
| userIdEq | refundAccount.userId | 사용자 ID |
| deleteYnEq | refundAccount.deleteYn | 삭제되지 않은 계좌만 (Yn.N) |

### QueryDSL 코드

```java
queryFactory
    .select(new QRefundAccountInfo(
        refundAccount.id,
        refundAccount.bankName,
        refundAccount.accountNumber,
        refundAccount.accountHolderName))
    .from(refundAccount)
    .where(userIdEq(userId), deleteYnEq())
    .fetchOne()
```

---

# 6. fetchMyPage - 마이페이지 정보 조회

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/user/my-page` |
| Controller | UserController |
| Service | MyPageFindService → MyPageFindServiceImpl |
| Repository | MyPageFindRepository → MyPageFindRepositoryImpl |
| 인증 | `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")` |

## 📥 Request

없음 (SecurityContext에서 userId 추출)

## 📤 Response

### Response DTO: MyPageResponse

```java
public class MyPageResponse {
    private String name;
    private String phoneNumber;
    private String email;
    private SocialLoginType socialLoginType;

    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private LocalDateTime registrationDate;

    private UserGradeEnum userGrade;
    private double currentMileage;
    private List<OrderCountDto> orderCounts;  // 주문 상태별 건수
}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": {
    "name": "홍길동",
    "phoneNumber": "01012345678",
    "email": "hong@example.com",
    "socialLoginType": "KAKAO",
    "registrationDate": "2024-01-15 10:30:00",
    "userGrade": "VIP",
    "currentMileage": 50000.0,
    "orderCounts": [
      { "orderStatus": "PAYMENT_COMPLETED", "count": 5 },
      { "orderStatus": "SHIPPING", "count": 2 },
      { "orderStatus": "DELIVERED", "count": 10 }
    ]
  }
}
```

## 🔄 호출 흐름

```
┌────────────────────────────┐
│        Controller          │  UserController.fetchMyPage()
│  GET /api/v1/user/my-page  │
└────────────┬───────────────┘
             │
             ▼
┌────────────────────────────┐
│          Service           │  MyPageFindServiceImpl.fetchMyPage()
│      @Transactional        │  1. 주문 상태별 건수 조회 (OrderFindService)
│      (readOnly=true)       │  2. 마이페이지 정보 조회
│                            │  3. UserMapper로 합침
└────────────┬───────────────┘
             │
      ┌──────┴──────┐
      ▼             ▼
┌──────────┐  ┌───────────────┐
│ Order    │  │   MyPage      │
│ Service  │  │  Repository   │
└──────────┘  └───────────────┘
```

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| users | users | FROM | - |
| user_mileage | userMileage | INNER JOIN | users.id = userMileage.id |
| user_grade | userGrade | INNER JOIN | users.userGradeId = userGrade.id |

### QueryDSL 코드

```java
queryFactory
    .select(new QMyPageResponse(
        users.name,
        users.phoneNumber,
        users.email,
        users.socialLoginType,
        users.insertDate,
        userGrade.gradeName,
        userMileage.currentMileage))
    .from(users)
    .innerJoin(userMileage).on(users.id.eq(userMileage.id))
    .innerJoin(userGrade).on(users.userGradeId.eq(userGrade.id))
    .where(userIdEq(userId))
    .fetchOne()
```

---

# 7. fetchMyFavorites - 찜 목록 조회

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/user/my-favorites` |
| Controller | UserController |
| Service | UserFavoriteFindService → UserFavoriteFindServiceImpl |
| Repository | UserFavoriteFindRepository → UserFavoriteFindRepositoryImpl |
| 인증 | `@PreAuthorize("hasAnyAuthority('NORMAL_GRADE')")` |

## 📥 Request

### Request DTO: MyFavoriteFilter

```java
public class MyFavoriteFilter {
    private Long lastDomainId;  // 커서 기반 페이징용 마지막 ID
}
```

### Request Parameters

| 파라미터 | 타입 | 필수 | 설명 |
|----------|------|------|------|
| lastDomainId | Long | ❌ | 커서 기반 페이징 (마지막 userFavoriteId) |
| page | int | ❌ | 페이지 번호 (Pageable) |
| size | int | ❌ | 페이지 크기 (Pageable) |

## 📤 Response

### Response DTO: CustomSlice<UserFavoriteThumbnail>

```java
public class UserFavoriteThumbnail extends ProductGroupThumbnail {
    protected long userFavoriteId;

    // 상속 필드 (ProductGroupThumbnail)
    // private long productGroupId;
    // private long sellerId;
    // private String productGroupName;
    // private BrandDto brand;
    // private String productImageUrl;
    // private Price price;
    // private LocalDateTime insertDate;
    // private ProductStatus productStatus;
}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": {
    "content": [
      {
        "userFavoriteId": 100,
        "productGroupId": 1234,
        "sellerId": 10,
        "productGroupName": "프리미엄 티셔츠",
        "brand": {
          "brandId": 5,
          "brandName": "NIKE"
        },
        "productImageUrl": "https://cdn.example.com/images/product.jpg",
        "price": {
          "regularPrice": 50000,
          "currentPrice": 35000,
          "discountRate": 30
        },
        "insertDate": "2024-01-20T15:30:00",
        "productStatus": {
          "soldOutYn": "N",
          "displayYn": "Y"
        }
      }
    ],
    "hasNext": true,
    "totalElements": 25
  }
}
```

## 🔄 호출 흐름

```
┌─────────────────────────────────┐
│          Controller             │  UserController.fetchMyFavorites()
│  GET /api/v1/user/my-favorites  │  @ModelAttribute MyFavoriteFilter, Pageable
└──────────────┬──────────────────┘
               │
               ▼
┌─────────────────────────────────┐
│            Service              │  UserFavoriteFindServiceImpl.fetchUserFavorites()
│        @Transactional           │  1. 찜 목록 조회
│        (readOnly=true)          │  2. 전체 건수 조회
│                                 │  3. CustomSlice 변환
└──────────────┬──────────────────┘
               │
               ▼
┌─────────────────────────────────┐
│          Repository             │  UserFavoriteFindRepositoryImpl
│           QueryDSL              │  .fetchMyFavorites()
│                                 │  .fetchUserFavoriteCountQuery()
└──────────────┬──────────────────┘
               │
               ▼
┌─────────────────────────────────┐
│           Database              │  Tables: user_favorite, product_group,
│                                 │          product_group_image, brand
└─────────────────────────────────┘
```

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| user_favorite | userFavorite | FROM | - |
| product_group | productGroup | INNER JOIN | productGroup.id = userFavorite.productGroupId |
| product_group_image | productGroupImage | INNER JOIN | productGroupImage.productGroup.id = productGroup.id |
| brand | brand | INNER JOIN | brand.id = productGroup.brandId |

### WHERE 조건

| 조건 | 필드 | 설명 |
|------|------|------|
| userIdEq | userFavorite.userId | 사용자 ID |
| onStock | productGroup.productStatus.soldOutYn | 품절되지 않은 상품 (Yn.N) |
| userFavoriteIdLt | userFavorite.id | 커서 기반 페이징 (lastDomainId보다 작은) |
| imageType | productGroupImage.imageDetail.productGroupImageType | MAIN 이미지만 |
| deleteYn | productGroupImage.deleteYn | 삭제되지 않은 이미지만 |

### QueryDSL 코드

```java
queryFactory
    .from(userFavorite)
    .innerJoin(productGroup)
        .on(productGroup.id.eq(userFavorite.productGroupId))
    .innerJoin(productGroupImage)
        .on(productGroupImage.productGroup.id.eq(productGroup.id))
        .on(productGroupImage.imageDetail.productGroupImageType.eq(ProductGroupImageType.MAIN))
        .on(productGroupImage.deleteYn.eq(Yn.N))
    .innerJoin(brand)
        .on(brand.id.eq(productGroup.productGroupDetails.brandId))
    .where(
        userIdEq(userId),
        onStock(),
        userFavoriteIdLt(filter.getLastDomainId()))
    .orderBy(userFavorite.id.desc())
    .limit(pageable.getPageSize() + 1)
    .transform(GroupBy.groupBy(productGroup.id)
        .list(new QUserFavoriteThumbnail(...)))
```

---

## 📊 전체 테이블 요약

| Query | 주요 테이블 | 조인 테이블 |
|-------|-------------|-------------|
| fetchUser | users | user_grade, user_mileage |
| isExistUser | users | user_mileage |
| fetchAddressBook | shipping_address | - |
| fetchRefundAccount | refund_account | - |
| fetchMyPage | users | user_mileage, user_grade |
| fetchMyFavorites | user_favorite | product_group, product_group_image, brand |

---

## 🔗 다음 단계

DTO 변환:
```bash
/legacy-convert web:UserController.fetchUser
/legacy-convert web:UserController.fetchMyFavorites
```

Persistence Layer 생성:
```bash
/legacy-query UserController.fetchUser
/legacy-query UserController.fetchMyFavorites
```
