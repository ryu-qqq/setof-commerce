# API Flow Documentation: BrandController.fetchBrands

> Generated by `/legacy-flow` command
> Date: 2026-02-03
> Source: bootstrap-legacy-web-api

---

## 📌 기본 정보

| 항목 | 값 |
|------|-----|
| HTTP Method | GET |
| Path | `/api/v1/brand` |
| Controller | `BrandController` |
| Service | `BrandFindService` → `BrandFindServiceImpl` |
| Repository | `BrandFindRepository` → `BrandFindRepositoryImpl` |

---

## 📥 Request

### Request Parameters

| 파라미터 | 타입 | 필수 | 설명 | Validation |
|----------|------|------|------|------------|
| searchWord | String | ❌ | 브랜드명 검색어 (한글/영문) | - |

### Request DTO 구조

```java
// BrandFilter.java
public class BrandFilter extends AbstractSearchFilter {
    // 상속된 필드
    private String searchWord;

    public BrandFilter(String searchWord) {
        super(searchWord);
    }
}

// AbstractSearchFilter.java
@Getter
@NoArgsConstructor(access = AccessLevel.PROTECTED)
@AllArgsConstructor
public class AbstractSearchFilter implements SearchFilter {
    private String searchWord;
}
```

### Request 예시

```http
GET /api/v1/brand?searchWord=나이키
```

---

## 📤 Response

### Response DTO 구조

```java
// BrandDisplayDto.java
public class BrandDisplayDto {
    private final long brandId;           // 브랜드 ID
    private final String brandName;       // 브랜드명 (영문 코드)
    private final String korBrandName;    // 브랜드명 (한글)
    private final String brandIconImageUrl; // 브랜드 아이콘 이미지 URL

    @QueryProjection
    public BrandDisplayDto(
            long brandId,
            String brandName,
            String korBrandName,
            String brandIconImageUrl) {
        this.brandId = brandId;
        this.brandName = brandName;
        this.korBrandName = korBrandName;
        this.brandIconImageUrl = brandIconImageUrl;
    }
}
```

### Response JSON 예시

```json
{
  "success": true,
  "data": [
    {
      "brandId": 1,
      "brandName": "NIKE",
      "korBrandName": "나이키",
      "brandIconImageUrl": "https://cdn.example.com/brands/nike.png"
    },
    {
      "brandId": 2,
      "brandName": "ADIDAS",
      "korBrandName": "아디다스",
      "brandIconImageUrl": "https://cdn.example.com/brands/adidas.png"
    }
  ]
}
```

---

## 🔄 호출 흐름

```
┌──────────────────────────────────────────────────────────────┐
│   Controller                                                  │
│   BrandController.fetchBrands(@ModelAttribute BrandFilter)   │
│   @GetMapping("/brand")                                       │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Service                                                     │
│   BrandFindServiceImpl.fetchBrands(BrandFilter filter)       │
│   @Transactional(readOnly = true)                            │
│                                                               │
│   비즈니스 로직:                                               │
│   - searchWord 유무에 따라 분기                                │
│   - StringUtils.hasText(filter.getSearchWord())              │
│     → 있으면: fetchBrands(filter)                             │
│     → 없으면: fetchBrands() (전체 조회)                        │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Repository                                                  │
│   BrandFindRepositoryImpl                                    │
│   - fetchBrands(): 전체 브랜드 조회                           │
│   - fetchBrands(BrandFilter): 검색어 필터 조회                │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│   Database                                                    │
│   Table: brand                                                │
│   Columns: brand_id, brand_name, brand_icon_image_url,       │
│            display_english_name, display_korean_name          │
└──────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Query 분석

### 조회 테이블

| 테이블 | Alias | 조인 유형 | 조인 조건 |
|--------|-------|----------|----------|
| brand | brand | FROM | - |

### QueryDSL 코드

#### 전체 조회 (searchWord 없음)

```java
@Override
public List<BrandDisplayDto> fetchBrands() {
    return queryFactory
            .select(
                    new QBrandDisplayDto(
                            brand.id,
                            brand.brandName,
                            brand.displayKoreanName,
                            brand.brandIconImageUrl))
            .from(brand)
            .fetch();
}
```

#### 검색 조회 (searchWord 있음)

```java
@Override
public List<BrandDisplayDto> fetchBrands(BrandFilter filter) {
    return queryFactory
            .select(
                    new QBrandDisplayDto(
                            brand.id,
                            brand.brandName,
                            brand.displayKoreanName,
                            brand.brandIconImageUrl))
            .from(brand)
            .where(brandNameLike(filter))
            .fetch();
}

private BooleanExpression brandNameLike(BrandFilter filter) {
    if (StringUtils.hasText(filter.getSearchWord())) {
        return brand.displayKoreanName
                .like("%" + filter.getSearchWord() + "%")
                .or(brand.displayEnglishName.like("%" + filter.getSearchWord() + "%"));
    }
    return null;
}
```

### WHERE 조건

| 조건 메서드 | 필드 | 설명 |
|------------|------|------|
| brandNameLike | displayKoreanName, displayEnglishName | 한글명 OR 영문명 LIKE 검색 |

### 생성되는 SQL (예상)

```sql
-- 전체 조회
SELECT
    b.brand_id,
    b.brand_name,
    b.display_korean_name,
    b.brand_icon_image_url
FROM brand b;

-- 검색 조회
SELECT
    b.brand_id,
    b.brand_name,
    b.display_korean_name,
    b.brand_icon_image_url
FROM brand b
WHERE b.display_korean_name LIKE '%나이키%'
   OR b.display_english_name LIKE '%나이키%';
```

---

## 📁 소스 파일 경로

| 구분 | 파일 경로 |
|------|----------|
| Controller | `bootstrap/bootstrap-legacy-web-api/src/main/java/com/setof/connectly/module/brand/controller/BrandController.java` |
| Service Interface | `bootstrap/bootstrap-legacy-web-api/src/main/java/com/setof/connectly/module/brand/service/BrandFindService.java` |
| Service Impl | `bootstrap/bootstrap-legacy-web-api/src/main/java/com/setof/connectly/module/brand/service/BrandFindServiceImpl.java` |
| Repository Interface | `bootstrap/bootstrap-legacy-web-api/src/main/java/com/setof/connectly/module/brand/repository/BrandFindRepository.java` |
| Repository Impl | `bootstrap/bootstrap-legacy-web-api/src/main/java/com/setof/connectly/module/brand/repository/BrandFindRepositoryImpl.java` |
| Request DTO | `bootstrap/bootstrap-legacy-web-api/src/main/java/com/setof/connectly/module/brand/dto/BrandFilter.java` |
| Response DTO | `bootstrap/bootstrap-legacy-web-api/src/main/java/com/setof/connectly/module/brand/dto/BrandDisplayDto.java` |
| Entity | `bootstrap/bootstrap-legacy-web-api/src/main/java/com/setof/connectly/module/brand/entity/Brand.java` |

---

## 🔗 다음 단계

### DTO 변환
```bash
/legacy-convert web:BrandController.fetchBrands
```

### Persistence Layer 생성
```bash
/legacy-query web:BrandController.fetchBrands
```
